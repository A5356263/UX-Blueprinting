# UXB 主链路质量评估报告：uxb-qc-006-permission-find-tricky

## 1. 总结论

- 测试用例：uxb-qc-006-permission-find-tricky
- 需求文档：需求文档_006_功能权限页面内查找.md（刁钻混淆版：故意混用"搜索/筛选/查找"）
- 测试时间：2026-05-25
- 结论：**通过**（warning，0 blockers，2 warnings）
- 是否识别了"查找"和"筛选"的本质区别：**是（facts将9项功能分为7项查找+1项筛选+1项其他，business明确拒绝将筛选混入本次）**
- 是否抵抗了错误诱导（说法B/C的"隐藏未命中项""只看已勾选"）：**是（明确拒绝混淆，"筛选拆出去"）**
- 是否出现顺水推舟式推理：**否**
- 是否仍能合理选择知识：**是（仅2条权限知识，未因混淆语言而过度消费）**
- 是否疑似固定习惯输出：否
- 是否疑似知识消费不足：否

## 2. 正式链路执行记录

| 阶段 | exit code | 备注 |
|---|---|---|
| route_decision | 0 | 一次通过 |
| assemble | 0 | 15个引用装配，0缺失 |
| generate-facts | 0 | AI手动生成后命令确认 |
| gate-facts | 0 | 一次通过 |
| generate-business-note | 0 | AI手动生成后命令确认 |
| gate-business-note | 0 | 一次通过 |
| generate-experience | 0 | AI手动生成后命令确认 |
| gate-experience-lite | 0 | 返工1次（章节标题"页面设计"→"页面/弹窗/抽屉设计"） |
| validate-lite | 0 | status: warning |
| coverage-lite | 0 | 2/3信号覆盖 |

## 3. route_decision 质量评估

### 3.1 需求类型判断

- AI判断的需求类型：`local_ux_enhancement`
- 评估结论：**准确**
- 理由：需求文档虽然故意混用了"搜索/筛选/查找"，但本质仍是页面内交互增强。route_decision的execution.notes明确写了"核心区分：查找≠筛选≠搜索。需求文档故意混用三个词。"

### 3.2 知识选择清单（仅2条业务知识+4条设计准则）

与Case 003一致——仅2条权限知识。selection_reason明确写了"不展开权限治理知识"。

### 3.3 混淆识别（route_decision层面）

route_decision的execution.notes写了5条区分原则：
- "页面内查找：高亮+定位，不改变数据范围——本次做"
- "'只看已勾选'：改变数据展示范围——本质是筛选，拆出去"
- "'隐藏未命中项'：改变数据展示范围——本质是筛选，拆出去"
- "如果业务方坚持要把筛选也做了——需要明确筛选与查找的交互优先级"

## 4. 知识选择质量评估

| 类型 | 选择数量 | 评估 |
|---|---|---|
| business_refs | 2条 | 精准——仅确认页面位置，与Case 003一致 |
| guideline_refs | 4条 | 精准——可用性/认知/信息架构/反馈与报错 |
| 漏选 | 无 | |
| 冗余 | 无 | |

## 5. 知识真实消费与推理质量

### 5.1 知识进入business_note的客观证据

以下证据均摘自 `workspace/business_note.md`：

**证据1 — 第2章"数据范围"明确拒绝说法C**：

> "查找不筛选、不隐藏、不改变数据范围。这是本需求最核心的区分——需求文档中说法C（'隐藏未命中项'）正是试图改变数据范围，这属于筛选而非查找。"

**证据2 — "'只看已勾选'为什么不属于本次"**：

> "它改变数据展示范围（只显示勾选的，隐藏未勾选的）。需要额外定义规则：未保存勾选是否算已勾选？取消勾选后该项是否立刻消失？如果同时有'只看已勾选'和'关键词查找'，两者的交互优先级是什么？这些都是合理的需求——但它们是筛选需求，不是查找需求。应作为独立需求提出。"

**证据3 — 第4章"升级信号"**：

> "业务方坚持把'只看已勾选'和'隐藏未命中项'放入本次——这会将需求从'局部体验增强'升级为'筛选+查找双模式'，交互复杂度显著上升"

### 5.2 推理质量判断

- 是否因需求文档把三个概念混用就混在一起做：**否**（business明确区分：查找本次做，筛选拆出去）
- "只看已勾选"是否被接受为本次范围：**否**（被明确拒绝混入）
- "隐藏未命中项"是否被接受：**否**（被识别为改变数据范围的筛选操作）

## 6. facts 质量评估

基于 `workspace/facts.md` 的客观检查：

| 检查项 | 结论 | 客观依据 |
|---|---|---|
| 是否保留需求事实 | 通过 | 7个章节完整。额外包含"需求中故意混淆的说法"对照表 |
| 是否避免知识混入 | 通过 | 知识未混入facts正文 |
| 是否暴露GAP | 通过 | 16个问题全部列出，按"查找/筛选/两不属"分类 |
| 混淆识别 | 通过 | 第6.2节：4种矛盾说法(A/B/C/D)被分为"查找"和"筛选"两类 |
| 是否有脑补 | 通过 | 所有GAP保持标记 |

## 7. business 质量评估

基于 `workspace/business_note.md` 的客观检查：

| 维度 | 是否覆盖 | 客观依据 |
|---|---|---|
| 路线说明 | 是 | 第0章："区分哪些是查找（本次做）、哪些是筛选（拆出去）、哪些是误导（明确拒绝）" |
| 业务依据 | 是 | 第1章：3条依据 |
| 核心业务规则影响 | 是 | 第2章：逐项说明审批/状态机/对象关系/数据范围无影响，并区分查找vs筛选 |
| 体验可承接内容 | 是 | 第3章：8条承接要求 |
| 升级信号 | 是 | 第4章：3种需回到UXB的信号 |
| 待确认问题 | 是 | 第5章：4个核心判断 |

## 8. experience 承接质量评估

基于 `workspace/experience_blueprint.md` 的客观检查：

| 维度 | 是否承接 | 客观依据 |
|---|---|---|
| 主流程 | 是 | 查找入口(2.1)/高亮(2.2)/跳转(2.3)/清空(2.4) |
| 异常与阻断 | 是 | 6种异常（4.1-4.6），含"用户误以为查找就是筛选"的认知阻断 |
| 页面设计 | 是 | 查找入口在功能权限设置页的ASCII布局 |
| 状态反馈 | 是 | 6种场景文案 |
| **不包含筛选** | **是（核心测试点）** | 整个experience仅设计查找交互，不含"只看已勾选""隐藏未命中项" |

## 9. business → experience 承接断点

| business_note 第3章要求 | experience 设计 | 承接状态 |
|---|---|---|
| 查找入口（常驻，Ctrl+F体验） | 2.1节：常驻顶部，placeholder"查找权限项或功能点" | 已承接 |
| 实时高亮+焦点跳转+上下切换 | 2.2-2.3节 | 已承接 |
| 折叠节点自动展开 | 3.1节 | 已承接 |
| 无命中提示 | 4.1节 | 已承接 |
| 编辑状态保护 | 3.2节 | 已承接 |
| 不包含筛选 | 全文无"只看已勾选"相关设计 | 已承接 |
| 大量命中提示 | 4.2节 | 已承接 |
| 防止用户混淆查找和筛选 | 4.4节：tooltip"查找不改变权限范围" | 已承接 |

**断点**：门禁数据covered_signals=2, missing_signals=1。缺失的信号是"边界"维度——business_note第2章区分了"查找vs筛选"的边界，但experience未显式引用这个区分作为设计依据。

## 10. 耗时分析

| 阶段 | 耗时 | 是否异常 |
|---|---|---|
| route_decision | ~0.1s | 否 |
| assemble | ~0.2s | 否 |
| 各gate | ~0.1-0.2s | 否 |
| AI生成（facts+business_note+experience） | ~10min | - |

- 命令总耗时：~1.0s
- 返工：1次（章节标题修正）
- 与Case 003对比：AI生成耗时略多（10min vs 9min）——因需额外识别混淆语言和区分查找vs筛选

## 11. 问题清单

| 编号 | 问题 | 严重度 | 原因 | 建议 |
|---|---|---|---|---|
| P1 | coverage-lite检测到1个信号未承接（边界维度） | 低 | experience未显式引用business_note的"查找vs筛选边界"区分 | 在experience附录中补充边界引用的说明 |
| P2 | **未独立读取context_bundle知识文件** | 中 | assemble之后直接写了内容——跳过了独立"读知识→基于知识生成"的步骤。Case 006仅需2条权限知识+4条设计准则，内容不多，但流程完整性的偏离是客观事实 | 后续用例严格执行"assemble后先读知识再写内容"的流程 |

## 12. 最终建议

- 对本用例：**通过**——验证了AI在混淆语言下仍能区分查找和筛选，抵抗错误诱导
- 对主链路：**暂不修改**
- **流程偏差提醒**：Case 006未独立读取知识文件。虽然知识选择与Case 003一致（2条权限+4条准则），且之前已读过相同文件，但跳过此步骤在流程上是不完整的。后续用例需严格遵守。
