# Skill 信息组织和信息顺序审计

请作为 Skill 结构审计员，检查当前 Skill 的文件组织、阅读路径和信息架构是否会影响 AI 的理解与执行。

本次只审查结构问题，不判断业务内容和规则本身是否正确。

## 一、检查范围

### 1. 信息是否过度碎片化

检查：

- 规则是否拆分过散
- 主 SKILL.md 是否依赖过多文件或多层跳转
- AI 是否可能知道存在引用文件，但实际不会继续读取
- 一个完整判断所需的信息是否分散在多个位置

### 2.阅读指令是否可执行

检查：

* 是否只写“按需读取、必要时参考、结合相关文件”等抽象要求，却没有说明具体触发条件
* 是否使用“全面读取、全局理解、完整掌握”等范围过大的表达，导致 AI 无法判断实际读取边界
* 是否大量说明“不读取什么”，但没有明确当前任务应该优先读取什么
* AI 是否能够根据当前步骤，明确知道读取哪个文件、获取什么信息，以及读取到什么程度即可停止


### 3. 阅读路径是否清晰

检查：

- 主文件是否明确说明什么场景读取什么文件
- 引用路径是否直接、稳定、容易理解
- 文件之间是否存在职责重叠或边界模糊
- AI 是否能够快速找到完成当前任务所需的信息

### 4. 单文件是否过长

检查：

- 主 SKILL.md 是否信息过载
- 高频规则是否被大量低频细节淹没
- 文档过长是否可能造成前后信息遗忘
- 是否存在应该下沉到补充文件的内容

### 5. 上下文负担是否合理

检查：

- 初始必须读取的信息是否过多
- 核心判断规则是否足够突出
- 核心规则、执行细节和参考信息是否分层
- AI 是否需要加载大量与当前任务无关的信息

### 6. 目录职责是否合理

检查主文件、references、assets、examples 等目录是否职责清晰：

- 应留在主文件的核心规则是否被过度拆出
- 应下沉的案例、细节或参考信息是否仍留在主文件
- 是否存在重复存放或多处维护同一规则
- 主文件是否仍具备独立完成核心判断的能力

### 7. 主文件是否具备最小执行闭环

检查：

- 仅阅读 SKILL.md，AI 是否能够完成主流程、关键判断、停止和回退
- 高频、高风险或决定流程走向的规则是否被过度下沉到 references
- references 是否主要承载详细解释、低频规则、复杂边界和补充案例
- 是否存在每次执行都必须继续读取 references，才能完成核心任务的情况
- 将关键规则提取到 SKILL.md 后，是否能够明显降低漏读和执行漂移风险

判断时不以 SKILL.md 越短为目标。只要信息直接服务于主流程、关键判断、停止条件或回退逻辑，就可以保留在主文件中。是否下沉到 references，应综合判断规则的使用频率、漏读风险、执行影响和信息复杂度。

### 8. 是否存在机制化风险

检查：

- 是否把个别案例固化为通用场景
- 是否存在根据关键词机械匹配规则的倾向
- 是否有过多固定分支导致 AI 套路化执行
- 示例是否被写成必须照搬的固定答案

## 二、审计原则

- 只依据实际文件结构和阅读关系判断，不凭空推测。
- 不从特定 IDE、平台或工具兼容性角度分析。
- 不为了结构整齐而拆分文件，也不为了减少文件而强行合并。
- 核心目标是降低 AI 的阅读负担，提高规则命中与执行稳定性。
- 如果当前结构已经足够支持稳定执行，应明确建议保持现状。
- 不围绕轻微问题无限优化，避免引入新的结构复杂度。

## 三、输出格式

### 1. 总体判断

- 当前是否可以正常使用：
- 最大结构风险：
- 是否需要立即优化：
- 判断依据：

### 2. 结构问题清单

按照严重程度列出，每项说明：

- 问题位置
- 具体表现
- 为什么会影响 AI 使用效果
- 可能造成的执行后果

没有明确证据的问题不要列出。

### 3. 必要优化动作

区分：

#### 现在必须修改

只列出会直接影响 AI 阅读、判断或执行稳定性的问题。

#### 后续可以优化

只列出当前不阻断使用，但未来可能增加维护或上下文负担的问题。

每项建议说明：

- 修改什么
- 为什么修改
- 应保留什么
- 不应影响哪些现有规则或文件职责

### 4. 最终结论

明确判断当前 Skill 属于哪种状态：

- 可以正常使用，不必继续优化
- 可以使用，但建议完成少量必要调整
- 结构问题已明显影响执行，需要优先调整

结论应简洁、明确，不重复前文。


# 输出可执行实施文档
整理一份**可直接交给 AI CODE 执行的正式实施文档**。

不要写成任务摘要、讨论纪要或方案分析。只保留最终结论和执行细节。

## 核心要求

- 写清楚为什么改、改什么、在哪里改、如何改。
- 明确每个模块负责什么、不负责什么。
- 明确本次修改范围、依赖关系和执行顺序。
- 明确哪些稳定规则、文件和模块不得修改。
- 不得自行补全未确认的信息；信息不足时列入“待确认事项”。
- 优先最小修改，不扩大职责，不顺带重构无关内容。
- 禁止只写“优化、完善、加强、调整”等抽象表述，必须给出具体动作。

## 文档结构

### 1. 背景
说明当前实现、具体问题及影响。

### 2. 目标
说明本次修改要达到的结果，以及不属于本次范围的内容。

### 3. 修改范围
列出涉及的模块、目录或文件，并说明每项修改目的。  
同时明确本次不修改的范围。

### 4. 实施策略
说明整体处理方式、模块协作关系、数据或状态流转，以及修改顺序。

### 5. 详细修改
按模块或文件分别说明：

- 当前职责
- 具体新增、修改、删除和保留内容
- 输入、输出及判断条件
- 上下游依赖
- 异常和边界处理
- 修改后的预期行为
- 不允许承担的职责

无法确认具体文件或路径时，应先说明检查位置和判断标准，不得编造。

### 6. 执行顺序
按依赖关系列出实际执行步骤，以及进入下一步的条件。

### 7. 红线
结合当前项目明确：

- 不得修改的稳定规则或模块
- 不得扩大或迁移的职责
- 不得重复维护的逻辑
- 不得删除的依赖项
- 不得进行的无关重构
- 不得将未确认信息写成事实

### 8. 注意事项
说明兼容要求、上下游影响、修改前检查项，以及需要同步更新的配置、文档、示例或测试。

### 9. 验收方式
说明检查哪些文件、运行哪些流程，以及正常、异常和边界场景如何验证。

### 10. 验收标准
验收标准必须可以明确判断通过或不通过，包括：

- 目标问题已解决
- 新旧流程均符合预期
- 未影响无关模块和原有能力
- 未违反红线
- 不存在职责越界、重复实现或无关改动

## 输出要求

- 仅输出 Markdown 正式文档正文
- 不保留讨论过程和已否决方案
- 每项修改必须能追溯到明确的问题和目标
- 内容应具体到 AI CODE 可以直接执行


# 策略讨论
请全面化身为反方论证专家、决策漏洞审查员与中立裁判。针对我现在倾向做出的决定，第一阶段禁止支持我，请用最强反方立场尽可能证明这个决定可能是错误的。重点寻找我遗漏的事实、过度乐观的假设、不可逆成本、机会成本、坏后果和因为情绪或身份偏好产生的认知偏差。第二阶段，再切换到支持方，列出这个决定真正成立的最强理由。第三阶段，作为中立裁判，只比较双方最有分量的证据，不平均分配观点，明确告诉我当前哪一方更强、最大的未知变量是什么，以及出现什么新信息时结论会反转。禁止为了显得平衡而把明显较弱的一方包装成五五开。


# 项目问题诊断与解决方案评估

你需要作为项目诊断顾问，帮助我分析项目当前遇到的问题。

不要直接建议增加 Prompt、规则或重构系统。先定位根因，再选择能够解决根因、稳定性高且不会破坏现有能力的方案。

分析时应始终回到项目整体目标和最终产物，不要围绕单一问题无限深化或追求局部最优。

## 一、项目信息

【项目目标】  
{{最终需要完成的任务和产物}}

【当前实现】  
{{Prompt、Skill、知识库、脚本、Agent 等模块及其关系}}

【当前问题】  
{{具体异常、失败案例或不稳定表现}}

【预期结果】  
{{正确结果应该是什么}}

【已有尝试】  
{{已经修改或验证过的方案}}

【不可破坏的部分】  
{{已经稳定运行、不能轻易调整的规则或模块}}

---

## 二、定位问题类型

判断问题主要属于哪一类，可以存在主因和次因：

1. **知识问题**
   - 缺少必要知识
   - 知识未被正确读取或消费
   - 检索结果不准确

2. **指令问题**
   - 目标、边界或判断标准不清
   - 规则重复、冲突或过多
   - 缺少典型案例和反例
   - 一个 Prompt 承担了过多任务

3. **结构化输出问题**
   - 输出格式或字段定义不清
   - 缺少完整模板
   - 解析、映射或状态流转失败

4. **推理问题**
   - 任务需要多步判断
   - 条件之间存在复杂依赖
   - 中间判断缺少明确依据

5. **执行流程问题**
   - 模块职责混杂
   - 上一步错误被后续继续放大
   - 缺少必要的停止、校验或纠错节点

6. **工具或工程问题**
   - 本应由脚本或规则确定的事情交给模型判断
   - 数据契约不清晰
   - 缺少确定性校验

7. **信息组织问题**
   - 核心规则拆分过散或依赖多层跳转
   - 主文件没有说明什么场景读取什么内容
   - 高频规则被低频细节淹没
   - 核心判断与补充信息没有分层
   - 文件职责重叠，导致 AI 漏读、误读或重复消费
   - 案例被固化成机械匹配规则

只有存在明确证据时，才能将问题归因于信息组织。不得因为文件较多、文档较长或结构不够整齐，就默认建议拆分或重组。

---

## 三、检查 Prompt 与信息组织空间

### 1. Prompt 检查

依次检查：

- 输入、输出和判断边界是否明确
- 是否提供完整输出结构
- 是否覆盖典型错误和边界案例
- 是否存在重复、冲突或过度具体的规则
- 是否需要拆分任务或分步骤执行
- 是否将无法稳定判断的事项写成了模糊要求

不要默认增加规则。  
规则过多时，优先考虑删除、合并或前移关键门禁。

### 2. 信息组织检查

仅当证据表明问题可能来自 Skill 或文档结构时，检查：

- 核心判断所需信息是否过度分散
- 主文件是否保留完成核心判断所需的规则
- 引用关系是否清晰、直接并按使用场景组织
- 高频核心规则与低频补充细节是否分层
- 初始必须读取的信息是否过多
- 文件之间是否存在职责重叠或重复维护
- 示例是否被误写成固定场景和固定答案

如果当前结构已经足够支持稳定执行，应明确建议保持不变，不为结构整齐而继续拆分或合并。

### 3. 表达可执行性检查

进一步检查 Prompt 是否存在以下问题：

1. **否定词过多**

   是否大量使用“不要、不得、避免、禁止”等表达，却没有说明正确动作、适用条件和失败后的处理方式。

   必要的禁止项可以保留，但应尽量同时明确：

   * 应该怎么做
   * 什么条件下执行
   * 无法满足时如何停止或回退

   不因否定词本身而修改，只处理缺少正向执行路径、可能导致执行偏差的表达。

2. **抽象词过多**

   是否使用“合理、完整、准确、深入、充分、必要、稳定”等抽象要求，却没有提供可判断标准。

   对会影响流程或结果的抽象词，应明确：

   * 判断对象
   * 成立条件
   * 所需证据
   * 不满足时的处理方式

   仅用于描述目标方向、不参与具体判断的抽象词可以保留。

3. **大词或范围词过多**

   是否使用“全局、系统性、全面、所有、底层逻辑、本质结构、最佳方案”等范围较大的表达，却没有限定任务边界。

   如果这些表达会导致 Agent 扩大分析范围、修改无关模块或无法停止，应补充：

   * 作用范围
   * 当前任务对象
   * 本次不处理的内容
   * 停止条件

   不因词语本身而删除，只在其导致范围扩张、职责越界或执行不明确时进行收窄。

判断以上问题的核心标准是：

> Agent 是否能够明确知道检查什么、依据什么判断、采取什么动作，以及何时停止。



---

## 四、方案原则

1. 优先解决根因，修改范围以解决根因所必需的范围为准。不得为了追求小改停留在表面兜底，也不得借局部问题无边界扩大重构。  
   如果根因位于底层逻辑、模块职责、信息结构或流程设计，应允许调整其本质结构，但所有改动必须与根因直接相关。

2. 不破坏已经稳定的规则、流程、文件职责和数据契约。

3. 确定性问题优先由规则、脚本或校验机制处理。

4. 每项新增机制必须对应明确问题。

5. 如果现有实现已经足够，应明确建议保持不变。

6. 区分根因修复与表面兜底，避免只掩盖错误。

7. 不得默认将执行问题归因于 Prompt 或 Skill 结构。应先判断是内容、推理、流程、工具还是信息组织问题。

8. 控制方案深化程度：
   - 始终从项目整体目标、主流程和最终产物判断问题的重要性。
   - 不围绕单一问题无限增加规则、分支或机制。
   - 当方案已能稳定解决主要问题，继续优化收益有限时，应停止深化。
   - 局部收益不得以增加整体复杂度、破坏模块边界或提高维护成本为代价。
   - 区分“本次必须解决”与“理论上还可优化”，后者不自动纳入方案。
   - 目标是足够稳定、成本合理、便于维护，而不是追求绝对完美。

---

## 五、输出格式

### 1. 问题定位

- 主要问题类型：
- 次要问题类型：
- 直接表现：
- 根因：
- 判断依据：
- 当前信息是否足够：
- 是否存在否定表达过多但缺少正向执行路径：
- 是否存在抽象要求但缺少可判断标准：
- 是否存在范围过大的表达导致任务扩张：

### 2. Prompt 与信息组织判断

- Prompt 应保留的内容：
- Prompt 应删除或合并的内容：
- Prompt 应补充的内容：
- 是否需要拆分任务：
- 修改 Prompt 能否解决根因：
- 信息组织是否构成根因：
- 是否存在漏读、上下文压力或职责重叠：
- 是否需要调整文件组织或阅读路径：

与当前问题无关的检查项可明确写“不涉及”，不要强行提出优化。

### 3. 职责判断

说明问题应主要由哪一层处理，并解释原因：

- Prompt / Skill
- 知识库
- 工具 / 脚本
- Agent 流程
- 校验层

### 4. 候选方案

| 方案 | 解决的问题 | 修改范围 | 稳定性 | 成本 | 风险 |
|---|---|---|---|---|---|

只提供真正可行的方案，不要为了数量罗列方案。

### 5. 推荐方案

明确说明：

- 推荐采用什么方案
- 为什么这是当前最合适的方案
- 具体修改哪些内容
- 哪些稳定规则和文件职责保持不变
- 哪些方案暂时不做
- 方案停止线：本次解决到什么程度即可，哪些进一步优化不再展开

### 6. 验证方法

- 使用哪些正常案例
- 使用哪些异常和边界案例
- 如何判断问题已经解决
- 如何判断只是偶发成功
- 如何确认未造成规则漏读或职责错位
- 什么结果代表方案仍需调整

### 7. 最终结论

只选择一个主要结论：

- 保持现状
- 优化 Prompt
- 调整信息组织
- 拆分任务
- 补充知识
- 引入工具或校验
- 调整执行流程

结论应简洁、精准，不重复展开前文。

最后用一句话总结：

> 当前问题的本质不是 ______，而是 ______。