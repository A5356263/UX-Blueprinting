# 产品脑暴记录（聚焦版）

**主题**：员工自助申请权限
**HMW**：我们如何能让员工在现有权限判定链路（L0可见性→L1功能权限→L2数据范围→L3治理与生效）的框架下，自助发起权限申请，并自然汇入已有的来源模型、治理状态模型和审计契约？
**日期**：2026-06-05
**产出创意数**：11个（全部基于现有架构约束）

---

## 架构锚点（脑暴约束条件）

本次脑暴严格遵循以下现有产品架构约束，不发明新概念、不制造平行架构：

**决策链路**：L0可见性（APP_VISIBILITY不可见具有覆盖性）→ L1功能权限（ACL_DIRECT + RBAC_ROLE叠加）→ L2数据范围（范围类型+条件组）→ L3治理与生效（GOVERNANCE_MODE修饰）

**来源模型**：ACL_DIRECT（个人直授）、RBAC_ROLE（角色授予）、APP_VISIBILITY（应用可见性）、COLLAB_VISIBILITY（协作可见性）+ GOVERNANCE_MODE（修饰因子，不作为来源）

**治理状态**：draft → pending → approved → effective / rejected / revoked

**已有治理模式**：子管理员模式（与双管理员互斥，R-009）、双管理员模式、权限变更审批模式（依赖外部审批中台）

**配置主路径**：用户授权 → 功能授权 → 数据授权 → 权限详情

**已有页面承载**：People-centric（用户授权/功能授权/数据授权/权限详情）、Role-centric（角色管理/授权规则）、App-centric（应用管理/应用设置页）、Governance（权限管理模式/子管理配置/双管理员配置/权限变更审批配置）、查询排障（权限查询/按用户查询结果）

**体验风险红线**：TR-003概念不增殖、TR-001可解释性优先、TR-006安全第一

**已标注GAP**：按权限查/功能点查承载不明确、按变更查分散在权限域与审批域之间缺少统一台账、批量授权后结果核对页未明确

---

## 第一步：问题聚焦

### 1. 谁有这个问题？

- 员工（subject）：在日常工作中遇到"可见但不可操作"（GRANT.NO_FUNCTION_GRANT）或"可操作但数据范围为空"（SCOPE.DATA_SCOPE_EMPTY）的场景，需要获取权限但目前只能私下找管理员。
- 超级管理员/子管理员：当前是权限变更的唯一发起方（配置路径：用户授权→功能授权→数据授权），所有权限请求都汇聚到管理员侧处理。
- 审批人：在权限变更审批模式下已有审批职责，但目前只处理管理员发起的变更，不处理员工发起的申请。

### 2. 现在怎么解决？

员工通过IM、电话、当面沟通等方式联系管理员，管理员在后台走现有配置路径（用户授权→功能授权→数据授权）完成授权。整个过程：不走审批中台（除非已开启权限变更审批模式）、不经过治理状态流转（直接effective）、无结构化审计记录（不满足审计契约的9个最低字段要求）。

### 3. 为什么重要？

- 管理员配置路径被打断：管理员需要频繁从其他工作切换到用户授权页面处理琐碎请求。
- 授权过程无治理保障：未经审批直接effective，如果企业未开启权限变更审批模式，则完全没有审批环节。
- 审计闭环缺失：线下沟通的授权不满足审计契约的who/to_whom/what/when/why/approval/outcome/snapshot字段要求。
- 员工体验断裂：员工在"权限查询→按用户查询结果"中只能被动查看，不能在发现缺失时直接发起申请。

### 4. 什么算成功？

员工自助发起的权限申请，经过治理状态流转（draft→pending→approved→effective），最终授权结果落入现有来源模型（ACL_DIRECT或RBAC_ROLE），审计记录满足9个最低字段，且不引入新的权限概念（TR-003）。

### 5. 约束条件

- 申请结果必须落入已有来源枚举：ACL_DIRECT或RBAC_ROLE，不能发明新来源。
- 审批必须对接已有的权限变更审批模式配置页和审批中台，不另建审批引擎。
- 数据范围选择必须遵循已有的"范围类型+条件组"模型。
- 审计记录必须满足已有审计契约。
- 不增殖新概念（TR-003），不破坏已有判定链路。
- 子管理员模式与双管理员模式的互斥关系（R-009）不受影响。

### HMW

**我们如何能让员工在现有四层判定链路和五种来源模型的框架下，自助发起权限申请并自然汇入已有的治理状态流转和审计契约？**

---

## 第二步：SCAMPER七维度发散

### S - 替换

**创意1：用"权限查询"页面替换独立的"申请入口"**

当前"权限查询→按用户查询结果"已支持按应用聚合展示某用户的所有权限，包含来源解释和原因码。将这条查询路径延伸为员工自己的权限视图：员工进入后看到的是"我的权限"，缺失的权限以原因码（GRANT.NO_FUNCTION_GRANT / SCOPE.DATA_SCOPE_EMPTY）展示，每个缺失项旁边附带"申请"按钮。申请发起后，授权结果自然落入ACL_DIRECT来源，在同一个页面中可以查看来源和生效状态。

替换了什么：用已有的查询解释链路（权限查询→按用户查询结果→权限详情）替换需要从零设计的"员工权限中心"。
锚定现有架构：复用查询路径1.1（按用户查），复用来源模型解释，复用原因码定位。

**创意2：用已有的"授权规则"引擎替换低风险的审批人工环节**

角色管理页的"授权规则"已支持按部门、职务、成员组等条件自动授予和回收角色。将员工申请的低风险权限（如只读类功能权限）转化为一条临时授权规则：申请审批通过后，系统不是直接做ACL_DIRECT授予，而是创建一条条件匹配的授权规则，将员工与对应权限关联。这样低风险权限可以通过规则实现自动授予，与现有的RBAC_ROLE来源保持一致。

替换了什么：用已有的授权规则引擎替换"人工逐条审批+手动配置"的模式。
锚定现有架构：RBAC_ROLE来源、角色管理→授权规则路径。

### C - 组合

**创意3：将申请表单与已有配置路径语义对齐**

现有管理员配置路径为：用户授权→功能授权→数据授权。员工申请表单应直接复用这个三步语义：第一步选择功能权限（从可申请范围内勾选，以末级菜单或操作点为粒度，与L1功能权限一致）；第二步选择数据范围（遵循范围类型+条件组模型：先选全部/部分/无，部分时进入条件组配置）；第三步填写申请原因并提交。审批人看到的申请内容，与管理员在功能授权和数据授权页面看到的配置语义完全一致，无需二次翻译。

组合了什么：将"申请"动作与"配置"路径的语义合并，审批人无需在不同心智模型间切换。
锚定现有架构：功能授权页面语义、数据范围"范围类型+条件组"模型、L1→L2的前置关系（R-003）。

**创意4：将申请流程与已有的"权限变更审批模式"对接**

权限变更审批模式配置页已支持将权限变更纳入审批流治理，对接外部审批中台。员工自助申请作为这个模式的一个新触发源：管理员发起的变更走现有审批流，员工发起的申请也走同一个审批流，只是"发起方"字段从"管理员"变为"员工"。审批配置、审批节点设计、审批人指定全部复用现有能力，权限域只需要在审批模式配置页增加一个"是否允许员工发起"的开关。

组合了什么：员工申请触发源 + 已有审批治理链路。
锚定现有架构：权限变更审批模式配置页、外部审批中台、治理状态模型（draft→pending→approved→effective）。

### A - 调整

**创意5：借鉴已有"原因码定位"能力构建"申请引导"**

当员工访问无权限功能时，系统已经能通过判定链路定位到失败层级和原因码（VISIBILITY.APP_NOT_VISIBLE / GRANT.NO_FUNCTION_GRANT / SCOPE.DATA_SCOPE_EMPTY等）。将这个定位能力调整为员工端的"申请引导"：根据原因码不同，给出不同的引导策略——APP_NOT_VISIBLE时引导员工申请应用可见性（走应用管理路径）；NO_FUNCTION_GRANT时引导申请功能权限（走自助申请路径）；DATA_SCOPE_EMPTY时引导申请数据范围扩展（走数据授权路径）。不同原因码对应不同申请类型，避免员工在不该申请的地方申请。

调整了什么：将排障定位能力（面向管理员/排障人员）调整为申请引导能力（面向员工）。
锚定现有架构：conflict_reason_code体系、failed_layer定位字段、final_effective_rule。

### M - 修改

**创意6：将"权限详情"页从只读解释修改为"解释+申请"双模式**

当前"权限详情"页承载的是只读解释与核对职责——展示某用户的权限来源、范围、生效状态。修改为双模式：管理员查看时保持只读解释模式；员工自己查看时增加"申请"操作模式。员工在权限详情中看到自己缺少某权限的原因解释（如"可见但未授予对应功能权限"），旁边可直接点击"申请此权限"，申请表单自动预填该权限信息。审批通过后，同一页面自动刷新显示新的来源和生效状态。

修改了什么：权限详情页从纯只读→读+操作双模式。
锚定现有架构：权限详情页面（People-centric能力域）、来源模型解释、原因码展示。

### P - 另作他用

**创意7：将"按变更查"的GAP通过申请台账填补**

文档中已标注GAP："按变更查当前分散在权限域与审批域之间，权限域内缺少统一变更台账页"。员工自助申请恰好产生大量变更记录，以这批数据为基础建立统一的"权限变更台账"页面：同时收录管理员直接变更和员工自助申请的变更记录，统一展示who/to_whom/what/when/why/approval/outcome，满足审计契约要求。这个台账不仅服务自助申请场景，也填补了原有的架构GAP。

另作何用：员工申请数据不只是申请流程的附属产物，而是填补"按变更查"架构GAP的数据源。
锚定现有架构：审计契约9字段、查询路径1.4（按变更查）、已标注GAP。

### E - 消除

**创意8：消除申请表单中超出已有语义的字段**

不发明"申请理由等级""紧急程度""使用时长"等已有体系之外的字段。申请表单只包含：选择功能权限（L1语义）、选择数据范围（L2语义：范围类型+条件组）、填写申请原因（对应审计契约的why字段）。管理员可配置申请原因为"必填"或"选填"，不增加额外字段。保持表单与已有配置路径的语义一致性，员工填写的内容可以直接映射到功能授权和数据授权页面的配置项。

消除什么：消除在申请表单中发明新字段的冲动。
锚定现有架构：功能授权页面字段、数据授权页面字段、审计契约why字段。

### R - 重排

**创意9：重排治理状态流转——申请预校验**

当前配置路径是"管理员配置→（如有审批模式）进入pending→审批通过→effective"。员工申请路径需要重排为"员工提交（draft）→系统预校验→进入审批（pending）→审批通过→系统执行授权→effective"。预校验环节在员工提交时即运行判定链路的前三层（L0/L1/L2），检查申请的合理性：如应用本身对该员工不可见（L0失败），则在提交时直接提示"该应用当前对您不可见，申请功能权限前需先获取应用访问权限"，避免无效申请进入审批流。

重排什么：在治理状态流转中，在draft→pending之间增加"预校验"节点，提前拦截无效申请。
锚定现有架构：四层判定链路、治理状态模型（draft状态）、原因码体系。

---

## 第三步：5 Why根因追问

### 痛点1：员工不知道在哪里申请权限

- Why1：为什么员工不知道去哪里申请？→ 因为当前没有面向员工的权限申请入口。
  - Why2：为什么没有员工入口？→ 因为权限域的所有页面承载都面向管理员（People-centric、Role-centric、Governance），没有面向普通员工的设计。
    - Why3：为什么只面向管理员？→ 因为权限管理一直被定义为"管理员配置"的职责，而非"员工自助"的场景。
      - Why4（根因）：现有入口分区语义（5大入口分区）中缺少"员工自助"分区，能力地图的6大能力域也没有覆盖员工发起方。

**根因方案**：在现有入口分区语义中增加第6个分区——"Employee Self-Service"（员工自助区）。这个分区不发明新的权限模型，而是将已有的查询路径（按用户查→权限详情）和配置路径（功能权限+数据范围语义）以员工视角重新组织。对应能力地图新增一个能力域"员工自助申请"，页面承载为"我的权限"和"权限申请"。

### 痛点2：申请表单与管理员配置路径心智不一致

- Why1：为什么申请表单设计困难？→ 因为需要决定让员工填什么字段。
  - Why2：为什么字段选择困难？→ 因为申请表单的语义需要与授权后的配置结果对应，但两者目前使用不同的表达。
    - Why3（根因）：管理员配置路径（功能授权→数据授权）使用的是系统级技术语义，而员工理解的是业务语义，两者之间缺少"体验翻译层"。

**根因方案**：申请表单严格复用已有配置路径的语义结构（功能权限+数据范围类型+条件组），但通过体验翻译（参考31_体验翻译需求.md的15条转译要求）将技术标识符转为业务可理解的名称。如"finance_report_view_v2"翻译为"查看财务报表"。翻译只改展示层，不改底层模型。

### 痛点3：低风险权限申请也要走完整审批流

- Why1：为什么低风险权限也要审批？→ 因为权限变更审批模式一旦开启，所有变更都进入审批流。
  - Why2：为什么不能按风险等级区分？→ 因为现有的权限变更审批模式没有"按条件自动通过"的能力，它依赖外部审批中台完成流程设计。
    - Why3（根因）：审批中台的流程设计能力是否支持"条件分支自动通过"取决于审批中台本身的配置能力，权限域无法单方面决定。

**根因方案**：在权限域侧，通过已有的"授权规则"（角色管理→授权规则）实现低风险权限的自动授予——低风险权限不进入审批流，而是通过预设的授权规则直接完成RBAC_ROLE关联。中高风险权限走权限变更审批模式。这样"自动通过"的逻辑留在权限域内（授权规则），不依赖审批中台的条件分支能力。

### 痛点4：申请过程中员工无法理解自己为什么缺权限

- Why1：为什么员工不理解？→ 因为判定链路和原因码体系只面向管理员和排障人员暴露。
  - Why2：为什么不对员工暴露？→ 因为原因码使用技术语义（如GRANT.NO_FUNCTION_GRANT），不适合直接展示给员工。
    - Why3（根因）：原因码的"体验翻译"尚未完成——已有原因码体系和文案口径策略（32_文案与解释策略.md），但只覆盖了配置完成后、查询详情中、错误状态下的解释，未覆盖"员工申请前"这个时机。

**根因方案**：在文案与解释策略中增加第4种解释时机——"申请前引导"。为每个原因码定义员工视角的引导文案：VISIBILITY.APP_NOT_VISIBLE→"该应用当前未对您开放，需管理员先开启应用可见性"；GRANT.NO_FUNCTION_GRANT→"您可以看到该功能入口，但尚未获得操作权限，可申请开通"；SCOPE.DATA_SCOPE_EMPTY→"您已拥有操作权限，但数据访问范围为空，可申请扩展数据范围"。引导文案与"申请"按钮联动，将解释转化为行动。

---

## 第四步：竞品启发法（收敛到架构约束内）

### 跨界启发（锚定到现有架构的落地方式）

**1. AWS IAM策略模拟器 → 适配为"权限预览"**

AWS IAM的Policy Simulator允许用户在配置前测试策略效果。在你们架构中的落地方式：不是构建独立的模拟器页面，而是在员工申请表单的"提交确认"环节，复用已有的权限详情页面的解释能力，展示"审批通过后，您将获得的权限效果预览"——包括功能权限清单（L1结果）和数据范围计算结果（L2结果，基于选择的范围类型+条件组）。预览使用权限详情页面已有的展示逻辑，不新建预览引擎。

**2. Okta Access Certification → 适配为"定期权限复核"**

Okta的定期权限认证机制。在你们架构中的落地方式：利用已有的"按用户查"查询路径和权限详情页面，增加"定期复核"触发器——管理员可按周期（如每季度）对通过ACL_DIRECT和RBAC_ROLE授予的权限进行批量复核，标记保留或回收。复核操作走已有的治理状态流转（effective→revoked），审计记录落入已有的变更台账。

**3. 金蝶申请单模式 → 差异化：更轻量的表单+更深的架构融合**

金蝶的申请单是独立表单，与权限配置页面使用不同语义。差异化方向：你们的申请表单与配置路径共享语义（功能权限+数据范围类型+条件组），审批人审批时看到的不是"申请单"而是与功能授权/数据授权页面一致的配置视图，无需在两种心智间切换。

### 差异化启发

**4. 竞品都没有的：原因码驱动的申请引导**

你们已有完整的conflict_reason_code体系和final_effective_rule，竞品通常只有"无权限"这一个笼统提示。差异化：在员工遇到权限阻断时，系统精确定位到失败层级和原因码，根据原因码给出针对性的申请引导（而非统一跳转到一个通用申请页面）。这让申请体验与排障体验共享同一套定位能力，是竞品难以复制的结构性优势。

---

## 第五步：约束创新法

### 约束1：零新增页面

如果只能在现有页面上扩展，不能新增独立页面：
**创意10**：在已有的"权限查询→按用户查询结果"页面增加"自助模式"。管理员看到的是完整的管理视图（含配置操作），员工自己查看自己时进入自助视图（查看已有权限+缺失权限原因码+申请按钮）。申请提交复用已有的权限变更审批模式配置页的审批流。申请记录复用已有的"按变更查"路径（同时填补这个GAP）。

### 约束2：不触碰治理模式

如果完全不能修改权限管理模式页、子管理员配置页、双管理员配置页、权限变更审批模式配置页：
自助申请的审批只能复用权限变更审批模式已有的配置和审批中台对接。管理员在权限变更审批模式配置页中增加一个"员工申请"审批模板（不是新治理模式，而是现有模式下的一个模板）。审批人、审批节点、通知方式全部复用。

### 约束3：只改一个页面

如果只能改一个页面：
改"权限查询→按用户查询结果"——当查询目标为用户自己时，在原因码旁增加"申请"按钮。这是ROI最高的切入点：利用已有的查询能力和原因码定位能力，在发现权限缺失的瞬间引导申请。

### 约束4：不发明新术语

所有面向员工的文案必须使用已有的文案口径策略（32_文案与解释策略.md）中的8种口径，新增文案不创造新的权限术语。"申请"不是新的权限概念，它是现有配置路径的"发起方变更"（从管理员发起变为员工发起）。

---

## 第六步：反向脑暴法

**反向问题：如何让员工的权限申请体验在现有架构下变得最糟糕？**

| 序号 | 恶化方案 | 逆转后的改进方案 | 锚定的现有架构 |
|------|---------|----------------|--------------|
| 1 | 让员工去后台管理页面申请权限 | 在前台已有的"无权限"提示页面嵌入申请按钮，根据原因码精准引导 | conflict_reason_code + 前台页面 |
| 2 | 申请表单用技术术语（ACL_DIRECT、scope_expression） | 用体验翻译后的业务语义展示，但底层模型保持不变 | 31_体验翻译需求的15条转译要求 |
| 3 | 申请通过后管理员还要手动去用户授权页面配置 | 审批通过后系统自动执行授权操作（ACL_DIRECT写入或RBAC_ROLE关联），状态自动流转至effective | 治理状态模型（approved→effective的handoff） |
| 4 | 员工申请了应用不可见的功能（注定失败） | 提交时运行L0预校验，不可见时阻止提交并解释原因 | R-001可见性前置 + R-002不可见覆盖 |
| 5 | 申请表单让员工填数据范围的"交集/并集算法" | 数据范围选择遵循已有"范围类型+条件组"UI，员工只需选择范围和条件，平台侧负责计算 | R-004/R-005/R-006 + 平台侧计算职责 |
| 6 | 审批人看到的申请内容与配置页面的字段完全不一致 | 审批人看到的申请详情复用功能授权和数据授权页面的展示逻辑 | 功能授权页面 + 数据授权页面的语义 |
| 7 | 申请记录和审批记录分散在不同地方 | 所有变更记录统一落入"权限变更台账"，同时填补按变更查的GAP | 审计契约9字段 + 查询路径1.4 GAP |

---

## 第七步：Impact/Effort快速筛选

### Quick Wins（高Impact，低Effort）——优先验证

| 序号 | 创意名称 | 简述 | 锚定架构 |
|------|---------|------|---------|
| C01 | 无权限页面原因码引导申请 | 在L0/L1/L2失败页面展示原因码的员工翻译文案+对应类型的申请按钮 | conflict_reason_code + 文案口径策略 |
| C02 | 权限查询页增加员工自助视图 | 复用"按用户查询结果"页面，员工看自己时展示可查看+可申请+缺失原因 | 查询路径1.1 + 来源模型 |
| C03 | 申请表单与配置路径语义对齐 | 功能权限选择+数据范围（范围类型+条件组）+申请原因，与管理员配置语义一致 | 功能授权 + 数据授权页面语义 |
| C04 | L0预校验拦截无效申请 | 提交时运行可见性前置检查，应用不可见时阻止提交并引导 | R-001/R-002 + draft状态 |
| C05 | 申请原因映射审计why字段 | 申请表单唯一自由文本字段直接映射审计契约的why字段 | 审计契约9字段 |

### Big Bets（高Impact，高Effort）——值得投入

| 序号 | 创意名称 | 简述 | 锚定架构 |
|------|---------|------|---------|
| C06 | 员工申请对接权限变更审批模式 | 员工申请作为审批模式的新触发源，复用审批中台对接和审批流配置 | 权限变更审批模式 + 治理状态模型 |
| C07 | 权限变更统一台账 | 以申请数据为基础建立统一变更台账，填补按变更查的GAP | 查询路径1.4 GAP + 审计契约 |
| C08 | 授权规则驱动的自动授予 | 低风险权限通过预设授权规则自动完成RBAC_ROLE关联，跳过人工审批 | 角色管理→授权规则路径 |

### Fill-ins（低Impact，低Effort）——有空再做

| 序号 | 创意名称 | 简述 | 锚定架构 |
|------|---------|------|---------|
| C09 | 申请效果预览 | 提交前复用权限详情展示逻辑预览获批后的权限效果 | 权限详情页面 + final_effective_rule |
| C10 | 定期权限复核触发器 | 在查询路径基础上增加定期复核能力 | 按用户查询 + effective→revoked状态流转 |
| C11 | 原因码员工文案扩展 | 为文案口径策略增加"申请前引导"时机 | 32_文案与解释策略 + 31_体验翻译需求 |

---

## 第八步：创意清单

| 编号 | 创意描述 | 解决的痛点 | 来源框架 | 预期价值 | 实现难度 | 象限 | 架构锚点 | 下一步验证方式 |
|------|---------|----------|---------|---------|---------|------|---------|-------------|
| C01 | 无权限页面原因码引导申请：当员工遇到权限阻断时，系统根据判定链路定位的failed_layer和reason_code，展示员工可理解的引导文案和对应类型的申请按钮（APP_NOT_VISIBLE引导联系管理员，NO_FUNCTION_GRANT引导申请功能权限，DATA_SCOPE_EMPTY引导申请数据范围） | 员工不知道在哪里申请、不知道缺什么权限 | SCAMPER-A | 减少无效申请30%以上（原因码精准引导避免申请错误类型的权限） | 低 | Quick Win | conflict_reason_code体系、32_文案口径策略 | 选3个高频无权限页面，添加原因码引导文案和申请按钮，A/B测试申请转化率 |
| C02 | 权限查询页增加员工自助视图：在已有的"权限查询→按用户查询结果"页面增加自助模式，员工查看自己时展示：已有权限（含来源解释）、缺失权限（含原因码）、可申请权限（含申请按钮）。复用已有的来源模型解释和原因码展示 | 员工没有查看自己权限的入口，不知道缺什么 | SCAMPER-S | 填补员工视角的权限可见性空白，管理员"帮我查下我有什么权限"类咨询减少80% | 低 | Quick Win | 查询路径1.1（按用户查）、来源模型、权限详情页面 | 原型测试：在查询页面增加"查看自己"入口，邀请10名员工测试 |
| C03 | 申请表单与配置路径语义对齐：申请表单分两步——第一步选择功能权限（以末级菜单或操作点为粒度，与L1一致），第二步选择数据范围（范围类型+条件组，与L2一致）。表单结构复用功能授权和数据授权的页面语义 | 申请表单与授权配置使用不同心智模型，审批人需要二次翻译 | SCAMPER-C | 审批人审批效率提升50%（看到的申请内容与配置语义一致） | 低 | Quick Win | 功能授权页面语义、数据授权"范围类型+条件组"模型、R-003功能权限前置数据权限 | 对比测试：审批人分别处理"语义对齐表单"和"传统申请单"，记录审批耗时和错误率 |
| C04 | L0预校验拦截无效申请：员工提交申请时，系统先运行判定链路的L0层，检查目标应用对该员工是否可见。如不可见（VISIBILITY.APP_NOT_VISIBLE），阻止提交并提示"该应用当前未对您开放，请联系管理员开启应用可见性后再申请功能权限" | 员工申请了应用不可见的功能，审批人被迫拒绝，浪费双方时间 | SCAMPER-R | 无效申请减少40%（L0失败在申请前即被拦截） | 低 | Quick Win | R-001可见性前置、R-002不可见覆盖、draft→pending状态流转 | 统计当前被拒绝申请中L0失败（应用不可见）的占比，评估拦截价值 |
| C05 | 申请原因直接映射审计why字段：申请表单中"申请原因"字段直接映射到审计契约的why字段。不增加"紧急程度""使用场景""预计使用时长"等额外字段，保持表单最小化 | 申请表单发明多余字段，与已有审计契约不对齐 | SCAMPER-E | 表单简化，审计记录自动满足合规要求 | 低 | Quick Win | 审计契约9字段（why字段） | 审计团队确认：申请原因字段是否满足审计why字段的要求 |
| C06 | 员工申请对接权限变更审批模式：员工申请作为权限变更审批模式的一个新触发源。管理员在审批模式配置页中增加"员工申请"审批模板（指定审批人、审批节点、通知方式），员工提交后进入draft→pending流转，审批通过后状态流转至approved→effective。复用已有的审批中台对接 | 需要从零建设审批流，开发成本高且与已有治理模式重复 | SCAMPER-C | 审批流开发成本降低70%（复用已有审批模式和中台对接） | 高 | Big Bet | 权限变更审批模式配置页、治理状态模型、外部审批中台 | 技术评估：审批中台是否支持"员工发起"作为新触发源，审批模板配置是否可扩展 |
| C07 | 权限变更统一台账：建立统一的权限变更台账页面，同时收录管理员直接变更和员工自助申请的变更记录。展示审计契约的9个最低字段（who/to_whom/what/where/when/why/approval/outcome/snapshot），支持按变更类型、时间、操作人筛选。同时填补"按变更查分散在权限域与审批域之间"的已标注GAP | 按变更查的GAP（权限域内缺少统一变更台账页） | SCAMPER-P | 填补已标注架构GAP，审计准备时间减少50%以上 | 高 | Big Bet | 查询路径1.4 GAP、审计契约9字段 | 与审计团队对齐台账页面的字段和格式要求，评估数据源完整性 |
| C08 | 授权规则驱动的自动授予：对于管理员标记为"低风险"的权限（如只读类功能权限），不进入人工审批流，而是通过预设的角色授权规则自动完成RBAC_ROLE关联。管理员在角色管理→授权规则中预设规则（如"产品部全员自动关联产品查看角色"），员工申请匹配的权限时系统自动执行规则关联 | 低风险权限也要走完整审批流，审批资源浪费 | 5 Why根因3 | 低风险权限获取时间从小时级降至秒级，审批人工作量降低60% | 高 | Big Bet | 角色管理→授权规则路径、RBAC_ROLE来源、自动授权规则引擎 | 技术评估：授权规则引擎是否支持"申请触发"模式（当前仅支持条件匹配自动授予） |
| C09 | 申请效果预览：员工提交申请前，系统复用权限详情页面的展示逻辑，预览"审批通过后您将获得的权限效果"——包括功能权限清单和数据范围计算结果 | 员工不确定申请的权限是否满足需求 | 约束创新 | 减少"申请后发现不是自己想要的"导致的二次申请 | 中 | Fill-in | 权限详情页面展示逻辑、final_effective_rule | 原型测试：为1个应用设计预览界面，测试是否帮助员工做出正确选择 |
| C10 | 定期权限复核触发器：管理员可按周期触发对ACL_DIRECT和RBAC_ROLE授予的权限进行批量复核，标记保留（保持effective）或回收（流转至revoked）。复核操作复用查询路径，审计记录落入变更台账 | 权限"给了不回收"，越积越多 | 反向脑暴 | 无效权限占比下降，权限体系健康度提升 | 中 | Fill-in | 查询路径、effective→revoked状态流转、审计契约 | 需求确认：管理员是否有定期复核的意愿和操作频率 |
| C11 | 原因码员工文案扩展：在文案与解释策略中增加第4种解释时机"申请前引导"，为每个原因码定义员工视角的引导文案和行动建议 | 原因码的技术语义不适合直接展示给员工 | 5 Why根因4 | 员工自主理解权限阻断原因的比例提升，减少管理员解释成本 | 低 | Fill-in | 32_文案口径策略、31_体验翻译需求、conflict_reason_code | 文案评审：为7个原因码各写一条员工引导文案，请10名员工测试理解率 |

---

## Top 3 推荐创意

### 创意1：无权限页面原因码引导申请 + 申请表单语义对齐（Quick Win组合）

- **描述**：当员工访问无权限功能时，系统基于判定链路定位到failed_layer和reason_code，在页面上展示员工可理解的引导文案（而非技术原因码），并根据失败层级给出对应的申请引导——NO_FUNCTION_GRANT时展示"申请功能权限"按钮，DATA_SCOPE_EMPTY时展示"申请数据范围"按钮。点击后进入申请表单，表单结构与管理员配置路径语义对齐：第一步选择功能权限（末级菜单/操作点粒度，与L1一致），第二步选择数据范围（范围类型+条件组，与L2一致），第三步填写申请原因（映射审计why字段）。提交时运行L0预校验，应用不可见时阻止提交。

- **解决的痛点**：
  - 员工不知道在哪里申请权限（原因码精准引导到正确申请类型）
  - 申请表单与配置路径心智不一致（语义对齐消除翻译成本）
  - 无效申请浪费审批资源（L0预校验提前拦截）
  - 申请记录不满足审计要求（原因字段直接映射审计契约）

- **预期效果**：
  - 管理员收到的"帮我看看我缺什么权限"类咨询减少80%
  - 无效申请（因应用不可见等原因注定被拒的）减少40%
  - 审批人审批效率提升50%（申请内容与配置语义一致，无需二次翻译）
  - 审计合规性自动满足（申请记录覆盖审计契约who/what/when/why字段）

- **实现路径**：
  1. 在前台已有的无权限提示页面，增加原因码识别逻辑（复用判定链路的failed_layer和reason_code输出）。
  2. 为每个原因码编写员工引导文案（扩展32_文案口径策略，增加"申请前引导"时机）。
  3. 开发申请表单组件，复用功能授权和数据授权页面的字段语义（功能权限选择器+数据范围选择器）。
  4. 提交时运行L0预校验（复用判定链路L0层），失败时阻止提交并展示引导。
  5. 申请数据存储时自动映射审计契约字段。

- **验证方式**：选3个高频触达无权限页面的功能点，添加原因码引导+申请按钮，灰度2周，监控申请转化率、无效申请率、审批耗时变化。

- **风险**：
  - 风险1：前台无权限提示页面当前可能不暴露原因码（仅展示笼统的"无权限"）。应对：与前端团队确认当前页面是否已有原因码数据，如无需评估透传成本。
  - 风险2：数据范围"条件组"配置对普通员工可能过于复杂。应对：低风险权限默认"全部数据权限"，仅中高风险展示条件组配置；或提供常见条件组模板。

### 创意2：员工申请对接权限变更审批模式 + 统一变更台账（Big Bet组合）

- **描述**：员工自助申请不另建审批流，而是作为已有"权限变更审批模式"的一个新触发源。管理员在权限变更审批模式配置页中增加"员工申请"审批模板，配置审批人（如组织负责人+权限管理员的双级审批）、审批节点、超时规则。员工提交申请后进入治理状态流转：draft（员工可编辑）→ pending（审批中）→ approved（已通过，系统自动执行授权操作）→ effective（已生效）/ rejected（被拒绝，通知员工含原因）。所有变更记录（无论来自管理员直接变更还是员工申请）统一落入"权限变更台账"页面，展示审计契约9个最低字段，支持按变更类型、时间、操作人筛选。

- **解决的痛点**：
  - 需要从零建设审批流（复用已有审批模式降低开发成本）
  - 按变更查的GAP（统一台账填补架构缺口）
  - 员工申请与管理员变更的审计记录分散在不同位置

- **预期效果**：
  - 审批流开发成本降低70%（复用已有审批中台对接）
  - 审计准备时间减少50%（统一台账自动满足合规要求）
  - 审批人可在同一个审批待办列表中处理管理员变更和员工申请

- **实现路径**：
  1. 技术评估审批中台是否支持"员工发起"作为新触发源，审批模板是否可扩展。
  2. 在权限变更审批模式配置页增加"员工申请"审批模板配置入口。
  3. 实现治理状态流转对接：员工申请创建state=draft的记录，提交后流转至pending，审批通过后系统自动执行ACL_DIRECT写入或RBAC_ROLE关联，状态流转至effective。
  4. 建立权限变更台账页面，数据源同时包含管理员直接变更记录和员工申请记录，按审计契约9字段结构化展示。

- **验证方式**：技术评审确认审批中台扩展可行性后，先实现"员工申请→审批→授权"最小闭环（1个部门试点），再扩展台账页面。

- **风险**：
  - 风险1：审批中台不支持"员工发起"作为新触发源。应对：与审批中台团队对接，评估扩展方案；如不可行，考虑在权限域内实现轻量审批（仅同意/拒绝），但需评估与R-007治理影响生效的一致性。
  - 风险2：统一台账数据量大，查询性能问题。应对：按时间范围默认过滤（近30天），支持按需扩展查询范围。

### 创意3：授权规则驱动的自动授予（Big Bet）

- **描述**：对于管理员标记为"低风险"的权限，员工申请后不进入人工审批流，而是通过预设的角色授权规则自动完成授权。管理员在角色管理→授权规则中预设规则，例如"产品部全员自动关联产品查看角色"、"入职满3个月的正式员工自动关联基础数据查看角色"。员工申请匹配的权限时，系统检查是否命中已有授权规则，命中则自动执行RBAC_ROLE关联，状态直接流转至effective；未命中则进入正常审批流。

- **解决的痛点**：
  - 低风险权限也要走完整审批流，审批资源浪费
  - 新员工入职后权限配置慢（需管理员逐个处理）

- **预期效果**：
  - 低风险权限获取时间从小时级降至秒级
  - 审批人日常审批工作量降低60%
  - 新员工入职首日权限就绪率提升

- **实现路径**：
  1. 技术评估授权规则引擎是否支持"申请触发"模式（当前仅支持条件匹配自动授予/回收）。
  2. 如支持：管理员在授权规则中增加"允许员工自助申请触发"选项，命中规则时自动授予。
  3. 如不支持：在权限域内实现轻量规则匹配——申请提交时检查预设规则，命中则跳过审批直接执行RBAC_ROLE关联。
  4. 所有自动授予记录落入变更台账，审计记录标记为"系统自动授予（规则命中）"。

- **验证方式**：选2-3个只读类角色（如"报表查看角色"），开启自助申请自动授予，运行2周监控使用量和异常率。

- **风险**：
  - 风险1：授权规则引擎不支持申请触发模式。应对：在权限域内实现轻量规则匹配作为过渡方案。
  - 风险2：规则配置错误导致不当授权。应对：自动授予仅适用于管理员显式标记为"允许自助申请"的角色，默认关闭；所有自动授予记录可审计、可回收。

---

## 被放弃的创意（存档参考）

上一轮脑暴中以下创意因与现有架构矛盾而在本轮放弃：

- "权限风险分级管理体系"：放弃原因——产品已有四层判定链路和GOVERNANCE_MODE修饰因子，新增平行分级体系违反TR-003概念不增殖原则。分级语义可通过已有的治理状态模型和审批模式配置实现，无需新概念。
- "角色权限包一键申请"：放弃原因——RBAC_ROLE+角色管理→授权规则已实现权限包能力（角色本身就是权限包，授权规则实现自动关联），不需要重新发明。
- "临时权限租借"：放弃原因——治理状态模型（draft/pending/approved/rejected/effective/revoked）中无"过期自动回收"状态，引入临时权限需扩展状态模型，违反不增殖原则。如后续需要，应作为治理状态模型的扩展单独评估。
- "IM机器人申请闭环"：放弃原因——申请记录需满足审计契约9字段要求（含approval、outcome、snapshot），IM消息作为审批凭证无法支撑。
- "权限目录市场（App Store模式）"：放弃原因——开发投入大，且已有入口分区语义（5大分区+新增的员工自助分区）和查询路径已覆盖权限发现需求。
- "部门经理共享子权限"：放弃原因——与子管理员范围隔离（R-008）和治理模式互斥（R-009）冲突，引入新的授权来源违反来源模型约束。

---

## 与上一轮脑暴的关键差异

| 维度 | 上一轮（无业务规则） | 本轮（锚定业务规则） |
|------|-------------------|-------------------|
| 创意数量 | 22个 | 11个 |
| 新概念发明 | 6个（风险分级、权限包、临时租借、目录市场、家庭共享、IM机器人） | 0个（所有创意均在已有模型内扩展） |
| 与已有架构的冲突 | 至少4个创意存在冲突 | 0个冲突（每个创意标注了架构锚点） |
| 填补已知GAP | 未覆盖任何已标注GAP | C07直接填补"按变更查"的GAP |
| 来源模型一致性 | 未考虑来源枚举约束 | 所有授权结果落入ACL_DIRECT或RBAC_ROLE |
| 治理状态一致性 | 自由发挥审批流程 | 复用已有状态模型和审批模式 |
| 审计合规 | 未对齐审计契约 | 所有创意对齐9个最低审计字段 |
| 可落地性 | 约50%需要架构改造 | 约90%可在现有架构内实现 |

---

## 下一步行动

- [ ] **行动1：技术可行性评估** -- 与开发团队确认：(a) 前台无权限页面是否已暴露reason_code数据；(b) 审批中台是否支持"员工发起"作为新触发源；(c) 授权规则引擎是否支持"申请触发"模式。
- [ ] **行动2：Quick Wins快速落地** -- 技术评估完成后，优先实施C01（原因码引导申请）+ C03（表单语义对齐）+ C04（L0预校验），预计2-3周可完成MVP。
- [ ] **行动3：文案口径扩展** -- 为conflict_reason_code体系增加"申请前引导"解释时机，编写7个原因码的员工引导文案，与UX团队评审确认。
- [ ] **行动4：Big Bet方案对齐** -- 组织产品+技术+安全团队评审C06（对接审批模式）和C07（统一变更台账），确认技术方案和排期。
- [ ] **行动5：用户验证** -- 邀请5名员工和2名权限管理员进行原型测试，验证原因码引导的可理解性和申请表单的可用性。
- [ ] **行动6：体验翻译补充** -- 基于31_体验翻译需求.md的15条转译要求，为申请表单和申请引导中的技术术语编写业务翻译（如ACL_DIRECT→"个人授权"、scope_expression→"数据访问范围"）。
