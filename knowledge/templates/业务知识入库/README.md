# 业务知识入库模板

这里存放知识入库模板，只作为 AI 规划和编写 raw 的轻量骨架参考。

约束：

- templates 不属于正式 `knowledge/raw/**`
- templates 不进入 `knowledge/wiki/index.md`
- templates 不生成 summary
- 模板只提供结构，不预填具体业务语义

推荐用法：

1. 先判断资料应该并入现有领域还是新建领域
2. 读取相邻领域示例和这里的模板
3. 先输出入库计划，再落盘 raw
