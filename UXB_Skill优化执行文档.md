# UXB Skill 输出稳定性优化执行文档

> **致 Code Agent — 执行前必读**
>
> 1. 请先检查本文档是否存在信息滞后、冲突或错误。对照当前 `SKILL.md` 和 `references/` 目录的实际内容，判断本文档描述的"当前状态"是否与真实代码一致。
> 2. 如果优化任务中的部分内容已经在项目中完成，请逐项对比后判断：是"优化已局部存在"还是"需要整体替换"。对于已局部存在且正确的部分，保留并补齐缺失部分；对于已局部存在但与本文档冲突的部分，以本文档为准并修正。
> 3. 修改代码时，如果涉及删除代码逻辑或信息，必须检查所删除内容的依赖方：`gate`、`validate`、`coverage`、`archive` 是否需要同步变更；`specs/`、`templates/` 是否需要同步更新。确认无影响后方可删除。
> 4. 本文档的所有修改均为增量式或替换式，不涉及文件合并、文件删除或目录结构调整。

---

## 一、背景

UXB skill 由一个主文件 `SKILL.md`（278 行）和 13 个 references 文件（约 2026 行）组成，覆盖知识问答、诊断咨询、正式蓝图任务、知识维护四种工作状态。

当前存在一个结构性问题：AI 在不同会话中执行 UXB skill 时，输出的文案风格、产物结构和颗粒度不稳定。同一个需求在不同会话中可能得到不同格式的分析结果、不同深度的判断单、不同措辞的用户侧回复。

**根因定位**：`SKILL.md` 第 84-100 行的"按需分流读取"策略允许 AI"最多追加读取 1-3 份最相关 guide"。这意味着 references 中的行为约束（输出格式、表达规则、流程硬规则）是概率性加载的——AI 可能只读取了部分 guide，导致未被加载的约束在本次会话中不生效。

**之前已尝试但失败的方案**：将 9+ 个 references 文件合并为 3 个大文件。该方案导致信息丢失、上下文混乱、AI 在大文件中压缩和简化内容，最终回退到原始结构。

## 二、目标

在不改变现有文件结构（不合并文件、不删除文件、不调整目录）的前提下，解决三个问题：

1. **输出格式稳定**：分析结果、判断单、用户侧回复的骨架结构和措辞规范在每次会话中一致。
2. **知识消费可靠**：知识读取遵循"summary 定位 → 必须读 raw 确认"的硬规则，不因 AI 选择性跳过而遗漏关键事实。
3. **知识关联清晰**：references 之间的概念交叉点有显式交叉引用，AI 不会把同一概念在不同文件中的表述当作孤立信息。

## 三、策略

采用"骨架内联 + 硬规则内联 + 交叉引用"三层策略：

- **骨架内联**：将 AI 不可压缩的输出格式模板和流程检查清单直接写入 `SKILL.md`。因为 `SKILL.md` 是每次会话必读的文件，内联到这里的约束 100% 生效。
- **硬规则内联**：将知识读取的强制规则写入 `SKILL.md`，同时修正 `knowledge_usage_guide.md` 中与新规则冲突的旧表述。
- **交叉引用**：在 references 文件中的概念重叠点添加指向性注释，让 AI 知道同一概念在其他文件中有不同上下文的应用，不做内容删除。

## 四、模块责任边界

| 模块 | 职责 | 本次是否修改 |
|---|---|---|
| `SKILL.md` | 角色定义、状态分流、阅读顺序、行为硬规则、骨架模板 | 是（任务 1 + 任务 2） |
| `references/user_response_guide.md` | 用户侧表达约束完整定义 | 否（仅被 SKILL.md 引用） |
| `references/knowledge_usage_guide.md` | 知识读取策略完整定义 | 是（任务 2：修正旧表述） |
| `references/consulting_guide.md` | 咨询态行为完整定义 | 是（任务 3：加交叉引用） |
| `references/demand_type_judgment_guide.md` | 需求类型判断完整定义 | 是（任务 3：加交叉引用） |
| `references/execution_guide.md` | 正式执行流程完整定义 | 是（任务 3：加交叉引用） |
| `references/task_submission_guide.md` | 任务摘要完整定义 | 是（任务 3：加交叉引用） |
| `references/knowledge_candidate_guide.md` | 知识候选完整定义 | 是（任务 3：加交叉引用） |
| `references/uxb_route_decision_authoring_guide.md` | 判断单生成完整定义 | 是（任务 3：加交叉引用） |
| `references/asset_map.md` | 资产地图 | 否 |
| `references/complexity/00_core_complexity_judgment.md` | 复杂度判断核心 | 是（任务 3：加交叉引用） |
| `references/complexity/01_domain_router.md` | 领域路由 | 否 |
| `references/complexity/02_uncertain_judgment_questioning.md` | 不确定判断提问 | 否 |
| `references/complexity/03_knowledge_candidate_reminder.md` | 知识候选提醒 | 是（任务 3：加交叉引用） |
| `specs/` | 正式规则契约 | 否 |
| `templates/` | 产物模板 | 否 |
| `packages/` | 执行中枢 | 否 |

---

## 五、任务 1：SKILL.md 骨架模板与流程硬规则内联

### 5.1 问题描述

当前 `SKILL.md` 对分析阶段的输出格式只有方向性描述（如"先给判断、影响、建议、需要确认"），没有固定的骨架模板。AI 在不同会话中会自行组织输出结构，导致格式漂移。同时，references 中的关键行为约束（如用户侧表达规则）因为"按需分流读取"策略而可能被跳过。

### 5.2 修改范围

仅修改 `SKILL.md`，在现有内容基础上增加以下段落。不删除现有内容，不修改 references 文件。

### 5.3 具体修改

#### 5.3.1 在"问题定性（需求文档场景）"段落（约第 54-68 行区域）之后，插入输出骨架模板

在 `### 现象定性（体验诊断场景）` 段落结束（约第 82 行区域）之后、`## 阅读顺序`（第 84 行）之前，插入以下新段落：

```markdown
## 分析阶段输出骨架（硬模板）

分析阶段的所有输出必须严格按以下骨架组织，不得省略段落、合并段落或改变段落顺序。

### 需求文档场景骨架

当输入为需求文档、PRD、功能说明时，输出必须包含以下段落，按此顺序：

```
## 我先判断这件事的性质

这次更像：[问题 / 方案 / 半成品方案]

我看到的关键问题是：
- ...

可能存在的偷换是：
- ...
（如无偷换，写"暂未发现偷换。"）

如果顺着当前方向做，最容易漏掉的前提是：
- ...
（如无遗漏风险，写"当前方向的前提基本成立。"）

我建议先讨论：
- ...
（如无需要讨论的点，写"无需额外讨论，可以继续推进。"）

## 复杂度判断

业务改动：
- [逐条列出六问中命中的项；未命中的统一写"未涉及"]

体验压力：
- [逐条列出五问中命中的项；未命中的统一写"压力较低"]

复杂度结论：
- [低 / 中 / 高 / 信息不足]，原因是：[一句话]

后续重点：
- [1-3 条]

## 分析结果

### 判断
[一段话，先说结论]

### 影响
- [逐条列出]

### 建议
- [逐条列出]

### 需要确认
- [最多 3 个关键问题；如无阻断型问题，写"当前无阻断型问题。"]
```

### 体验诊断场景骨架

当输入为页面截图、流程描述、体验问题时，输出必须包含以下段落，按此顺序：

```
## 我先判断这个现象的性质

问题位置：
- [用户指出的位置 / 我观察到的位置] 是否真正的问题位置：[是 / 可能不是，真正位置在 ...]

症状范围：
- [局部 / 系统性]，判断依据：[一句话]

修复影响：
- [如果只修这个点，上下游是否会产生新断点：会 / 不会 / 需要进一步确认]

## 复杂度判断

（格式同需求文档场景）

## 分析结果

### 判断
[一段话]

### 影响
- [逐条]

### 建议
- [逐条]

### 需要确认
- [最多 3 个]
```

### 骨架规则

- 以上骨架是硬约束，不是推荐格式。每次输出必须包含所有段落标题，即使某段落内容为"暂无"。
- 段落标题的措辞不可更改。例如"我先判断这件事的性质"不可改为"需求定性"或"问题分析"。
- 骨架中 `[...]` 标注的位置是内容填充位，按实际分析结果替换。
- 如果分析过程中发现阻断型问题，在"需要确认"段落之后追加"## 阻断提示"段落，说明阻断原因和建议处理方式。
```

#### 5.3.2 在"分析结果出现多条路径时"的固定编号格式

在 5.3.1 插入内容的"骨架规则"之后，紧接着增加以下子段落：

```markdown
### 多路径分析的固定格式

当分析阶段出现多条可能路径（如多种需求定性结论、多种复杂度判断、多种建议方向）时，必须使用以下固定编号格式呈现，不得用模糊描述代替：

```
存在以下 [N] 条可能路径：

**路径 1：[路径名称]**
- 判断：[一句话结论]
- 适用条件：[什么时候走这条路]
- 影响：[主要影响]
- 建议：[处理方向]

**路径 2：[路径名称]**
- 判断：[一句话结论]
- 适用条件：[什么时候走这条路]
- 影响：[主要影响]
- 建议：[处理方向]

（如有更多路径，继续编号）

我的初步倾向是路径 [X]，原因是：[一句话]。
需要你确认走哪条路径。
```

规则：
- 每条路径必须独立编号（路径 1、路径 2、路径 3...），不可合并描述。
- 每条路径必须包含判断、适用条件、影响、建议四个子项。
- 必须在最后给出 AI 的倾向性建议和原因。
- 必须以"需要你确认走哪条路径"收尾，等待用户决策。
```

#### 5.3.3 在"阅读顺序"段落中增加必读约束

在 `## 阅读顺序` 段落（第 84-100 行区域）的末尾，即"不要因为目录里文档很多，就顺序通读所有 guide。UXB 的正常工作方式是'先分流，再按需深读'。"这句话之后，追加以下内容：

```markdown
### 必读约束（不可跳过）

无论在哪个工作状态，以下约束始终生效，不需要额外读取对应 guide 即可执行：

1. **输出骨架**：分析阶段输出必须严格按本文件的"分析阶段输出骨架"段落执行，不可自由组织格式。
2. **多路径格式**：出现多条分析路径时，必须使用本文件的"多路径分析的固定格式"段落，不可模糊合并。
3. **用户侧表达底线**：
   - 不向用户暴露 route、pipeline、gate、validate、coverage、fast/standard/full 等内部词
   - 先给结论，不铺垫
   - 用户只问一个点时，只回答这个点
   - 完整规则见 `references/user_response_guide.md`，进入分析或咨询前必须读取该文件
4. **知识读取硬规则**：见本文件的"知识读取硬规则"段落。
5. **确认门槛**：用户未明确确认前，不创建项目目录、不写正式产物、不运行主链路。
```

---

## 六、任务 2：知识读取硬规则

### 6.1 问题描述

当前 `knowledge_usage_guide.md` 第 92-106 行的"Summary First, Raw Later"段落表述为"只有下面这些情况再读更细的 raw：summary 不够、需要证据、用户要求可追溯来源"。这个表述在实践中被 AI 理解为"summary 通常够用，raw 是例外情况"，导致 AI 在 summary 命中后直接做出判断，跳过了对 raw 中关键细节（规则边界、状态定义、对象关系）的确认。

### 6.2 修改范围

1. `SKILL.md`：增加"知识读取硬规则"段落。
2. `knowledge_usage_guide.md`：修正"Summary First, Raw Later"段落的表述，使其与新硬规则一致。

### 6.3 具体修改

#### 6.3.1 在 SKILL.md 中增加"知识读取硬规则"段落

在 `## 知识使用` 段落（约第 244-250 行区域）之前、`## 正式执行` 段落（约第 236-242 行区域）之后，插入以下新段落：

```markdown
## 知识读取硬规则

以下规则在所有工作状态下强制生效，优先级高于 references 中的知识读取策略：

### 规则 K-1：Summary 命中后必须升级 Raw

当通过 summary、route card、README 或 index 命中某个知识领域或知识文件时，**必须继续读取对应的 raw 文件**后才能基于该知识做出判断。

执行步骤：
1. 通过 summary / route card / README 定位到候选知识文件。
2. 读取对应的 raw 文件（如 `knowledge/raw/业务/<领域>/<具体文件>.md`）。
3. 基于 raw 内容确认或修正 summary 给出的信息。
4. 才可以在分析或判断中使用该知识。

禁止行为：
- 在只读了 summary 而未读 raw 的情况下，直接引用该知识作为判断依据。
- 将 summary 的描述当作最终事实，不做 raw 层面的交叉验证。

例外情况（仅在以下条件全部满足时允许不读 raw）：
- 当前问题纯粹是概念性或方向性的（如"这个模块大概做什么"）。
- summary 已经完整覆盖了当前问题所需的全部信息。
- 不涉及规则边界、状态定义、对象关系、字段含义、流程细节中的任何一项。

### 规则 K-2：知识读取范围与判断深度匹配

- L1（领域定位）：可只读 index + summary + README，但判断结论必须标注"基于 summary，未经 raw 确认"。
- L2（路由判断）：summary 为主，但涉及具体规则、状态、流程的判断点必须有 raw 支撑。
- L3（正式产物）：所有写入判断单或正式产物的知识必须有 raw 来源。

### 规则 K-3：知识读取链路可追溯

每次在分析或判断中引用知识时，必须在内部记录读取链路：

```
[summary/README 入口] → [命中的 raw 文件] → [支撑的具体判断点]
```

如果某条判断无法追溯到 raw，必须标注为"基于推断，未有知识文件直接支撑"。
```

#### 6.3.2 修改 knowledge_usage_guide.md 的 "Summary First, Raw Later" 段落

找到 `knowledge_usage_guide.md` 第 92-106 行区域的以下内容：

```markdown
## Summary First, Raw Later

优先读：

- summary 文件
- route card
- 模块描述
- 概览入口

只有下面这些情况再读更细的 raw：

- summary 不够
- 需要证据
- 用户要求可追溯来源
```

将其替换为：

```markdown
## Summary First, Raw Required

优先用 summary 定位：

- summary 文件
- route card
- 模块描述
- 概览入口

**Summary 命中后，必须继续读取对应的 raw 文件确认，才能将该知识用于判断。**

Summary 的作用是路由定位，不是事实确认。Raw 的作用才是提供事实依据。

仅在以下条件全部满足时，允许不读 raw：

- 当前问题纯粹是概念性或方向性的
- summary 已完整覆盖当前问题所需全部信息
- 不涉及规则边界、状态定义、对象关系、字段含义、流程细节
```

### 6.4 依赖检查

本任务修改了 `knowledge_usage_guide.md` 中的知识读取策略表述。请检查以下文件是否引用了"Summary First, Raw Later"的说法并需要同步更新：

- `uxb_route_decision_authoring_guide.md`（第 12 行提到"知识读取入口、summary/raw 分层...以 knowledge_usage_guide.md 为准"）——此处引用的是文件整体，不需要修改。
- `SKILL.md`（第 246-248 行提到"知识读取范围控制、summary 优先..."）——需要将"summary 优先"改为"summary 定位 + raw 确认"。

找到 `SKILL.md` 第 246-248 行区域的以下内容：

```markdown
## 知识使用

知识读取范围控制、summary 优先和用户纠错处理，统一见：
```

将其替换为：

```markdown
## 知识使用

知识读取范围控制、summary 定位 + raw 确认和用户纠错处理，统一见：
```

---

## 七、任务 3：References 交叉引用

### 7.1 问题描述

13 个 references 文件中有 5 组概念重叠区域。当前这些重叠点没有交叉引用，AI 可能将不同文件中的同一概念当作孤立信息处理，导致理解碎片化或判断不一致。

### 7.2 设计原则

- **只增加交叉引用注释，不删除任何现有内容。**
- 交叉引用的目的是让 AI 知道"这个概念在其他文件中有不同上下文的应用"，不是合并信息。
- 交叉引用使用统一格式。

### 7.3 交叉引用格式

在每个重叠概念所在位置，追加以下格式的注释块：

```markdown
> **交叉引用**：同一概念在以下文件中有不同上下文的应用：
> - `[文件路径]`：[该文件中此概念的角色说明，一句话]
> - `[文件路径]`：[该文件中此概念的角色说明，一句话]
```

### 7.4 具体修改

#### 7.4.1 重叠区域 A：复杂度判断

涉及文件和位置：

**文件 1：`references/demand_type_judgment_guide.md`**

在"## 判断顺序"段落（第 12-28 行区域）的末尾（第 28 行"4. 最后判断是低、中、高复杂度，还是信息不足。"之后），插入：

```markdown

> **交叉引用**：复杂度判断在以下文件中有不同上下文的应用：
> - `references/complexity/00_core_complexity_judgment.md`：复杂度判断的核心公式和分级标准（正式定义源）
> - `references/uxb_route_decision_authoring_guide.md`：复杂度判断结果如何映射到判断单的 `required_outputs` 字段
> - `SKILL.md`"正式任务启动前的需求类型判断"段落：复杂度判断的入口路由
```

**文件 2：`references/complexity/00_core_complexity_judgment.md`**

在"## 核心公式"段落（第 3-9 行区域）的末尾（第 9 行"不要把领域词、风险词或功能名直接等同于复杂度结论。"之后），插入：

```markdown

> **交叉引用**：复杂度判断在以下文件中有不同上下文的应用：
> - `references/demand_type_judgment_guide.md`：需求类型判断中如何触发复杂度判断
> - `references/uxb_route_decision_authoring_guide.md`：复杂度结论如何影响判断单的输出深度选择
> - `SKILL.md`"正式任务启动前的需求类型判断"段落：复杂度判断的入口和前置条件
```

**文件 3：`references/uxb_route_decision_authoring_guide.md`**

在"## 复杂度判断"段落（第 96-114 行区域）的末尾（第 114 行"不要只靠关键词或领域名自动下结论。"之后），插入：

```markdown

> **交叉引用**：复杂度判断在以下文件中有不同上下文的应用：
> - `references/complexity/00_core_complexity_judgment.md`：复杂度公式和分级标准的正式定义
> - `references/demand_type_judgment_guide.md`：从需求类型角度触发复杂度判断的完整维度
> - `SKILL.md`"正式任务启动前的需求类型判断"段落：SKILL 层面的入口路由
```

#### 7.4.2 重叠区域 B：知识读取策略

涉及文件和位置：

**文件 1：`references/knowledge_usage_guide.md`**

在"## 知识消费深度"段落（第 107-113 行区域）的末尾（第 113 行关于 L3 的描述之后），插入：

```markdown

> **交叉引用**：知识读取策略在以下文件中有不同上下文的应用：
> - `references/uxb_route_decision_authoring_guide.md`"知识选择规则"段落：判断单中 `knowledge_selection` 字段如何体现读取策略
> - `SKILL.md`"知识读取硬规则"段落：summary 定位 + raw 确认的强制规则（优先级高于本文件）
```

**文件 2：`references/uxb_route_decision_authoring_guide.md`**

在"## 知识选择规则"段落（第 135-178 行区域）的末尾（第 178 行关于 guideline 选择条件的内容之后），插入：

```markdown

> **交叉引用**：知识选择规则在以下文件中有不同上下文的应用：
> - `references/knowledge_usage_guide.md`：知识读取策略的完整定义（summary/raw 分层、消费深度、文件边界）
> - `SKILL.md`"知识读取硬规则"段落：summary 命中后必须读 raw 的强制规则
```

#### 7.4.3 重叠区域 C：用户侧表达约束

涉及文件和位置：

**文件 1：`references/consulting_guide.md`**

在"## 默认回答风格"段落（第 39-67 行区域）的末尾（第 67 行"如果用户只问一个具体点，可以更短"的代码块之后），插入：

```markdown

> **交叉引用**：用户侧表达约束在以下文件中有不同上下文的应用：
> - `references/user_response_guide.md`：用户侧表达的完整规则定义（正式定义源），包括固定引导语、回复结构、禁用术语
> - `references/task_submission_guide.md`"用户可见表达"段落：任务摘要场景下的表达约束
```

**文件 2：`references/task_submission_guide.md`**

在"## 用户可见表达"段落（第 53-64 行区域）的末尾（第 64 行"除非用户明确追问内部实现。"之后），插入：

```markdown

> **交叉引用**：用户侧表达约束在以下文件中有不同上下文的应用：
> - `references/user_response_guide.md`：用户侧表达的完整规则定义（正式定义源）
> - `references/consulting_guide.md`"默认回答风格"段落：咨询态的回复结构
```

#### 7.4.4 重叠区域 D：知识候选触发

涉及文件和位置：

**文件 1：`references/consulting_guide.md`**

在"## 与知识候选区的关系"段落（第 99-116 行区域）的末尾（第 116 行"不要问用户'是否切换到知识维护态'。"之后），插入：

```markdown

> **交叉引用**：知识候选触发规则在以下文件中有不同上下文的应用：
> - `references/knowledge_candidate_guide.md`：知识候选的完整定义（目录结构、文件命名、模板、内容范围、创建条件）
> - `references/complexity/03_knowledge_candidate_reminder.md`：复杂度判断过程中产生可复用结论时的轻量提醒机制
```

**文件 2：`references/complexity/03_knowledge_candidate_reminder.md`**

在该文件末尾追加：

```markdown

> **交叉引用**：知识候选提醒在以下文件中有不同上下文的应用：
> - `references/knowledge_candidate_guide.md`：知识候选的完整定义和操作流程（正式定义源）
> - `references/consulting_guide.md`"与知识候选区的关系"段落：咨询态下的候选触发条件和话术
```

#### 7.4.5 重叠区域 E：任务摘要与确认流程

涉及文件和位置：

**文件 1：`references/task_submission_guide.md`**

在"## 确认门槛"段落（第 66-81 行区域）的末尾（第 81 行"不运行 UXB 主链路"之后），插入：

```markdown

> **交叉引用**：任务确认流程在以下文件中有不同上下文的应用：
> - `references/execution_guide.md`"执行前"段落：确认后进入正式执行的具体步骤
> - `references/user_response_guide.md`"任务摘要与正式启动"段落：确认后的用户侧表达约束
```

**文件 2：`references/execution_guide.md`**

在"## 执行前"段落（第 5-19 行区域）的末尾（第 19 行"当前目标确实是正式产物执行，而不是纯咨询或知识候选整理"之后），插入：

```markdown

> **交叉引用**：执行前确认流程在以下文件中有不同上下文的应用：
> - `references/task_submission_guide.md`"确认门槛"段落：哪些用户表达构成有效确认
> - `references/user_response_guide.md`"任务摘要与正式启动"段落：确认环节的用户侧表达
```

### 7.5 依赖检查

本任务仅在 references 文件中追加交叉引用注释块，不修改、不删除任何现有内容。因此：

- 不影响 `specs/`、`templates/`、`packages/` 的任何逻辑。
- 不影响 `gate`、`validate`、`coverage`、`archive` 的判断条件。
- 不改变任何文件的现有语义，只增加指向性信息。

---

## 八、注意事项

### 8.1 禁止操作

1. **不合并文件**：不得将多个 references 文件合并为少数大文件。每个文件有独立职责，合并会破坏按需加载的粒度。
2. **不删除内容**：不得因为"看起来重复"删除 references 中的任何段落。5 组重叠区域中的每个"重复"实际上是同一概念在不同上下文中的应用，删除会导致源模块信息缺失。
3. **不改变目录结构**：不得移动文件位置或修改目录层级。
4. **不简化表述**：不得为了缩减 SKILL.md 长度而压缩内联的骨架模板。骨架模板的措辞和结构是硬约束，压缩会导致约束失效。
5. **不修改 `specs/`、`templates/`、`packages/`**：本次优化不涉及正式规则、产物模板和执行中枢。

### 8.2 执行顺序

建议按以下顺序执行：

1. 先执行任务 2（知识读取硬规则），因为任务 1 的骨架模板中引用了"知识读取硬规则"段落。
2. 再执行任务 1（骨架模板），确认内联内容与任务 2 的硬规则段落一致。
3. 最后执行任务 3（交叉引用），因为交叉引用中部分注释引用了任务 1 和任务 2 新增的段落名。

### 8.3 验证方式

每完成一个任务后，执行以下验证：

1. **结构验证**：检查修改后的文件是否仍可被正常解析（Markdown 格式完整、无断裂标题、无未闭合代码块）。
2. **引用验证**：检查所有新增的交叉引用中提到的文件路径是否正确存在。
3. **语义验证**：通读修改后的文件，确认新增内容与现有内容不存在逻辑矛盾。
4. **回归验证**：确认修改未影响以下场景的正常执行流程：
   - 知识问答态：用户提问 → AI 读知识 → 回答
   - 诊断咨询态：用户描述问题 → AI 判断 → 建议
   - 正式蓝图任务态：用户给需求 → 分析 → 摘要 → 确认 → 判断单 → 执行
   - 知识维护态：用户纠正知识 → 候选 → 确认 → 入库

---

## 九、验收标准

### 9.1 任务 1 验收标准

| 编号 | 验收项 | 标准 | 验证方式 |
|---|---|---|---|
| T1-01 | 分析阶段输出骨架已内联到 SKILL.md | SKILL.md 中存在"分析阶段输出骨架（硬模板）"段落，包含需求文档场景骨架和体验诊断场景骨架 | 文件内容检查 |
| T1-02 | 骨架模板包含所有必需段落标题 | 需求文档骨架包含：性质判断、复杂度判断、分析结果（判断/影响/建议/需要确认）；体验诊断骨架包含：现象定性、复杂度判断、分析结果 | 文件内容检查 |
| T1-03 | 多路径分析固定格式已内联 | SKILL.md 中存在"多路径分析的固定格式"段落，包含编号格式模板和规则 | 文件内容检查 |
| T1-04 | 多路径格式包含四个子项 | 每条路径必须包含判断、适用条件、影响、建议 | 文件内容检查 |
| T1-05 | 必读约束已追加到阅读顺序段落 | "阅读顺序"段落末尾存在"必读约束（不可跳过）"子段落 | 文件内容检查 |
| T1-06 | 必读约束引用了正确的段落名 | 必读约束中引用的"分析阶段输出骨架""多路径分析的固定格式""知识读取硬规则"段落名在 SKILL.md 中实际存在 | 交叉引用检查 |
| T1-07 | 未删除现有内容 | SKILL.md 中原有的所有段落（核心角色、总原则、上位推理、阅读顺序、工作状态等）均完整保留 | Diff 对比 |

### 9.2 任务 2 验收标准

| 编号 | 验收项 | 标准 | 验证方式 |
|---|---|---|---|
| T2-01 | 知识读取硬规则已内联到 SKILL.md | SKILL.md 中存在"知识读取硬规则"段落，包含规则 K-1、K-2、K-3 | 文件内容检查 |
| T2-02 | 规则 K-1 包含强制升级 raw 要求 | K-1 明确要求 summary 命中后必须读 raw，并列出禁止行为和例外条件 | 文件内容检查 |
| T2-03 | 规则 K-2 包含分层深度要求 | K-2 对 L1/L2/L3 三个深度层级分别给出 raw 要求 | 文件内容检查 |
| T2-04 | 规则 K-3 包含链路追溯要求 | K-3 要求记录读取链路，无法追溯时标注"基于推断" | 文件内容检查 |
| T2-05 | knowledge_usage_guide.md 旧表述已修正 | "Summary First, Raw Later"已改为"Summary First, Raw Required"，内容包含强制读 raw 要求 | 文件内容检查 |
| T2-06 | SKILL.md"知识使用"段落描述已同步 | 第 246-248 行区域的"summary 优先"已改为"summary 定位 + raw 确认" | 文件内容检查 |
| T2-07 | 新规则与例外条件不矛盾 | SKILL.md 中的硬规则例外条件与 knowledge_usage_guide.md 中的例外条件一致 | 语义对比 |

### 9.3 任务 3 验收标准

| 编号 | 验收项 | 标准 | 验证方式 |
|---|---|---|---|
| T3-01 | 重叠区域 A（复杂度判断）已添加交叉引用 | demand_type_judgment_guide.md、00_core_complexity_judgment.md、uxb_route_decision_authoring_guide.md 三处均有交叉引用注释块 | 文件内容检查 |
| T3-02 | 重叠区域 B（知识读取策略）已添加交叉引用 | knowledge_usage_guide.md、uxb_route_decision_authoring_guide.md 两处均有交叉引用注释块 | 文件内容检查 |
| T3-03 | 重叠区域 C（用户侧表达）已添加交叉引用 | consulting_guide.md、task_submission_guide.md 两处均有交叉引用注释块 | 文件内容检查 |
| T3-04 | 重叠区域 D（知识候选触发）已添加交叉引用 | consulting_guide.md、03_knowledge_candidate_reminder.md 两处均有交叉引用注释块 | 文件内容检查 |
| T3-05 | 重叠区域 E（任务确认流程）已添加交叉引用 | task_submission_guide.md、execution_guide.md 两处均有交叉引用注释块 | 文件内容检查 |
| T3-06 | 交叉引用格式统一 | 所有交叉引用使用 `> **交叉引用**：` 开头的引用块格式 | 格式检查 |
| T3-07 | 交叉引用中文件路径正确 | 所有引用的文件路径在当前仓库结构中实际存在 | 路径验证 |
| T3-08 | 未删除任何现有内容 | 所有 references 文件的现有段落和内容完整保留 | Diff 对比 |
| T3-09 | 未合并任何文件 | references 目录下仍保持 13 个文件 + complexity 子目录的结构 | 目录结构检查 |

### 9.4 整体验收

| 编号 | 验收项 | 标准 | 验证方式 |
|---|---|---|---|
| A-01 | 四种工作状态未被破坏 | 知识问答态、诊断咨询态、正式蓝图任务态、知识维护态的定义和分流逻辑完整 | 语义验证 |
| A-02 | 阅读顺序策略仍然有效 | "先分流，再按需深读"的整体策略未被取消，只是追加了必读约束 | 语义验证 |
| A-03 | 无文件被删除 | references 目录下的文件数量和名称不变 | Diff 对比 |
| A-04 | 无文件被合并 | 不存在将多个文件内容合并到单一文件的操作 | Diff 对比 |
| A-05 | 无目录结构变更 | `.claude/skills/uxb/` 下的目录层级不变 | 目录结构检查 |
| A-06 | specs/templates/packages 未被修改 | 这三个目录下的文件无任何变更 | Diff 对比 |
