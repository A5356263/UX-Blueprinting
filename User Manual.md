# 使用手册

## 这是什么

这是一套围绕产品需求分析、体验方案生成、页面规格整理和知识维护构建的 Skill 套件。

它不是一个固定死流程的大系统，而是一套可组合的能力集：

- 正式主链负责把需求推进到体验方案与页面规格
- 增强 Skill 负责在特定场景下补充问题诊断、异常态、视觉方向、知识维护等能力

## 安装与激活

1. 解压项目后，用你的 Code Agent 工具打开项目根目录。
2. 项目中默认提供 `.claude` 目录；如果你的 IDE 不是 Claude/Codex 约定目录，就把目录名改成对应 IDE 识别的 Skill 目录名。
3. 按你所用 IDE 的 Skill 唤起方式直接调用即可。

## 目录说明

- `.claude/skills/`：Skill 定义目录
- `shared-workflow/`：Skill 编排与可视化说明
- `spark-output/`：正式产物输出目录

建议大家自行建立一个 `input/` 目录，用来统一存放：

- 需要入库的知识材料
- PRD、需求文档、业务文档
- 截图、页面说明、补充材料

这不是当前套件强依赖的内置目录，但建议这么做，方便后续使用和管理。，不要把信息直接丢到skill内部文件

## 推荐起步方式

默认建议从以下两种入口之一开始：

- 有 PRD、需求文档、业务材料：先用 `uxb`
- 没有 PRD，只有想法、问题描述、模糊需求：先用 `problem-framing`

当前推荐主链可以理解为：

```text
uxb / problem-framing
        ↓
     stories
        ↓
journey-analysis
        ↓
experience-blueprint
        ↓
page-spec
```

其中：

- `stories` 和 `journey-analysis` 是中间增强层，不是每次都必须逐个手动调用，但它们的职责是清晰存在的
- `experience-blueprint` 是正式体验方案层
- `page-spec` 是页面规格层

## Skill 起点指南

### `uxb`

适用：

- 已有 PRD、需求文档、业务材料
- 需要正式需求定案

作用：

- 定清业务问题、角色、目标、范围、规则边界、风险和正式承接要求

不负责：

- 用户故事
- 旅程图
- 页面方案
- 页面规格

### `problem-framing`

适用：

- 没有 PRD
- 只有模糊想法、口头需求、问题描述

作用：

- 获取必要信息，收敛问题定义、角色、目标、边界、推荐方向和承接契约

不负责：

- 正式需求定案
- 用户故事
- 旅程图
- 页面方案

### `stories`

适用：

- 需要把正式输入拆成可设计、可验证的任务单元

作用：

- 输出任务单元、完成标准、设计触点、关键假设

不负责：

- 重判需求方向
- 修改业务边界
- 写旅程阶段
- 写页面规格

### `journey-analysis`

适用：

- 需要按阶段看用户如何完成任务、哪里会卡住、哪里会流失

作用：

- 输出阶段、动作、触点、痛点、流失风险、机会点

不负责：

- 需求定案
- 页面交互设计
- 页面规格

### `experience-blueprint`

适用：

- 需要把需求和任务信息展开成正式体验方案

作用：

- 生成体验蓝图、交互方案、页面与状态设计基础

### `page-spec`

适用：

- 需要把体验方案整理成页面生成可消费的页面规格

作用：

- 提取页面结构、交互流程、状态表现、异常恢复、文案池、模板变量

不负责：

- 视觉风格方案
- 前端代码生成

### `edge`

适用：

- 主流程完成后，需要补齐异常态、空态和权限边界

作用：

- 输出异常态补丁，供 `page-spec` 等下游吸收

说明：

- 这是可选补强 Skill，不是每次都必须进入主链

### `board`

适用：

- 需要收敛视觉方向

作用：

- 基于体验方案产出视觉风格方向

### `interface-audit`

适用：

- 已经有页面、截图、DOM、A11Y、runtime 等现有界面证据
- 想先诊断当前界面问题

作用：

- 输出现状界面诊断结果，供 `uxb`、`problem-framing`、`product-analysis` 等继续消费

说明：

- 这是现状诊断 Skill，不是正式主链节点

### `product-analysis`

适用：

- 需求方向不清
- 现有方案可能不成立
- 需要先重构问题或发散方向

作用：

- 在进入正式定案前，做问题重构与方向分析

### `journey-metrics`

适用：

- 需要定义旅程关键节点的埋点和度量口径

作用：

- 输出旅程埋点与度量需求

## 怎么选 Skill

- 你关心“正式需求是否定清”时，用 `uxb`
- 你关心“问题本身还没说清”时，用 `problem-framing`
- 你关心“要拆成哪些任务、做到什么算完成”时，用 `stories`
- 你关心“用户一路怎么走、哪里会卡住”时，用 `journey-analysis`
- 你关心“正式体验方案怎么展开”时，用 `experience-blueprint`
- 你关心“页面生成要吃什么规格”时，用 `page-spec`
- 你关心“异常和边界状态补齐”时，用 `edge`
- 你关心“现有界面哪里有问题”时，用 `interface-audit`
- 你关心“视觉方向怎么收敛”时，用 `board`
- 你关心“埋点和度量怎么定义”时，用 `journey-metrics`

## 输出怎么看

正式产物默认写入 `spark-output/`。

常见结果包括：

- `spark-output/uxb_output.md`
- `spark-output/experience_blueprint.md`
- `spark-output/page_spec.md`
- `spark-output/context/*.json`

如果宿主支持，部分 Skill 也会刷新 `shared-workflow` 对应的进度预览。

## 知识库怎么维护

知识维护统一通过 `knowledge-wiki` 处理。

你不需要再按“组”的概念理解知识库，重点只看两件事：

1. 要新增、修改、删除什么知识
2. 然后唤起 `knowledge-wiki` 去处理

### 新增知识

建议先把原始资料整理成易读文本，优先用 Markdown。

可先把资料放到项目内任意明确位置，然后唤起 `knowledge-wiki`，告诉它：

- 哪个文件需要入库
- 属于什么业务主题
- 需要新增还是覆盖更新

### 修改知识

直接唤起 `knowledge-wiki`，明确说明：

- 要修改哪份知识
- 修改原因
- 替换后的材料路径或新的正文内容

### 删除知识

如果确认某份知识已经过期或错误，直接唤起 `knowledge-wiki`，明确说明：

- 要删除哪份知识
- 为什么删除
- 是否有替代知识需要补入

### 删除规则

尽量**不要**随意删除知识库中的大文件或顶层公共文件，尤其是这几类：

- 业务
- 设计准则
- 设计模式

原因：

- 这些通常是父级承载文件
- 直接删整份大文件，容易把整个知识路由一起破坏

推荐做法：

- 可以删除这些大文件下面的子文件、子文档
- 可以替换子文件内容
- 可以新增新的子文件
- 如果确实要删父级大文件，先确认已有等价替代结构，再让 `knowledge-wiki` 处理

一句话规则：

**优先删子文件，不轻易删父级大文件；知识的增删改统一通过 `knowledge-wiki` 执行。**

## 使用建议

- 第一次使用时，直接从 `uxb` 或 `problem-framing` 开始最稳
- 如果当前项目知识还不完整，也可以先靠对话推进，再逐步补知识
- 如果你已经有现有页面而不是需求文档，先用 `interface-audit`
- 如果你只想做单点增强，不必强行走完整主链

## 一句话总结

这套 Skill 的核心不是“每次都把所有 Skill 跑一遍”，而是：

**根据你当前手上的材料和问题类型，选择正确的起点 Skill，让主链负责正式推进，让增强 Skill 负责按需补强。**
