# Skill 套件使用手册 V0.2

## 1. 选择合适的路线

根据你手上的材料选择起点：

### 有正式 PRD 或明确需求
**执行路线**
prd-review → experience-blueprint → page-spec

- `prd-review`：审核需求文档是否闭环，闭环后才输出整理后的需求文档
  * `业务事实补充`：PRD 没写，但正式资料可以直接证明的业务事实。通常不需要产品回复。
  * `待产品确认`：缺少会影响角色、规则、范围或业务结果的决定，需要产品回答；未确认前不会生成正式需求基线。
  * `待体验定案`：业务规则已经明确，只缺用户如何发现、理解、操作或获得反馈的体验判断，不要求产品回答。
  * `你还需要补充什么吗？`：用于补充 PRD 中没有写清的已知信息。可以补充业务流程、使用场景、角色、规则、状态、异常或现有系统限制，Skill 会将内容归类到相应的审核结论中。
- `experience-blueprint`：根据需求文档输出完整体验交互方案
- `page-spec`：根据交互方案提取页面生成时需要的设计元素

**补充**
1. experience-blueprint前，可按需增加（一般完整的需求文档可以不做）：
- `stories`：根据需求文档拆解用户任务
- `journey-analysis`：根据需求文档补充用户旅程、断点和风险

2. `prd-review`为分为俩个阶段，第一个阶段输出一份prd审核问题待确认表，需要操作者把所有问题都补充才会进行下一步。


### 没有正式 PRD，只有问题或想法

问题可以来自问题描述、数据或工单分析结论、体验诊断结论，或一个正在被质疑的业务方向。不要直接把原始数据或未整理的聊天记录当作正式输入。
**执行路线**
problem-framing → experience-blueprint → page-spec

- `problem-framing`：明确问题、受影响角色和目标结果，比较业务方案，确定一个主推荐方案。它不负责写 PRD 或交互方案。
- `experience-blueprint`：根据业务方案文档输出完整体验交互方案
- `page-spec`：根据交互方案提取页面生成时需要的设计元素

**补充**
experience-blueprint前，可按需增加（比较推荐，因为这里是自己根据问题输出的业务方案）：
- `stories`：根据业务方案文档拆解用户任务
- `journey-analysis`：根据业务方案文档补充用户旅程、断点和风险
- `uxb`：把已经明确的需求或业务方案进一步转成项目层面的体验方向、设计标准和需要保留的体验结果。它不负责写页面和具体交互。

### 除了以上俩条路线，也可以单独调用skill使用
直接告诉 Agent 目标即可，例如：
```text
请使用 journey-analysis，分析【当前信息】的用户旅程。
```


## 2. 查看skill全貌或当前进度

### 查看skill全貌
打开：
`shared-workflow/progress-preview.html`
这里展示 Skill 的整体路线、主链和可选增强项。

### 查看当前进度
打开：
`spark-output/progress-preview.html`
这里展示当前任务已经完成的 Skill。完成一个 Skill 后刷新页面即可。
如果进度没有更新，可以告诉 Agent：请刷新进度预览。

## 知识库规则

### `knowledge-wiki\knowledge\raw` 下的四类知识

#### `业务`
业务事实的正式来源，也可以直接用于知识问答。主要回答：谁在什么条件下做什么，规则、状态和结果是什么？
消费它的 Skill：`knowledge-wiki`、`prd-review`、`uxb`、`journey-analysis`、`experience-blueprint`、`page-spec`。

#### `设计准则`
体验策略和设计取舍的判断依据，也可以直接用于知识问答。主要回答：为什么这样设计，应该优先保留什么体验结果？
消费它的 Skill：`knowledge-wiki`、`uxb`、`journey-analysis`、`experience-blueprint`。

#### `交互模式`
交互组织和承载方式的参考，也可以直接用于知识问答。主要回答：这类任务通常如何组织入口、操作、反馈、恢复和回流？
消费它的 Skill：`knowledge-wiki`、`uxb`、`journey-analysis`、`experience-blueprint`。

#### `设计规范`
当前产品具体设计标准的查询入口，主要服务设计问答。主要回答：当前产品的样式、组件、页面和场景具体应该怎么做？
消费它的 Skill：`knowledge-wiki`


### 知识自动维护机制

设计师不需要先手动判断材料应该放在哪个文件，先把负责业务的真实资料和信息整理出来即可。
你可以直接提供文字、文档、图片（需要支持多模态）、FAQ、会议记录或其他业务材料。`knowledge-wiki`会帮助你：

- 提取事实并整理结构
- 判断内容属于哪类知识
- 发现重复、冲突和缺口
- 判断哪些内容可以正式入库，哪些需要补充或确认

它会先给出处理建议，得到你的确认后再写入正式知识。由于业务知识复杂，自动整理不可能保证百分之百准确，所以还需要人认真审核。

### 四类知识梳理规范（推荐）
以下是按照**XFT** 设计师的分工，其他小组自定义即可

#### `业务`
**负责人**：再不动业务知识结构前提下，各自设计整理梳理负责的业务知识库的查漏补缺，如需改知识文件结构尽量拉*张锋*一起讨论，不要轻易改，具体梳理指南如下：
- 围绕一条完整业务任务收集信息，下面以**费用管理**、**权限管理**两个正式知识模块为例（例子来自 `.claude/skills/knowledge-wiki/knowledge/raw/业务/` 下的正式文件）：
1. **谁在使用**：有哪些角色，分别负责什么？
   - 费用管理：员工发起报销、审批人审批、财务人员审核支付、管理员配置费用体系、管理层查看报表
   - 权限管理：权限管理员给用户/角色授权、子管理员只能在管辖范围内授权、普通员工是被授权对象、双管理员互为互审
2. **要做什么**：每个角色要完成什么任务，前置条件是什么？
   - 费用管理：员工形成报销单并关联费用、发票后提交审批；财务审核通过后发起支付
   - 权限管理：权限管理员"给单个用户开权限"：先检索定位用户，再配置功能权限、数据范围，最后核对权限详情
3. **怎么完成**：使用什么功能、操作什么对象，有哪些规则或权限？
   - 费用管理：发票验真验重（同一张发票每天最多核验 5 次）；对私报销走薪资代发、云直联或 CBS；启用"有借款必核销"时报销提交必须完成借款核销
   - 权限管理：按决策链判断：先看可见性，再看功能权限，再看数据范围（部分数据权限下条件组内取交集、组间取并集），最后叠加治理生效
4. **结果是什么**：成功、拒绝或中断后会产生什么结果，下一步由谁接手？
   - 费用管理：报销单审批完成进入待支付，支付成功生成台账，凭证自动推送记账
   - 权限管理：权限审批通过后生效，变更写入审计日志，可供事后追溯
5. **状态如何变化**：过程中有哪些状态，状态在什么条件下变化？
   - 费用管理：报销单按"草稿 → 审批中 → 已退回/已撤回 → 审批完成 → 支付成功"流转
   - 权限管理：权限变更按"draft → pending → approved → effective → revoked"流转（治理状态模型）
6. **异常怎么办**：会遇到哪些失败或异常，谁处理，能否恢复？
   - 费用管理：支付失败可重新支付，或由财务手动确认成功；对账失败可重新对账；已审批单据需通过反冲单作废
   - 权限管理：用冲突原因码定位"为什么没权限"，如 VISIBILITY.APP_NOT_VISIBLE（应用不可见）、GRANT.NO_FUNCTION_GRANT（未配功能权限）、SCOPE.DATA_SCOPE_EMPTY（数据范围为空）

**备注**：尽量提供当前、真实、可核对的事实，已经过期、尚未确认或存在差异的内容不要放到知识库，梳理好了可以把文件夹路径复制，然后可以这样开始：
```text
请使用 knowledge-wiki，整理我负责的【业务文件路径】知识。
请先根据材料整理分类，指出重复、冲突、缺口和建议处理方式，等我确认后再入库。
```
**补充**：
1. 再不动业务知识结构前提下，各自设计整理梳理负责的业务知识库的查漏补缺
2. 业务模块中的**页面与字段** 文档，大家可以参考**权限管理**文件去建立，或者询问*张锋*、*王佳*也可以。该文档对于页面生成很有帮助。

##### 页面与字段

用于从页面截图、用户说明和领域业务知识中识别独立页面载体，统一维护各业务领域的"页面与字段"文档。产物包含领域信息架构与页面导航、按固定九字段排列的页面详情；页面条目的基本单位是独立页面载体而非截图。事实来源优先级为：用户说明 > 截图 > 既有信息架构 > 领域业务知识 > [GAP]，未知信息统一用 [GAP]，不猜测、不补写。详细规则见 docs/业务知识/页面与字段提取指南.md。

#### `设计准则、设计模式`
**负责人：张锋**：该模块知识涉及到具体体验策略消费机制，比较重要。大家可以提供对应知识。负责人来输入到知识库

#### `设计规范`
**负责人:张婷婷**：该模块主要用于问答，知识本身需要保持大致的一致性，大家可以提供知识，由婷婷统一输入到知识库。