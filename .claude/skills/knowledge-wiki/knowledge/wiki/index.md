# Knowledge Wiki Index

本页只负责选择知识类型、业务集合或可直接消费的领域。进入后按必要 README 或大文件内部路由定位正式文件或章节；不得遍历 raw。

## 系统页

- [当前健康状态](overview.md)
- [显式未决项](questions.md)

## 业务

### 人事服务

- 命中：这是人事服务业务知识库的二级路由入口，用来说明各子域目录分别承载什么知识、遇到什么问题该优先进入哪个子域。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[人事服务](../raw/业务/人事服务/README.md)

### 企业设置

- 命中：这是企业设置领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[企业设置](../raw/业务/企业设置/README.md)

### 协同办公

- 命中：这是协同办公业务知识库的二级路由入口，用来说明各子域目录分别承载什么知识、遇到什么问题该优先进入哪个子域。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[协同办公](../raw/业务/协同办公/README.md)

### 安全管理

- 命中：这是安全管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[安全管理](../raw/业务/安全管理/README.md)

### 审批管理

- 命中：这是审批管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[审批管理](../raw/业务/审批管理/README.md)

### 工作台与全局入口

- 命中：这是工作台与全局入口领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[工作台与全局入口](../raw/业务/工作台与全局入口/README.md)

### 应用管理

- 命中：这是应用管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[应用管理](../raw/业务/应用管理/README.md)

### 成员管理

- 命中：这是成员管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[成员管理](../raw/业务/成员管理/README.md)

### 服务管理

- 命中：这是服务管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[服务管理](../raw/业务/服务管理/README.md)

### 权限管理

- 命中：这是权限管理领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[权限管理](../raw/业务/权限管理/README.md)

### 组织架构

- 命中：这是平台组织底座知识库的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[组织架构](../raw/业务/组织架构/README.md)

### 账户与企业生命周期

- 命中：这是账户与企业生命周期领域的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[账户与企业生命周期](../raw/业务/账户与企业生命周期/README.md)

### 费用管理

- 命中：这是费用管理业务知识库的二级路由入口，用来说明各子域目录分别承载什么知识、遇到什么问题该优先进入哪个子域。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[费用管理](../raw/业务/费用管理/README.md)

### 项目背景

- 命中：这是项目背景知识库的二级路由入口，用来说明本领域承载什么知识、遇到什么问题该优先看哪些语义文件。
- 排除：只出现相近词但最终结果不由该入口解释时，按入口内的覆盖边界与条件依赖处理。
- 入口：[项目背景](../raw/业务/项目背景/README.md)

## 设计知识

### 设计准则

- 命中：需要判断交互行为应遵循的原则、红线、风险与例外。
- 排除：需要具体交互组织方式时进入交互模式；业务事实仍进入业务知识。
- 入口：[设计准则章节地图](../raw/设计准则/设计准则.md)

### 交互模式

- 命中：需要选择入口、承载、行为、状态反馈、恢复与回流的具体交互模式。
- 排除：只需原则判断时读取设计准则；不得用模式替代业务事实。
- 入口：[交互模式全局地图](../raw/交互模式/README.md)
