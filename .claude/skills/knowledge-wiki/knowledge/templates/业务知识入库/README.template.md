# <知识集合或领域名称>

## 这是什么

这是 `<知识集合或领域名称>` 的契约与路由入口。只有下层存在多个可选知识单元，且必须先判断边界、路由、依赖或停止条件时才使用。具体规则写入对应 raw 文件或章节，不在 README 复制正文。

## 知识集合契约

### 领域定义

<本知识集合或领域直接回答什么问题。>

### 覆盖与排除

- 覆盖：<本领域负责的对象、规则或结果。>
- 排除：<明确不由本领域回答的相近问题及所属领域。>

### 核心对象与责任

- <对象或责任。>

### 显式缺口

- [GAP] <尚未确认的事实；没有则写“暂无显式缺口”。>

## 任务触发索引

- <需求信号>：读 `<文件名>.md` 的 `<章节标题>`；<达到什么证据后停止>。

## 条件依赖与回主域

- <触发条件>：进入 `<依赖领域>/README.md`，读取 `<文件或章节>`；随后回到本域确认 `<结果>`。

## 正式知识清单

- [<文件标题>](<文件名>.md)

## 停止条件

- 命中任务所需文件或章节，并补齐明确触发的依赖域后停止。
- 未命中条件依赖时不扩读相邻领域；README 未定位到正式知识时直接记录知识缺口。
- 读取依赖域后必须回到本领域确认对象、规则、状态、结果与已知缺口。

## 维护边界

- README 只承载领域契约与路由，不展开具体业务规则正文。
- README 不能比上层明显缩小范围时，不应创建或保留。
- 有内容才建文件，不为编号、目录或形式完整批量造文件。
- 大文件可继续保留；行数只触发导航检查，不自动触发拆分。
- 信息不足时使用 `[GAP]`、`[CONFLICT]`、`[QUESTION]`，不得补造结论。
