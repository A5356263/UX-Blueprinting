# 知识候选提醒规则（已停用）

本文件保留仅为兼容旧引用。

当前规则已经调整为：

- `UXB` 不再在分析过程中主动提醒“是否整理成知识候选”
- `UXB` 不再承担知识候选生成或交接职责
- 与业务 Skill 正式产物相关的知识沉淀，统一后置给 `knowledge-wiki`

如果后续用户明确发起知识沉淀任务，应由 `knowledge-wiki`：

1. 扫描正式产物
2. 判断是否值得生成知识候选
3. 决定是新增、覆盖、重复，还是无需入库

因此：

- 不再使用旧的候选提醒文案
- 不再在 `UXB` 中询问“是否需要我整理成知识候选”
