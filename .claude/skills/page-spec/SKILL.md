---
name: page-spec
description: >
  页面规格提取 Skill。从体验蓝图或用户指定的设计材料中提取页面结构、交互、状态、文案和模板变量，输出可用于页面生成的零叙事规格文档。
  触发关键词：页面结构提取、提取页面、page spec、拆页面、把蓝图拆成页面、页面生成规格、从设计稿提取页面规格。
  仅在用户明确要求提取页面结构或生成页面规格时使用；不得仅因体验蓝图或设计文档已经存在而自动触发。
  排除：体验方案（用 experience-blueprint）、现有界面诊断（用 interface-audit）。
---

# Page Spec

这个 skill 只做一件事：把设计材料整理成页面生成契约，过滤掉页面生成无关的叙事和说服性内容，输出一份干净的、可直接用于页面生成的规格文档。主要输入是体验蓝图，也接受用户直接上传的设计材料。

## Step 0 · 运行入口

### Step 0.1 · 本 Skill 产物状态

执行本 Skill 前，只检查本 Skill 对应正式产物是否存在。

正式产物：
- `spark-output/page_spec.md`
- `spark-output/context/page-spec.json`

只允许检查文件是否存在；禁止读取产物正文、禁止解析 JSON 内容、禁止根据已有产物改变当前任务类型。

若任一正式产物存在，先输出以下状态提示，然后继续执行本 Skill 的入口规则：

```text
检测到本 Skill 已有正式产物（已产出）。
```

该提示只表示状态，不代表采取任何处理动作。

禁止：
- 读取产物正文
- 解析 JSON 内容
- 根据已有产物改变当前任务类型
- 根据已有产物执行下游
- 根据已有产物询问处理方式
- 根据已有产物推断用户意图

### Step 0.2 · 上游读取

启动后按以下顺序读取或识别输入：

1. 按当前 `SKILL.md` 的规则确认本 Skill 自身输入边界
2. 检查 `spark-output/context/experience-blueprint.json` 与 `spark-output/experience_blueprint.md` 是否存在
3. 若体验蓝图产物存在，优先读取 JSON，再读取 Markdown 正文
4. 可选检查并读取 `spark-output/context/edge.json` 与 `spark-output/edge_output.md`
5. 识别用户当前明确提供的文档、截图、交互稿或其他设计材料

### Step 0.3 · 模式判断与降级

完成输入识别后，再判断当前输入模式并进入正式提取流程。

`page-spec` 支持两种正式输入模式：

- **链路模式**：已有体验蓝图产出，优先基于蓝图提取页面规格
- **独立模式**：用户直接提供文档、截图、交互稿或其他设计材料，独立完成页面规格提取

#### 链路模式（检测到蓝图）

以 Step 0.2 已读取的体验蓝图作为正式输入；如存在异常态产物，将其作为可选补强输入。

如果 JSON 存在但 MD 不完整（缺少 §3/§5/§6/§7 中的任一章节），在输出开头标注缺失项，继续提取已有部分。

对用户的固定说明应表达为：

```text
已检测到体验蓝图产出。

推荐直接基于体验蓝图提取页面规格，这会是当前最稳定的输入路径。
如果你还想补充页面截图、交互稿、PRD 或其他设计材料，也可以一并提供，我会合并吸收后再提取。
```

输出上述固定说明后，直接基于体验蓝图进入正式提取；不得把可选补充材料理解为必须等待用户再次确认。

#### 独立模式（未检测到蓝图）

输出以下固定激活语，等待用户提供材料：

```text
当前未检测到体验蓝图产出。

推荐你先完成需求分析 / 体验策略，再进入体验蓝图，这样后续页面规格提取会更稳定。
如果你已经有页面截图、PRD、需求文档、交互稿或其他设计材料，也可以直接提供，我会先按现有材料提取页面规格。
```

用户提供的材料可以是文档、图片或其他设计产物。独立模式不是降级兜底，而是 `page-spec` 的正式能力之一；收到材料后按正文的提取规则执行。

## 角色定义

Page Spec 负责：

- 从体验蓝图或用户提供的设计材料中识别所有页面级实体（页面、弹窗、抽屉）
- 提取每个实体的结构、交互流程、状态枚举、异常恢复和结果态
- 提取并集中管理文案池与模板变量
- 保留 ASCII 线框用于结构表达
- 输出零叙事的结构化页面规格 MD

Page Spec 不负责：

- 布局、间距、颜色、字体、圆角、动画等视觉属性
- 具体组件库实现
- HTML/CSS/JS 生成
- 设计质量判断或方案评审

Page Spec 不是静态说明文档，而是页面生成契约。`ASCII 线框`、`文案池`、`模板变量` 是正式输出，不是可选附录。页面主体必须支持多状态、多异常、多结果态，而不是只保留 Happy Path。

Page Spec 必须保留语义组件类型，用于页面生成。语义组件类型包括但不限于：

`Page`、`Drawer`、`Modal`、`Toast`、`Button`、`Input`、`Textarea`、`Select`、`Checkbox`、`CheckboxGroup`、`Radio`、`Switch`、`Table`、`List`、`Tabs`、`Tag`、`Tooltip`、`InlineError`、`EmptyState`、`LoadingState`。

语义组件只描述承载方式和交互角色，不绑定具体 UI 库，不写颜色、圆角、间距、阴影、动画或实现代码。

## `edge` 可选补强输入规则

`edge` 不是 `page-spec` 的必需上游，但如果存在，必须按“状态补丁层”消费。

消费原则：

- `experience-blueprint` 仍然是主输入源
- `edge` 只补状态、异常恢复和文案提示
- `edge` 不得反向改写页面结构、主交互流程、实体范围和父页面关系

允许吸收的内容：

- `states_matrix[].states[].state_type`
- `required`
- `severity`
- `design_description`
- `fallback_behavior`
- `copy_hint`
- `target_section`
- `critical_missing[]`

不允许吸收为主规格主体的内容：

- 新的页面实体定义
- 对主流程结构的重写
- 对父页面 / 承载页关系的改写
- 脱离体验蓝图单独成立的页面方案

## 知识补充与父页面查找

页面主实体如果是弹窗、抽屉或从某页跳转进入的下级页面，通常都依附于某个承载页面。无论输入来自体验蓝图还是用户直接上传的设计材料，只要当前实体依附于承载页，就应尝试恢复该父页面结构。

父页面恢复是 `page-spec` 的通用提取步骤，不是体验蓝图专属前置。knowledge-wiki 只作为补强来源，不是唯一来源，也不是阻断条件。

### 执行顺序

1. 先从当前输入材料中判断是否已经能恢复父页面
2. 当前材料不足时，再进入 `knowledge-wiki` 做知识补充
3. 命中知识后，按 `summary -> raw` 顺序读取，再提取父页面结构
4. 如果知识未命中或 raw 不可读，继续输出当前结果，并在 `提取备注` 中标记缺口

### 知识查找流程

```text
先看当前材料能否恢复父页面
→ 不足时命中知识
→ 先读 summary（路由层）
→ 再读该 summary 对应的 raw（事实层）
→ 再进入父页面结构提取
```

知识消费发现顺序固定为：

1. 读取当前可用 knowledge-wiki 的知识索引 / overview / index
2. 从索引中定位目标领域的 summaries 入口
3. 读取命中的 summary
4. 根据 summary 中记录的 source_path / raw 指向读取对应 raw
5. raw 读取失败时，该知识不得进入“已消费知识”，只能进入知识缺口

`summary` 只负责路由，不能作为正式判断依据。
`raw` 以知识索引或 summary 中的指向为准。
不得用猜测目录名、README 替代 summary、直接广读 raw 的方式绕过索引路由。

### 父页面判断规则

1. **确定业务领域**：优先从当前输入材料中提取业务域；如果当前输入是体验蓝图，可从蓝图 §0 判断、§6 实体描述或 `experience-blueprint.json` 的 `project_name` 中提取业务域（如 `{业务领域}`）
2. **判断是否需要父页面**：对当前主实体判断类型：
   - **弹窗 / 抽屉** → 必须查找父页面。弹窗和抽屉没有独立入口，必须依附于某个页面
   - **下级页面**（从某页面跳转进入） → 必须查找来源页面
   - **顶级页面**（有独立菜单入口） → 自身就是承载页，不需要额外查找
3. **命中知识路由**：仅在当前材料不足时，按固定发现顺序定位覆盖页面载体 / 入口 / 路径的条目
4. **读 summary → 读 raw**：先读命中的 summary，再按 summary 中记录的 source_path / raw 指向读取对应 raw
5. **提取父页面结构**：从当前材料或载体语义文件中原样提取页面结构，保留原文的 ASCII 框图和组件类型标注（`[输入框]`、`[下拉框]`、`[Tab]`、`[主按钮]`、`[文本链接]`、`[标签]`、`[Left]`、`[Right]` 等），不重新排版、不自行概括、不将组件标注转写为自然语言描述
6. **写入实体 0**：将提取的父页面结构写入 `page_spec.md` 的实体 0，排在所有主实体之前

### 不需要查找的情况

- 当前主实体自身就是顶级页面（如有独立菜单入口的列表页、配置页）
- 当前材料已足够恢复承载页面，不需要再额外补知识
- 知识库中不存在对应的载体语义文件 → 在实体 0 中标注“知识库未找到承载页面定义”，并仅从当前材料的零散引用中收拢

## 核心提取规则

按以下 6 层依次提取，提取完成后不再保留源文档的章节结构。

### 第 0 层：页面摘要

在文档最顶部生成一个极简的定位锚点，固定 4 行：

- **产品领域**：从蓝图 §0 和 knowledge-wiki 中提炼所属产品模块与业务域（如"{产品模块} — {业务对象治理}"）
- **页面类型**：列出涉及的页面类型（弹窗 / 列表页 / 详情页等），优先标注主实体类型
- **用户角色**：从蓝图 §1 旅程消费摘要的来源角色中提取；如 §1 不完整，则回退到体验蓝图角色 / 流程章节中的角色信息
- **核心任务**：用一句话概括用户要完成的核心任务（非叙事，只写动作和对象）

摘要不写叙事推导，不写用户目标和痛点，只写锚点信息。

### 第 1 层：生成范围 + 承载页面识别 + 实体识别

先确定本次页面生成范围，固定分成三类：

- `生成`
- `只引用不生成`
- `不生成`

规则：

- 每个实体只能进入一个范围
- `只引用不生成` 只保留一句原因
- `不生成` 只保留一句原因
- 禁止把未定内容写进 `生成`

先扫描蓝图 §3、§4、§6 和 `knowledge-wiki` 知识库中的页面载体语义文件，识别两类实体：

**A. 承载页面**（父页面）
- 蓝图 §6 的主实体（弹窗/抽屉）所依赖的触发页面（如列表页、详情页）
- 蓝图通常不会完整设计承载页面，但会零散引用其结构（入口按钮位置、列表列定义、刷新行为、筛选区等）
- 从以下来源收拢承载页面的结构：
  1. 蓝图 §3 节点 1 中的触发动作描述
  2. 蓝图 §4 次流程中的页面跳转引用
  3. 蓝图 §9 UXB 承接追踪中的页面载体说明
  4. `knowledge-wiki` 中与该页面相关的载体语义类知识
- 承载页面作为实体 0，排在所有弹窗/抽屉实体之前

**B. 功能实体**（弹窗/抽屉/页面）
- §6 的每个子章节（6.1、6.2...）是一个实体。提取实体名称、类型、触发条件
- §4 次交互流程中引用的外部页面（如操作记录页），标记为独立实体，仅提取与本功能相关的内容

不提取：
- 纯引用组件（如"复用添加{业务对象}的{对象选择}弹窗"）——标注引用关系但不展开

### 第 2 层：结构提取

对每个实体，从 §6 的结构描述和 ASCII 框图中提取：

- 区域列表及包含关系（如"标题区 → 标题文字 + 关闭按钮"）
- 每个区域内包含的元素类型（文本、输入框、选择器、复选框、按钮、标签列表等）
- 区域内的信息内容（如来源信息区展示"{对象名称}/{对象标识}/{业务范围}/{能力范围}"）
- 异常状态下新增或变化的区域（如校验阻断时出现 inline 提示区、部分成功时出现跳过明细区）

页面结构主体只保留区域、语义组件、字段、状态和交互规则。来源、缺口、降级原因进入 `提取备注`，不混入页面结构主体。

主体章节只写当前能支撑生成的已定页面信息。待确认项只写影响和约束，不预写实现，不得把待确认项混入页面结构、交互流程、状态表或文案池主体。

页面规格主体默认使用“标题 + 正文 / 短列表”承载。结构化不等于表格化；只有在枚举项非常密集、逐行对照明显更清楚时，才允许短表格。

页面结构建议格式：

```markdown
### 页面结构

- 页面标题区
  - Text：{页面标题}
  - Button：{主操作}
- {业务对象}统计区
  - StatCard：{业务对象总数}
  - StatCard：{分类数}
- {业务对象}列表区
  - ListGroup：按 {分类名} 分组
  - Table：{业务对象名称} / {业务范围} / 来源 / 状态 / 操作

### 提取备注

- 承载页面知识未命中，结构来自体验蓝图 §6.1。
```

不要优先改写成：

- 字段 / 类型 / 说明的大表
- 区域 / 元素 / 规则的大表
- 页面结构总览大表

结构中的示例业务数据必须变量化。不得把具体人名、具体数量、具体业务应用名当作默认页面数据。

使用：

- `{业务对象总数}`
- `{分类数}`
- `{分类名}`
- `{业务对象名称}`
- `{业务范围}`
- `{处理人姓名}`
- `{处理时间}`

ASCII 线框既是结构来源，也是正式主体内容。页面规格主体必须同时保留 ASCII 线框与对应的区域、语义组件清单。

### 第 3 层：交互提取

从 §3 主交互流程和 §4 次交互流程中提取：

- 用户动作（点击、输入、选择等）
- 系统反馈（弹窗变化、状态切换、数据加载等）
- 下一步指向

从 §5 异常与阻断流程中提取：

- 校验顺序与分支条件
- 阻断条件与阻断后行为（弹窗不关闭 / inline 提示）
- 非阻断条件与确认行为（展示名单 → 跳过并继续 / 取消）
- 每个异常的用户恢复路径

交互描述保留原始动作动词（点击/选择/输入），不泛化为"用户操作"。

交互承载要求：

- 主交互流程和次交互流程默认用小标题分块 + 正文 / 短列表
- 不用大表格承载完整交互流程
- 不把动作、反馈、下一步压成一张总表作为主体

### 第 4 层：文案与状态提取

文案从以下位置提取，去重合并：

- §3 各节点的"建议文案"
- §6 各实体的"具体文案"和"按钮"描述
- §7 的"关键文案模板"

每条文案记录原文案字符串，保留模板变量（`{姓名}`、`{N}`、`{M}`、`{当前状态}` 等），并注明该文案出现在哪个区域/状态下。

状态从 §7 的统一状态口径表提取：

- 状态名称
- 触发条件（从"含义"列推导）
- UI 表现（从"统一反馈口径"和"用户可操作"列推导）
- 对应文案

状态与文案承载要求：

- 状态 key、模板变量、极短文案映射允许短表格
- 异常恢复、结果态、长说明文案优先用正文或短列表

如果存在 `edge` 补强输入，则按以下规则吸收：

- `状态表现`：优先补充蓝图中未明确写出的 must / should 状态
- `异常恢复`：补充恢复路径、阻断后的用户出口、是否丢失已填信息
- `文案池`：只补充与状态反馈直接相关的 `copy_hint`
- `不参与本次生成的待确认项`：收口 `critical_missing[]` 中当前仍无法写入主体的缺口

硬规则：

- `edge` 只补主体，不重写主体
- `edge` 的 `copy_hint` 进入文案池时，必须保留来源为异常态补强
- `edge` 的 `target_section` 只作为落位提示使用，不得凌驾于 `page-spec` 自身结构判断之上
- `critical_missing[]` 只有在当前主体仍无法落位时，才进入待确认项
- 同一条 `edge` 补强信息如果已经被吸收到 `状态表现`、`异常恢复` 或 `文案池`，不得再以同义内容重复写入 `不参与本次生成的待确认项`

### 第 5 层：待确认项集中隔离

所有待确认但当前不参与页面生成的内容，统一进入 `不参与本次生成的待确认项`。

规则：

- 每条至少写“待确认项”和“影响”
- 主体章节不再出现 `[GAP]` 式散落标记
- 不得因为有待确认项，就把未定实现写入正文

### 明确丢弃

以下内容不进入页面规格：

- §0 本次关键设计判断
- §1 旅程消费摘要（信心最低点、关键转折、流失风险等旅程分析内容）
- §2 交互流程总览（§3/§5 的上层摘要，信息冗余）
- §9 附录（设计指南消费、业务知识消费、知识缺口、UXB 承接追踪）
- 各节中的"需要前置解释的信息"（属于设计说服，不属于页面规格）
- 散落在正文里的待确认说明
- 纯工程实现讨论

## 输出结构

输出到 `spark-output/page_spec.md`。

输出规则补充：

- 如果宿主支持文件系统，先检查并创建 `spark-output/` 与 `spark-output/context/`，再写入产物

固定结构：

```markdown
# 页面生成规格：{项目名}

## 1. 页面摘要
- **产品领域**：
- **页面类型**：
- **用户角色**：
- **核心任务**：

---

## 2. 生成范围
### 生成
### 只引用不生成
### 不生成

---

## 3. 页面实体关系

---

## 4. {承载页面名}
### 页面目标
### 页面结构（ASCII 线框 + 区域说明）
### 提取备注
### 列表字段
### 关键操作

---

## 5. {主实体名}
### 弹窗目标
### 区域结构（ASCII 框图 + 区域编号说明）
### 区域说明
### 校验阻断态变化
### 不合规确认区结构

---

## 6. 主交互流程

---

## 7. 校验规则

---

## 8. 状态表现
固定短表格：

| 状态 | 触发条件 | UI 表现 | 文案 key |

---

## 9. 异常恢复

---

## 10. 结果态
### 10.1 全部成功
### 10.2 部分成功

---

## 11. 文案池
### 弹窗
固定短表格：`| key | 文案 | 模板变量 | 位置 |`
### 复制对象选择
固定短表格：`| key | 文案 | 模板变量 | 位置 |`
### 校验与执行
固定短表格：`| key | 文案 | 模板变量 | 位置 |`
### 结果与反馈
固定短表格：`| key | 文案 | 模板变量 | 位置 |`
### 按钮
固定短表格：`| key | 文案 | 位置 |`

---

## 12. 模板变量
固定短表格：

| 变量 | 含义 | 取值 |

---

## 13. 不参与本次生成的待确认项
固定短表格：

| 待确认项 | 影响 |
```

主体承载限制：

- 页面结构、交互流程、异常恢复三部分不得整章表格化
- 表格只保留在状态 key、模板变量、文案池、待确认项、极短字段对照等高密度枚举区域
- 未明确允许的章节，不用 Markdown 表格承载主体内容

生成范围规则：

- `生成`：本次页面生成必须覆盖的实体
- `只引用不生成`：用于交互跳转、状态来源或上下文承接，但不生成页面
- `不生成`：蓝图未给出结构、超出本次任务范围或属于已有系统能力
- 每个实体只能归入一个范围
- `只引用不生成` / `不生成` 最多写一句原因

保留能力提醒：

- 当前 skill 中已有的父级页面 / 承载页恢复逻辑属于正式提取能力，本轮不得改写、弱化或删除
- 当前材料不足时，通过 `knowledge-wiki` 补足父页面结构的规则必须保留
- 本轮优化只补充和收紧输出结构，不重写父页面恢复、知识补充或链路识别能力

## Context JSON 写入

文档生成后，按固定结构写入 `spark-output/context/page-spec.json`。

固定结构：

```json
{
  "skill": "page-spec",
  "version": "1.0",
  "generated_at": "unknown",
  "project_name": "unknown",
  "artifact_md": "spark-output/page_spec.md",
  "source_refs": [],
  "read_sections": [],
  "page_summary": {
    "product_domain": "unknown",
    "page_type": "unknown",
    "user_role": "unknown",
    "core_task": "unknown"
  },
  "generation_scope": {
    "generate": [],
    "reference_only": [],
    "do_not_generate": []
  },
  "entity_relationships": [
    {
      "entity": "unknown",
      "type": "unknown",
      "relation": "unknown"
    }
  ],
  "entities": [
    {
      "name": "unknown",
      "type": "unknown"
    }
  ],
  "extraction_summary": {
    "entities_count": 0,
    "states_count": 0,
    "copy_count": 0,
    "exceptions_count": 0
  },
  "pages": [
    {
      "page_id": "unknown",
      "page_name": "unknown",
      "page_goal": "unknown",
      "structure_ascii": "unknown",
      "regions": [
        {
          "region_id": "unknown",
          "name": "unknown",
          "purpose": "unknown",
          "content": []
        }
      ],
      "extraction_notes": [],
      "list_fields": [
        {
          "field": "unknown",
          "meaning": "unknown",
          "source": "unknown"
        }
      ],
      "key_actions": [
        {
          "action": "unknown",
          "trigger": "unknown",
          "result": "unknown"
        }
      ]
    }
  ],
  "entity_specs": [
    {
      "entity_name": "unknown",
      "surface_type": "unknown",
      "goal": "unknown",
      "structure_ascii": "unknown",
      "regions": [],
      "validation_blocking_state_changes": [],
      "non_compliance_confirm_area": {
        "structure": "unknown",
        "copy": [],
        "actions": []
      }
    }
  ],
  "main_interaction_flow": [
    {
      "step_id": "unknown",
      "trigger": "unknown",
      "user_action": "unknown",
      "system_response": "unknown",
      "state_change": "unknown",
      "next_step": "unknown"
    }
  ],
  "validation_rules": [
    {
      "rule_id": "unknown",
      "object": "unknown",
      "condition": "unknown",
      "blocking": "unknown",
      "message_key": "unknown"
    }
  ],
  "states": [
    {
      "state": "unknown",
      "trigger_condition": "unknown",
      "ui_behavior": "unknown",
      "copy_key": "unknown"
    }
  ],
  "exception_recovery": [
    {
      "exception": "unknown",
      "trigger": "unknown",
      "ui_behavior": "unknown",
      "recovery_path": "unknown"
    }
  ],
  "result_states": [
    {
      "state": "unknown",
      "condition": "unknown",
      "ui_behavior": "unknown",
      "copy_key": "unknown"
    }
  ],
  "copy_pool": {
    "modal": [],
    "object_selection": [],
    "validation_and_execution": [],
    "result_and_feedback": [],
    "buttons": []
  },
  "template_variables": [
    {
      "variable": "unknown",
      "meaning": "unknown",
      "value_range": "unknown"
    }
  ],
  "open_questions": [
    {
      "question": "unknown",
      "impact": "unknown"
    }
  ],
  "edge_consumed": false,
  "edge_trace": [
    {
      "screen_id": "unknown",
      "state_type": "unknown",
      "target_section": "unknown"
    }
  ]
}
```

硬规则：

- 字段固定，不得新增、删除或改名。
- 只填入本 Skill 正式 Markdown 已产出的信息；缺失信息写 `unknown`、空数组，或进入 `open_questions[]`。
- 不得为了填满 JSON 编造信息。
- 不得只保留实体和统计数；页面结构、流程、校验、状态、异常恢复、结果态、文案池必须同步写入。
- `copy_pool` 必须保留五类文案池。
- 未读取或未吸收 `edge` 时，`edge_consumed = false` 且 `edge_trace = []`。
- 吸收 `edge` 时，`edge_trace[]` 只记录来源锚点，不复制整段状态描述。
- JSON 不复制 Markdown 全文。
- 写入失败不阻断完成，但应在输出中提示。

## Handoff · 固定下一步

固定输出：

```text
页面设计文档已输出。
目前 design skill 正在建设中，你可以让 agent 根据文档直接生成 HTML 原型。
```

如需刷新进度预览，可使用项目已有预览入口；刷新失败不影响当前 Skill 完成。
