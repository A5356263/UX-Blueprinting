# Test Inputs 说明

本目录用于存放 Skill 的固定测试输入。

## 目录职责

这里的文件只负责：

- 固定测试口径
- 提供可重复使用的最小输入
- 支持同一条链路反复验证

这里的文件不负责：

- 定义布局规则
- 提供参考块实现
- 充当模板资产
- 替代真实用户需求

## 使用原则

- `references/` 回答“页面或区域通常如何组织”。
- `test-inputs/` 回答“我们用什么输入来测试 Skill”。
- 测试输入应尽量短，但必须足够明确，能稳定触发目标链路。
