# Modules

本目录按模块拆分规则，每个文件只负责模块的判断边界。

## 负责

- 触发条件
- 职责边界
- 不可破坏的节点关系
- 禁止项
- 条件分支
- 失败回退

## 不负责

- 默认 HTML / CSS 细节
- 默认轻交互接法说明
- 控件尺寸、状态、token 的长篇实现说明

这些稳定默认实现优先承接自 `references/`、`design-systems/` 和 `runtime/`。

## 统一格式

- 规则标识
- 触发条件
- 硬规则
- 禁止
- 条件分支
- 实现约束
- 失败回退
