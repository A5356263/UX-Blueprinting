# 管线衔接文案（精简版）

## 目标

本文件用于统一各个 Skill 组阶段完成后的用户可见衔接文案口径。

它解决的是：
- 当前阶段完成后怎么说
- 给用户什么下一步选项
- 单选、多选、终端节点分别怎么表达

它不解决：
- Skill 内部是否自动继续执行
- 下游 Skill 是否真实触发
- Skill 自己的执行策略、降级逻辑和产物生成逻辑

---

## 边界

本文件只约束 **Skill 组之间的衔接文案**，不约束 Skill 本身的内部流程。

具体边界如下：
- `SKILL.md`：负责 Skill 内部自己的执行逻辑和收尾策略
- `_shared/skill-graph.json`：负责 Skill 组关系、候选下游和推荐数据
- 本文件：只负责把这些关系翻译成统一、轻量、稳定的用户提示文案

因此：
- 本文件可以定义“完成后怎么提示”
- 本文件不能定义“完成后自动做什么”
- 本文件不能替代 Skill 内部的真实执行逻辑

---

## 执行原则

- 先输出完成句，再输出下一步推荐
- 只面向用户表达，不暴露内部执行细节
- 话术保持简洁，避免解释实现机制
- 推荐项只表达“可以继续做什么”，不表达“系统将自动做什么”
- 终端节点只收口，不继续分支
- 基础设施类 Skill 单独处理，不混入主设计管线阶段语气

---

## 验收标准

一条衔接文案是否合格，以以下标准判断：

1. 有完成句  
格式为：`✅ {name_zh} 完成，{产物简述}`

2. 多选项格式统一  
必须使用编号列表，且结尾统一为“你回复对应数字编号即可”

3. 单选项格式统一  
必须包含 `{label} — {reason}`，且结尾统一为“你回复"{下游 name_zh}"即可”

4. 终端节点不再给继续选项  
只保留完成句

5. 不掺入 Skill 内部逻辑  
不写“接下来会自动生成……”“系统会继续执行……”这类描述

6. 不暴露实现细节  
不出现脚本名、目录名、上下文文件路径、内部状态机等工程细节

7. 基础设施类 Skill 语气单独收口  
不误导用户把它们理解成主设计阶段节点

---

## 非职责

本文件不负责：
- 判定某个 Skill 是否真的执行
- 判定某个 Skill 是否自动继续
- 决定下游是否立即触发
- 修改 Skill 的产物内容
- 定义 Skill 的内部结尾逻辑

这些都属于 Skill 本身或 `_shared/skill-graph.json` 的职责。

## 模板

多选项：
```
✅ {name_zh} 完成，{产物简述}
你可以选择：
- 1.{label_a}
- 2.{label_b}
...
你回复对应数字编号即可
```

单选项：
```
✅ {name_zh} 完成，{产物简述}
你可以选择：{label} — {reason}
你回复"{name_zh}"即可
```

终端节点只输出完成句。

---

## 各 Skill 衔接文案

### UXB（需求定案）
```
✅ 需求定案完成，需求文档和结构化数据已就位。
你可以选择：
- 1.输出体验方案
- 2.输出方案埋点
你回复对应数字编号即可
```

### 体验蓝图
```
✅ 体验方案完成，交互流程、页面结构、状态反馈、文案都有了。
你可以选择：
- 1.输出设计文档，给AI生成原型页面
- 2.输出方案埋点
- 3.深度分析方案异常情况
- 4.视觉情绪板
- 5.设计走查
你回复对应数字编号即可
```

### 异常态
```
✅ 异常态分析完成，状态矩阵和异常设计补充已输出。
你可以选择：设计走查 — 整体过一遍完整性和一致性
你回复"设计走查"即可
```

### 视觉情绪板（终端）
```
✅ 视觉情绪板完成，视觉方向和设计变量已定。
```

### 设计走查（终端）
```
✅ 设计走查完成，发现清单已出。
```

### 页面规格提取（终端）
```
✅ 页面规格提取完成，页面结构、交互和文案规格已出。
```

### 旅程埋点（终端）
```
✅ 旅程埋点完成，埋点需求和异常追踪口径已出。
```

---

## 基础设施

### knowledge-wiki
```
✅ 知识库已更新。继续走管线的话，回到上一步就好。
```

### product-analysis
```
✅ 产品分析完成。
你可以选择：需求定案 — 回到需求侧继续推进
你回复"需求定案"即可
```

### design-strategy（独立，不参与管线）
```
✅ 设计策略报告已产出。
```
