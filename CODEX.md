# 编码执行约束

## 1. 先思考再编码
- 不得静默假设
- 有歧义先说明可选解释
- 有更简单方案需主动指出
- 不清楚时停止猜测

## 2. 简单优先
- 不做未被要求的能力
- 不为单次使用做抽象
- 不为假想场景补配置
- 避免过度设计

## 3. 外科式修改
- 只改与当前任务直接相关的代码
- 不顺手改无关注释、格式、结构
- 只清理本次改动产生的无用代码
- 发现历史问题只提示，不擅自处理

## 4. 目标驱动执行
- 先定义成功标准，再开始实现
- 能写测试先写测试
- 每一步都要有验证方式
- 不以“差不多能跑”作为完成标准

## 路径与目录规则

1. 正式主链路输出统一写入 `spark-output/`。
2. 业务知识与设计准则统一从 `knowledge/` 读取。
3. 用户输入统一从 `input/` 读取。
4. 跨 Skill 协调统一通过项目根目录下的 `_shared/` 完成。
5. Skill 定义统一位于 `.claude/skills/`。

## 跨系统稳定性原则

1. 项目实现不得偏向单一系统环境，默认必须保证 Windows、macOS、Linux 下都能稳定运行。
2. 不得依赖某一种系统特有的路径习惯、编码表现、命令语法、权限行为或文件分隔符。
3. 所有路径、格式、脚本入口和文件处理逻辑，都应优先采用跨平台写法，避免因系统差异阻塞主链路。
4. 只要存在跨系统运行可能，验证时就不能只在单一环境下判断“可用”，至少要显式考虑 Windows、macOS、Linux 的兼容性。

## 能力模块边界规则

1. `.claude/skills/uxb/` 负责需求分析定案。
2. `.claude/skills/experience-blueprint/` 负责体验蓝图输出。
3. `_shared/` 存放跨 Skill 协调文件，包括流转关系、context schema 和交接模板。
4. `knowledge/` 是共享数据层，不应被写死进 Skill 执行逻辑。
5. `input/` 是用户输入层，`spark-output/` 是输出层。
6. Skill 之间通过 `_shared/` 定义的协议交接，不直接调用对方。
7. `packages/experience_preview/` 仅作为 HTML 预览参考实现保留，不再承担正式主链路入口职责。

## 适用范围
- 本文件用于约束 AI 编程助手在本项目中的执行方式与行为边界。
