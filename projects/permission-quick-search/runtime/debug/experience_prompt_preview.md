# Experience Prompt 预览（仅调试）

> 说明：此文件仅用于排查，不参与主链路生成与评审。
> 权威输入：`projects/permission-quick-search/workspace/facts.md`、`projects/permission-quick-search/workspace/business_blueprint.md`、`E:/AI设计/体验蓝图构建思路/specs/10_experience_blueprint_contract.md`、`E:/AI设计/体验蓝图构建思路/templates/experience_blueprint.template.md`

## 1. 任务目标

- Protocol Name: Cross-AI Task Card
- Protocol Version: v0.2
- Task ID: permission-quick-search
- Task Name: 功能权限设置页面内查找定位
- Domain: 权限管理
- 在功能权限设置页面增加页面内关键词查找定位能力，帮助权限管理员在 1100+ 权限项中快速定位目标内容
- 本次输出主要服务于体验设计评审
- 这是新建任务，为已有功能权限设置页面新增查找定位交互

## 2. route 判断

- 需求类型：局部体验优化；建议路线：fast；置信度：unknown。
- 重点维度：信息密度高：1100+权限项中查找定位、折叠节点内命中需自动展开、大量命中时的性能与切换体验。
- route 输出建议：查找入口的可见性与可达性；命中高亮样式与当前命中项的视觉区分；折叠节点内命中的展开与定位
- route 判断理由：本次需求是在功能权限设置页面增加页面内关键词查找定位能力（类似 Ctrl+F）。查找行为不改底层业务对象、规则、状态机、生效机制、数据口径和外部回写链路。权限的勾选、取消、保存逻辑完全沿用现有流程。所有权限项始终可见，只增加文字高亮和焦点跳转交互。属于纯展示层体验优化。
- 写作规则以对应 specs 中的路线说明要求为准。

## 3. facts 摘要

- > 本文事实来自以下输入：
- > - projects/permission-quick-search/source/requirement.md（需求文档，已按操作者确认的交互方式改写）
- > - projects/permission-quick-search/source/background.md（业务背景与现状约束）
- 功能权限设置页面承载着企业权限管理的核心配置工作。当前页面末级菜单约 1100+ 项，操作功能点约 1200+ 项，企业开启所有标准应用后，权限管理员需滚动约 35 屏才能完整浏览。
- 权限管理员、实施人员和运营人员在日常工作中经常需要查找特定权限项——可能是确认某个授权对象已拥有哪些权限、核对配置是否正确、或定位需要纠正的权限项。目前他们只能逐屏滚动查找，效率低且容易遗漏。
- 本次需求希望在功能权限设置页面中增加一个页面内查找定位能力。管理员输入关键词后，页面中匹配的文字以高亮样式标记，页面视觉焦点自动滚动到第一个命中位置，并支持在多个命中项之间上下切换。查找行为不改变页面数据展示范围，所有权限项始终可见，权限的勾选、取消、保存、取消保存逻辑完全沿用现有流程。
- **关键词查找入口**：在功能权限设置区域提供查找输入框，管理员可在此输入关键词。
- **命中文字高亮**：输入关键词后，页面中所有匹配文字以高亮样式标记。关键词清空后高亮消失，页面恢复常规展示。高亮需在权限树结构中可见，包括折叠节点内的命中项。
- **焦点跳转**：输入关键词后，页面视觉焦点自动定位到第一个命中位置。如果命中项处于折叠节点内，需先展开对应层级再定位。
- **命中项切换**：支持在多个命中结果之间上下切换，切换时页面跟随滚动到对应位置。
- **无命中提示**：当关键词在页面内无任何匹配时，提示"未找到匹配内容"。无命中时不改变页面当前展示和滚动位置。
- 本次查找定位能力不引入新的业务规则。需要明确的是：

## 4. business 核心判断与承接要求

### 5. 推荐业务方案
- 角色与职责
- 主流程闭环
- 前置配置
- 结果回写与记录沉淀

### 6. 必须守住的规则和边界
- 不能放开的限制
- 必须审批的场景
- 必须二次确认的操作
- 必须前置说明的信息

### 7. 主要风险与保护策略
- 每个风险说明：
- 风险是什么
- 影响什么
- 如何缓解
- 涉及报错、阻断或状态反馈时，先判断真实问题属于业务规则、流程、权限、状态、配置、校验、反馈形式还是文案问题。不要把没有补救路径、规则冲突、责任边界不清的问题直接交给 experience 改提示。

### 8. 方案承接要求
- 后续 experience 阶段必须承接的内容：
- 必须覆盖的角色
- 必须闭环的主流程
- 必须处理的异常
- 必须解释的状态
- 必须保护的风险
- 可作为设计建议的内容
- 报错或阻断场景中，哪些业务依据已经足够支持体验方案，哪些仍需确认

### 9. 待确认问题
- 仅保留会影响后续方案的重要问题
- 每项说明：问题点、影响范围、建议确认方式

## 5. 设计指南导航（按需消费）

已选中的具体指南：
- knowledge/wiki/summaries/设计准则/信息架构.md
- knowledge/wiki/summaries/设计准则/反馈与报错/00_反馈与报错体验设计指南.md
- knowledge/wiki/summaries/设计准则/可用性.md

## 6. 设计指南消费判断

在输出体验蓝图前，请基于 facts.md 和 business_blueprint.md 判断：
1. 本次业务蓝图中是否出现报错、阻断、校验、状态反馈、审批延迟、批量风险、高风险配置等体验问题。
2. 这些问题是否命中 Design Guidelines 中的具体 summary。
3. 如果命中 summary，必须读取其 source_refs 指向的 raw，并只吸收原则，不暴露大段原文。
4. 体验蓝图不得凭指南替代业务事实；业务事实不足时，只能输出待确认问题或条件型建议。
5. 输出方案时，需要说明反馈时机、反馈形式、用户可见文案和用户下一步。


## 7. 设计原则摘要

- 先写主流程，再补次流程与异常阻断流程。
- 页面/弹窗/抽屉必须写清页面目标、进入条件、操作、状态反馈和异常处理。
- 文案必须给具体草案，不写抽象策略句。
- 禁止重做事实抽取、业务判断或需求全文重读。

## 8. 待确认问题

- <如无可填写 none>
- <如无可填写 none>
- <填写待补信息>

## 9. 输出模板要求

- 输出文件：`projects/{project_id}/workspace/experience_blueprint.md`
- 固定章节：
  - `## 1. 交互流程总览`
  - `## 2. 主交互流程`
  - `## 3. 次交互流程`
  - `## 4. 异常与阻断流程`
  - `## 5. 页面 / 弹窗 / 抽屉设计`
  - `## 6. 状态与反馈文案`
  - `## 7. 待确认问题`
