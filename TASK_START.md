# TASK_START

## 文件定位

本文件只用于约束 AI 在新任务开始时避免沿用旧任务惯性。

本文件不约束源码修改，不替代 `AGENTS.md`，不参与 facts / business / experience 的正式判断。

读取本文件时，不要求 AI 同时读取 `AGENTS.md`。  
只有当任务涉及修改项目源码、规则或结构时，才需要再读取并遵守 `AGENTS.md`。

## 核心原则

当用户提出新的任务编号、新目录、新真实需求或新一轮执行时，AI 必须按新任务处理。

新任务的默认起点是：
新任务开始时，AI 必须从当前输入重新理解任务，不得沿用历史任务产物

## 禁止默认复用
- 旧任务的任务定义
- 旧任务的背景归纳
- 旧任务的成功标准
- 旧任务的正式产物
- 旧任务的 facts / business / experience 输出

即使新需求与旧需求内容相似，也不得直接沿用旧任务结果。

## 判断标准
链路跑通不等于真实任务完成。
新真实任务必须基于当前输入重新建立任务身份，不能把历史项目产物当作默认答案。