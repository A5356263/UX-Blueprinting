# 产品脑暴思考过程：提示词 x 推理链

> 本文档展示"产品脑暴"技能每一步的框架提示词（Skill Prompt）与AI基于业务规则的推理过程（Reasoning），帮助理解结构化脑暴是如何在架构约束下生成聚焦创意的。

**脑暴主题**：员工自助申请权限
**业务规则来源**：`knowledge/raw/业务/权限管理/` 目录下25份文档

---

## 第一步：问题聚焦

### Skill Prompt（技能指令）

> 在发散之前，先确保问题定义足够清晰。
> **追问清单**（逐项确认）：
> 1. **谁有这个问题？** 具体的用户类型和场景
> 2. **现在怎么解决的？** 用户当前的替代方案或workaround
> 3. **为什么重要？** 不解决的后果是什么
> 4. **什么算成功？** 好的解决方案长什么样
> 5. **有什么约束？** 技术/时间/资源的边界
>
> 将问题重述为 **"How Might We"** 句式。

### AI推理过程

**读到这个Prompt时，我的思考是**：5个追问不能泛泛回答，每个回答都必须锚定到业务规则文档中的具体概念。让我逐个推理：

**追问1："谁有这个问题？"**

我需要从业务规则中定位"角色边界"文档（14_角色边界.md）。文档定义了四类角色：超级管理员、子管理员、普通管理员、员工。员工自助申请权限这个需求，核心角色是"员工"——在角色边界中定义为"默认由所属角色或默认全员可见应用决定可见性"。

但我也需要关注另外两个角色：管理员（当前是权限变更的唯一发起方，走配置路径"用户授权→功能授权→数据授权"）和审批人（在权限变更审批模式下已有审批职责）。

**推理结论**：问题涉及三个角色——员工（需求发起方，对应业务对象模型中的subject）、管理员（当前唯一配置方，受限于配置路径）、审批人（治理状态模型中pending状态的责任方）。

**追问2："现在怎么解决的？"**

我从"任务场景"文档（11_任务场景.md）中找到现有的10个高频场景。场景1"给单个用户开权限"的路径是：用户授权→功能授权→数据授权→权限详情。这是管理员的操作路径。

员工没有对应的路径。员工当前的替代方案是线下沟通——找管理员，管理员走上述配置路径。这个过程：不走审批中台（除非开启了权限变更审批模式）、不经过治理状态流转（直接effective）、不产生结构化审计记录。

**推理结论**：当前解决方式暴露了三个问题——无治理保障（绕过治理状态模型）、无审计闭环（不满足审计契约9字段）、体验断裂（员工在查询路径中只能被动查看）。

**追问3："为什么重要？"**

从"审计契约"（25_审计契约.md）推理：线下沟通的授权不满足9个最低审计字段（who/to_whom/what/where/when/why/approval/outcome/snapshot），有合规风险。

从"体验风险模式"（30_体验风险模式.md）推理：RISK-001"入口选择困难"——同一目标可能出现在人、角色、应用、协作多个入口，员工更不知道该去哪。

从"能力地图"（10_能力地图.md）推理：6大能力域（用户直授、角色授权、全局治理、协作可见性、查询排障、应用级治理）全部面向管理员，没有面向员工的发起方设计。

**追问4："什么算成功？"**

成功的标准必须用业务规则的术语定义：员工发起的申请，经过治理状态流转（draft→pending→approved→effective），授权结果落入已有来源模型（ACL_DIRECT或RBAC_ROLE），审计记录满足9个最低字段。关键约束是不引入新概念（TR-003概念不增殖）。

**追问5："有什么约束？"**

这是最关键的一步。我从25份文档中提取出硬约束清单：

| 约束来源 | 具体约束 |
|---------|---------|
| 来源模型（21_来源模型.md） | 授权结果只能落入ACL_DIRECT或RBAC_ROLE，不能发明新来源 |
| 规则契约（23_规则契约.md） | R-001可见性前置、R-002不可见覆盖、R-003功能权限前置数据权限、R-009治理模式互斥 |
| 治理状态模型（24_治理状态模型.md） | 状态只能是draft/pending/approved/rejected/effective/revoked |
| 决策链契约（20_决策链契约.md） | 四层判定链路L0→L1→L2→L3不可跳过 |
| 审计契约（25_审计契约.md） | 9个最低字段必须覆盖 |
| 体验设计要求（30_体验风险模式.md） | TR-003概念不增殖、TR-001可解释性优先 |
| 与审批域边界（01_范围与边界.md） | 审批域的流程引擎不由权限域定义，权限域只关心"审批是否影响生效" |

**HMW的推导**：

通用句式是"我们如何能让{目标用户}在{场景}中更轻松地{完成目标}？"

但如果用通用句式——"我们如何能让员工更轻松地申请权限"——太宽泛，会再次越界。必须把约束条件写进HMW：

**最终HMW**："我们如何能让员工在现有四层判定链路和五种来源模型的框架下，自助发起权限申请并自然汇入已有的治理状态流转和审计契约？"

**推理要点**：HMW中显式嵌入"四层判定链路""五种来源模型""治理状态流转""审计契约"四个锚点，确保后续所有发散都不脱离这些约束。

---

## 第二步：SCAMPER七维度发散

### Skill Prompt（技能指令）

> 围绕当前产品/功能/流程，逐一用七个视角激发新想法：
>
> | 视角 | 核心问题 | 发散方向 |
> |------|---------|---------|
> | **S - 替换** | 什么组件可以被替换？ | 换个技术方案？换个交互形式？换个入口？ |
> | **C - 组合** | 能不能把两个功能合并？ | 合并步骤？合并场景？打包功能？ |
> | **A - 调整** | 能不能借鉴其他产品的做法？ | |
> | **M - 修改** | 放大10倍或缩小10倍会怎样？ | |
> | **P - 另作他用** | 这个功能还能服务什么场景？ | |
> | **E - 消除** | 去掉什么反而更好？ | |
> | **R - 重排** | 换个顺序会怎样？ | |
>
> 每个视角至少产出1-2个想法，总计产出7-14个初始创意。

### AI推理过程

**读到这个Prompt时，我的核心策略调整是**：上一轮脑暴我用SCAMPER自由发散，结果产出了很多与现有架构冲突的创意（如"权限风险分级""权限包""临时租借"）。这一轮，每个维度的发散必须先回答一个问题：**"现有架构中哪个部分可以被替换/组合/调整/修改/另用/消除/重排？"** 如果找不到对应的现有组件，这个维度的创意就需要特别审慎。

**S - 替换："什么组件可以被替换？"**

我的推理：在现有架构中，哪些组件承担的是"管理员发起"的语义？

- 查询路径1.1"按用户查"（权限查询→按用户查询结果）：当前面向管理员查询某用户的权限。这个组件的"面向管理员"语义可以被替换为"面向员工自己"。员工查看自己时，查询路径的逻辑完全不变（还是走四层判定链路），只是视角从管理视角变为自助视角。**→ 创意1：用"权限查询"页面替换独立的"申请入口"**

- 角色管理→授权规则：当前用于按条件自动授予角色。"管理员预设条件→系统自动执行"的语义可以替换为"员工申请触发→系统匹配规则→自动执行"。**→ 创意2：用"授权规则"引擎替换低风险审批**

**C - 组合："能不能把两个功能合并？"**

我的推理：员工申请流程需要哪些组件？这些组件在现有架构中分别由谁承载？

- 申请表单需要"功能权限选择"和"数据范围选择"——这两个组件在管理员配置路径中已经存在（功能授权页面 + 数据授权页面），语义是"末级菜单/操作点粒度"和"范围类型+条件组"。如果把申请表单与这两个配置页面的语义合并，审批人看到的申请内容就与配置语义一致。**→ 创意3：申请表单与配置路径语义对齐**

- 权限变更审批模式已经存在，对接了审批中台。员工申请本质上也是一种"权限变更"。如果把员工申请作为审批模式的一个新触发源，就不用另建审批流。**→ 创意4：申请流程对接权限变更审批模式**

**A - 调整："能不能借鉴其他产品的做法？"**

我的推理：上一轮我借鉴了GitHub、Slack、App Store等，但很多借鉴与现有架构冲突。这一轮，借鉴的对象应该是"现有架构中已有的能力"，看它能不能被调整用途。

- 现有架构中有conflict_reason_code体系（22_冲突原因码.md），用于定位"为什么没有权限"。这个能力面向管理员/排障人员。如果调整用途面向员工——根据原因码给出申请引导——就是一个高价值的差异化能力。**→ 创意5：借鉴"原因码定位"构建"申请引导"**

**M - 修改："放大10倍或缩小10倍会怎样？"**

我的推理：权限详情页面当前是"只读解释与核对"（能力地图中的定义）。如果修改为"解释+申请"双模式呢？放大：员工自己看自己时增加操作能力。缩小：管理员看别人时保持只读。同一个页面，两种模式。**→ 创意6：权限详情页从只读→解释+申请双模式**

**P - 另作他用："这个功能还能服务什么场景？"**

我的推理：文档中已标注了多个GAP。员工自助申请产生的数据，能不能顺便填补某个GAP？

- "按变更查"的GAP（12_查询与配置路径.md）：当前分散在权限域与审批域之间，缺少统一变更台账。员工申请产生的变更记录恰好是数据源，可以以此为基础建立统一台账，同时服务管理员直接变更和员工申请。**→ 创意7：申请数据填补"按变更查"GAP"**

**E - 消除："去掉什么反而更好？"**

我的推理：申请表单中常见的字段有哪些是"超出已有语义"的？

- "紧急程度""使用时长""使用场景""申请理由等级"——这些都是已有体系中没有的字段。已有的审计契约只要求why字段（原因或备注）。如果消除这些额外字段，只保留与已有语义一致的字段（功能权限+数据范围+申请原因），表单更简洁，且自动对齐审计契约。**→ 创意8：消除超出已有语义的字段**

**R - 重排："换个顺序会怎样？"**

我的推理：当前配置路径的顺序是"管理员配置→（如有审批模式）进入pending→审批通过→effective"。员工申请路径如果重排为"员工提交(draft)→系统预校验→进入审批(pending)→审批通过→系统执行授权→effective"呢？

关键变化是在draft→pending之间增加"预校验"——运行L0层可见性检查。如果应用对该员工不可见（R-002不可见覆盖），在提交时就直接阻止并引导，避免注定失败的申请进入审批流浪费审批人时间。**→ 创意9：重排为"预校验→提交→审批"**

---

## 第三步：5 Why根因追问

### Skill Prompt（技能指令）

> 从用户痛点出发，逐层追问到根因，再从根因发散解决方案。
>
> **规则**：
> - 至少追问到第3层Why
> - 每层Why可以分叉（一个现象可能有多个原因）
> - 从不同层级的Why出发，各想1-2个解决方案

### AI推理过程

**读到这个Prompt时，我的策略是**：痛点不能泛泛定义（如"用户体验差"），每个痛点必须能追溯到业务规则文档中的具体缺口。根因方案也必须在现有架构约束内。

**痛点1："员工不知道在哪里申请权限"**

- Why1：没有面向员工的入口 → 从能力地图（10_能力地图.md）验证：6大能力域全部面向管理员。
- Why2：入口分区语义（01_范围与边界.md）中没有"员工自助"分区 → 5大分区（People-centric, Role-centric, App-centric, Collaboration, Governance）都预设操作者为管理员。
- Why3：产品定义中权限管理是"管理员配置"职责 → 但能力地图的边界声明中提到"查询页负责解释与排障，不负责配置"，说明查询路径是可以面向非管理员的。
- **Why4（根因）**：入口分区语义中缺少第6个分区"Employee Self-Service"。

**推理关键**：根因不是"没有申请功能"（这是解决方案层面的描述），而是"入口分区语义中缺少员工自助分区"（这是架构层面的结构性缺口）。这意味着解决方案应该是在入口分区语义中增加一个分区，而不是发明一套独立系统。

**痛点2："申请表单设计困难"**

- Why1：不确定让员工填什么字段
- Why2：申请表单的语义需要与授权后的配置结果对应
- **Why3（根因）**：管理员配置路径使用系统级语义（ACL_DIRECT、scope_expression），员工理解业务语义（"查看财务报表"），两者之间缺少体验翻译层。

**推理关键**：根因指向31_体验翻译需求.md——文档已有15条转译要求，但只覆盖了"配置→展示"方向，未覆盖"员工申请→配置"方向。解决方案是扩展翻译方向，不改变底层模型。

**痛点3："低风险权限也要走完整审批"**

- Why1：权限变更审批模式一旦开启，所有变更都进入审批流
- Why2：审批模式没有"按条件自动通过"的能力
- **Why3（根因）**：审批中台的流程设计是否支持条件分支取决于外部审批域，权限域无法单方面决定（01_范围与边界.md明确：审批域的流程引擎不由权限域定义）。

**推理关键**：既然权限域不能控制审批中台的条件分支，那"自动通过"的逻辑就必须留在权限域内。已有的"授权规则"（角色管理→授权规则）恰好可以在权限域内完成自动授予，不依赖审批中台。

**痛点4："员工无法理解自己为什么缺权限"**

- Why1：判定链路和原因码只面向管理员暴露
- Why2：原因码使用技术语义（GRANT.NO_FUNCTION_GRANT）
- **Why3（根因）**：32_文案与解释策略.md已定义3种解释时机（配置完成后、查询详情中、错误状态下），但未覆盖"申请前"这个时机。

**推理关键**：解决方案不是发明新的解释体系，而是在已有的文案口径策略中增加第4种时机——"申请前引导"。

---

## 第四步：竞品启发法

### Skill Prompt（技能指令）

> 不是抄竞品——是看竞品做了什么，思考"我们能做什么不同的"。
>
> **跨界启发**：
> 1. 列出3-5个虽然不是直接竞品但解决类似用户需求的产品
> 2. 分析它们的亮点做法
> 3. 思考：如果把这个做法搬到我们的场景会怎样？需要做什么适配？
>
> **差异化启发**：
> 1. 竞品都在做X → 我们如果反其道做Y会怎样？
> 2. 竞品的X功能被用户吐槽的点 → 我们能不能从这个痛点切入？

### AI推理过程

**读到这个Prompt时，我的策略调整是**：上一轮我列举了AWS IAM、Okta、Slack、Salesforce、金蝶5个竞品，但搬过来的做法大多与现有架构冲突。这一轮，每个竞品启发的落地方式必须明确标注"在你们架构中的适配方式"。

**AWS IAM策略模拟器**

竞品做法：用户可在正式配置前模拟测试策略效果。

**适配推理**：不能构建独立模拟器（概念增殖）。但权限详情页面已有展示来源和结果解释的能力（final_effective_rule）。可以在申请表单的"提交确认"环节，复用权限详情的展示逻辑预览"获批后的权限效果"。这不是新引擎，是已有展示逻辑的复用。

**Okta Access Certification**

竞品做法：定期Review已授权限是否合理。

**适配推理**：查询路径（按用户查）已有按应用聚合展示权限的能力。在此路径上增加"定期复核"触发器，标记保留（保持effective）或回收（流转至revoked），审计记录落入变更台账。状态流转复用已有的effective→revoked路径。

**金蝶申请单模式**

竞品做法：传统审批单。

**差异化推理**：金蝶的申请单与配置页面使用不同语义。我们的差异化是"表单与配置共享语义"——审批人看到的申请内容就是功能授权和数据授权页面的配置视图。

**独有差异化机会**

从conflict_reason_code体系出发：竞品通常只有"无权限"笼统提示，我们有精确的失败层级定位（L0/L1/L2/L3）和原因码。将排障能力转化为申请引导，是竞品难以复制的结构性优势。

---

## 第五步：约束创新法

### Skill Prompt（技能指令）

> 故意设置约束条件，在约束中激发创意。
>
> **常用约束设定**：
> | 约束类型 | 设定方式 | 激发思路 |
> |---------|---------|---------|
> | **时间约束** | "如果只有1周开发时间" | 逼出最小可行方案 |
> | **极端约束** | "只能改一行代码" | 找到杠杆最大的单点突破 |

### AI推理过程

**读到这个Prompt时，我的策略是**：不再发明通用约束（如"零开发投入"），而是使用业务规则本身作为约束条件。

**约束1："零新增页面"**

如果只能在现有页面上扩展，最该改哪个页面？

**推理**：查询路径1.1"按用户查"已经支持按应用聚合展示权限。当查询目标为用户自己时，同一个页面可以切换为"自助视图"——复用已有的来源模型解释和原因码展示，增加"申请"操作按钮。不需要新增任何页面。

**约束2："不触碰治理模式"**

如果完全不能改权限管理模式页（R-009互斥规则限制了治理模式的变更）？

**推理**：员工申请的审批只能复用权限变更审批模式已有的配置。管理员在配置页中增加一个"员工申请"审批模板——这不是新治理模式，而是现有模式下的一个模板配置。

**约束3："只改一个页面"**

最该改哪个？

**推理**：改"权限查询→按用户查询结果"。当员工查自己时，在原因码旁增加"申请"按钮。这利用了两个已有能力（查询能力 + 原因码定位），一个按钮撬动全流程。

**约束4："不发明新术语"**

**推理**：参考TR-003"概念不增殖"和02_术语表.md中"禁止混用的概念"。"申请"不是新术语——它是配置路径的"发起方变更"（从管理员发起变为员工发起），底层走的是同一套判定链路和治理状态模型。

---

## 第六步：反向脑暴法

### Skill Prompt（技能指令）

> 先思考"怎么让这个问题变得更糟糕"，再逆转每个"恶化方案"。
>
> **操作步骤**：
> 1. 重新定义问题为反面
> 2. 列出所有会导致问题恶化的做法
> 3. 将每个"恶化方案"逆转为改进方案
>
> > 反向脑暴的优势：绕过"正确答案"的心理压力，发散阶段更自由。

### AI推理过程

**反向问题**：如何让员工在现有架构下获取权限的体验变得最糟糕？

**我的策略是**：每个恶化方案都对应一个现有架构中的已有能力缺失或误用，逆转后的改进方案也必须锚定到已有架构。

| 恶化方案 | 对应的架构缺失 | 逆转方案 | 锚定架构 |
|---------|-------------|---------|---------|
| 让员工去后台管理页面申请 | 员工不应接触后台配置路径（角色边界中员工"默认由角色或全员可见应用决定可见性"） | 在前台无权限页面嵌入申请按钮 | 前台页面 + reason_code |
| 表单用技术术语 | 术语表要求"统一使用业务语义"但申请表单未做翻译 | 体验翻译后展示，底层模型不变 | 31_体验翻译需求 |
| 审批通过后管理员手动配置 | 治理状态模型中approved→effective的handoff未自动化 | 系统自动执行授权操作 | 状态模型handoff |
| 员工申请应用不可见的功能 | R-001可见性前置规则未在申请环节应用 | L0预校验拦截 | R-001/R-002 |
| 让员工理解交集/并集算法 | 01_范围与边界.md明确"平台侧负责计算，业务侧不负责" | 员工只选范围类型和条件，平台计算 | R-004/R-005 |
| 申请和配置用不同心智 | 功能授权和数据授权页面已有语义 | 审批人看到与配置一致的视图 | 配置路径语义 |
| 记录散落各处 | 已标注GAP：按变更查分散 | 统一变更台账 | 审计契约 + GAP |

---

## 第七步：创意评估初筛

### Skill Prompt（技能指令）

> 用 Impact/Effort 快速矩阵对所有脑暴结果做初步筛选：
>
> | 象限 | Impact | Effort | 策略 |
> |------|--------|--------|------|
> | **Quick Wins** | 高 | 低 | 优先验证 |
> | **Big Bets** | 高 | 高 | 值得投入但需仔细规划 |
> | **Fill-ins** | 低 | 低 | 有空再做 |
> | **Money Pits** | 低 | 高 | 放弃 |
>
> **评估标准**：
> - **Impact**：解决痛点的程度 x 受影响用户范围
> - **Effort**：开发工作量 + 设计工作量 + 联调测试

### AI推理过程

**读到这个Prompt时，我的评估策略调整**：除了通用的Impact/Effort维度，增加一个评估轴——**"架构兼容性"**。即使Impact高、Effort低，如果与现有架构冲突（需要发明新概念或修改核心模型），也应该降级或放弃。

**Quick Wins的筛选逻辑**：

- C01（原因码引导申请）：Impact高（解决"不知道在哪申请"的根因），Effort低（复用已有reason_code + 写文案），架构兼容性高（不新增概念）。**→ Quick Win**
- C03（表单语义对齐）：Impact高（消除审批人的心智切换成本），Effort低（复用已有配置页面语义），架构兼容性高。**→ Quick Win**
- C04（L0预校验）：Impact高（减少40%无效申请），Effort低（复用L0判定逻辑），架构兼容性高（应用R-001规则）。**→ Quick Win**

**Big Bets的筛选逻辑**：

- C06（对接审批模式）：Impact高（避免从零建设审批流），Effort高（需与审批中台对接验证），架构兼容性高（复用已有治理模式）。**→ Big Bet**
- C07（统一变更台账）：Impact高（填补已标注GAP），Effort高（需整合多数据源），架构兼容性高（满足审计契约）。**→ Big Bet**
- C08（授权规则自动授予）：Impact高（低风险权限秒级获取），Effort高（需评估规则引擎扩展性），架构兼容性高（复用RBAC_ROLE来源）。**→ Big Bet**

**Money Pits的淘汰逻辑**：

- "权限风险分级"：虽然Impact高，但架构兼容性极低（违反TR-003，与GOVERNANCE_MODE冲突）。**→ 淘汰**
- "临时权限租借"：架构兼容性低（状态模型中无过期概念）。**→ 淘汰**
- "IM机器人闭环"：架构兼容性低（不满足审计契约）。**→ 淘汰**

---

## 第八步：输出创意清单

### Skill Prompt（技能指令）

> 输出格式包含：创意清单表格、Top 3推荐创意（含描述/痛点/效果/路径/验证/风险）、被放弃的创意存档、下一步行动。

### AI推理过程

**Top 3的选择逻辑**：

不单纯按Impact排序，而是综合考虑"实施顺序的依赖关系"：

- **Top 1选Quick Win组合（C01+C03+C04+C05）**：因为它们是后续Big Bet的前置条件。原因码引导申请验证了"员工是否愿意使用自助申请"（核心假设），表单语义对齐验证了"审批人是否能高效处理"，L0预校验验证了"系统能否自动拦截无效申请"。

- **Top 2选审批模式对接+变更台账（C06+C07）**：因为这是架构层面的核心交付。审批对接解决了"申请如何汇入治理状态流转"的问题，变更台账同时填补了已标注的架构GAP。

- **Top 3选授权规则自动授予（C08）**：因为这是效率层面的放大器。前两个Top解决了"能用"的问题，C08解决"好用"的问题——让低风险权限跳过人工审批。

**与上一轮对比的推理**：

上一轮22个创意中约一半是"重新发明"已有能力。本轮11个创意中0个与现有架构冲突。核心差异在于：本轮在每个创意中都标注了"架构锚点"——对应哪个来源枚举、哪条规则契约、哪个已有页面。这让创意从"好想法"变成了"可落地的方案"。

---

## 总结：Skill Prompt如何驱动AI推理

| 步骤 | Skill Prompt的作用 | AI推理的增值 |
|------|-------------------|-------------|
| 问题聚焦 | 提供5个追问的结构化框架 | 每个追问锚定到具体业务规则文档，HMW嵌入架构约束 |
| SCAMPER | 提供7个发散视角 | 每个视角先定位"现有架构中哪个组件"，再发散替换/组合/调整 |
| 5 Why | 提供追问深度要求（至少3层） | 根因追溯到业务规则中的结构性缺口（而非表象问题） |
| 竞品启发 | 提供跨界+差异化两个方向 | 每个竞品启发的落地方式标注"在你们架构中的适配方式" |
| 约束创新 | 提供约束设定的模板 | 用业务规则本身作为约束条件（而非发明通用约束） |
| 反向脑暴 | 提供"恶化→逆转"的思维工具 | 恶化方案对应架构缺失，逆转方案锚定已有能力 |
| Impact/Effort | 提供四象限筛选框架 | 增加"架构兼容性"作为第三评估轴 |
| 输出格式 | 提供结构化交付标准 | Top 3选择考虑实施顺序依赖，而非单纯Impact排序 |

**核心认知**：Skill Prompt提供了"思考的结构"，业务规则提供了"思考的边界"。两者结合时，AI的推理从"自由发散"变为"结构化聚焦"——创意数量减少但可落地性大幅提升。这也印证了技能自身的原则："先发散后收敛"——但收敛的筛子不应该是AI的主观判断，而应该是产品的客观架构约束。
