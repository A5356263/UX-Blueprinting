# 项目介绍

这是一个基于 Plugin / Skill 组装的轻工作流项目。

它不是传统的大而全系统，而是一套“按步骤协作”的智能工作台：把复杂工作拆成多个独立 Skill，每个 Skill 只负责一类事情，需要时再串起来使用。这样做的好处是结构更轻、职责更清楚，也更方便后续扩展新能力。

## 当前架构

项目当前可以理解为 3 层：

- `.claude`：Skill 能力层，存放各个可调用的 Skill
- `_shared`：协同规则层，定义 Skill 之间怎么衔接、什么时候衔接、用什么格式衔接
- `spark-output`：结果输出层，存放每一步正式产出的文档和结构化数据

## 主流程怎么流转

当前正式主流程很简单，核心只有两步：

1. `uxb`
   负责理解需求、分析问题、收敛判断，输出正式的需求定案。
2. `experience-blueprint`
   负责读取需求定案，展开成体验蓝图和交互方案。

也就是说，主链路是：

`uxb -> experience-blueprint`

除此之外，还有一些不强制进入主流程的支撑型 Skill，例如：

- `knowledge-wiki`：按需提供知识查询、知识候选、知识入库、知识维护
- `product-analysis`：当需求还不适合直接进入 UXB 时，先做产品分析
- 其他 Skill：根据场景补充专题能力

这些 Skill 更像“随用随取的插件能力”，而不是必须经过的固定步骤。

## `_shared` 的作用

`_shared` 是这套轻工作流的公共协调层，作用不是产出业务内容，而是保证不同 Skill 能稳定协作。

### `skill-graph.json`

这是 Skill 流转关系的定义文件，相当于整套工作流的“流程图”。

它说明：

- 当前有哪些核心 Skill
- 哪些 Skill 是主流程节点
- 哪些 Skill 是支撑型能力
- 一个 Skill 完成后，下一步推荐进入哪个 Skill

例如现在就明确了：`uxb` 完成后，推荐进入 `experience-blueprint`；而 `knowledge-wiki` 属于基础设施型 Skill，不参与主流程排队。

### `ready-determination.md`

这是 Skill 的“启动规则”。

它说明：

- 什么情况下某个 Skill 算“可以开始”
- 是否已经具备上一步的输入条件
- 推荐启动什么，不推荐启动什么

它的作用是防止流程乱跳。简单说，就是避免还没完成需求定案，就直接进入后面的方案展开。

### `handoff.md`

这是 Skill 之间的“交接规范”。

它规定一个 Skill 做完后，要怎样把结果交给下一个 Skill，包括：

- 完成后怎么做一句话总结
- 下一步怎么推荐
- 给用户什么触发语，方便继续往下走

它的作用是让整个流程在切换阶段时更自然，不会出现“做完上一步，但不知道下一步怎么接”的情况。

### `context-schema.md`

这是结构化上下文的“数据接口定义”。

它规定每个 Skill 输出的 JSON 应该有哪些字段、字段含义是什么、格式怎么统一。

它的作用是让下游 Skill 能稳定读取上游结果，而不是靠自然语言猜测。  
简单理解，就是把“文档交接”变成“文档 + 结构化数据交接”。

## `spark-output` 的作用

`spark-output` 是正式输出区，用来存放每一步的结果。

当前主要包括：

- `uxb_output.md`：需求定案文档
- `experience_blueprint.md`：体验蓝图文档
- `context/`：对应的结构化 JSON，上下游 Skill 用它来衔接
- `preview/`：预览类结果

所以它既是结果存档区，也是流程继续往下执行时的输入来源。

## 一句话总结

这个项目本质上是一套以 Skill 为核心、以 `_shared` 为协同规则、以 `spark-output` 为结果出口的轻量智能工作流。

主流程负责“从需求到方案”，支撑型 Skill 负责“按需补充能力”，整体目标是让复杂工作拆解得更清楚、更稳定、更容易复用。
