# AGENTS.md

## 编码执行约束

### 1. 先思考再编码
- 不得静默假设
- 有歧义先说明可选解释
- 有更简单方案需主动指出
- 不清楚时停止猜测

### 2. 简单优先
- 不做未被要求的能力
- 不为单次使用做抽象
- 不为假想场景补配置
- 避免过度设计

### 3. 外科式修改
- 只改与当前任务直接相关的代码
- 不顺手改无关注释、格式、结构
- 只清理本次改动产生的无用代码
- 发现历史问题只提示，不擅自处理

### 4. 目标驱动执行
- 先定义成功标准，再开始实现
- 能写测试先写测试
- 每一步都要有验证方式
- 不以“差不多能跑”作为完成标准

## 语言与沟通规则

### 1. 可见输出语言

- 所有可见输出必须使用简体中文。
- 包括：执行计划、操作确认、风险提示、错误说明、修改总结、验证结果、下一步建议。
- 修改文件前，必须用中文说明：
  - 准备修改什么
  - 为什么修改
  - 影响范围是什么
- 执行命令前，必须用中文说明命令目的。
- 若展示 thinking summary、plan、analysis、decision 等可见推理摘要，也必须使用中文。
- 代码、命令、文件路径、字段名、API 名称可保留英文原文。
- 不要输出英文解释，除非是日志、报错、代码或专有名词原文。

### 2. 文件描述语言

- 任务输出与优化文件中的描述类信息统一使用中文表达。
- 不使用英文描述，除非是代码、配置字段、路径、命令、日志或专有名词原文。

## 路径与目录规则

1. 正式主链路输出统一写入 `spark-output/`。
2. 业务知识与设计准则统一从 `.Codex/skills/knowledge-wiki/knowledge/` 读取（项目根目录的 `knowledge/` 仅作备份保留，不再作为消费源）。
3. `input/` 是用户输入材料的统一存放目录，不是自动选择入口。
4. 只有用户明确提供具体文件路径、附件、粘贴正文，或明确要求查看目录时，才允许读取输入材料。
5. 禁止仅因 `input/` 存在文件而自动扫描、枚举、排序、选择或推荐输入文件。
6. 跨 Skill 协调统一通过项目根目录下的 `shared-workflow/` 完成。
7. Skill 定义统一位于 `.Codex/skills/`。

## 跨系统稳定性原则

1. 项目实现不得偏向单一系统环境，默认必须保证 Windows、macOS、Linux 下都能稳定运行。
2. 不得依赖某一种系统特有的路径习惯、编码表现、命令语法、权限行为或文件分隔符。
3. 所有路径、格式、脚本入口和文件处理逻辑，都应优先采用跨平台写法，避免因系统差异阻塞主链路。
4. 只要存在跨系统运行可能，验证时就不能只在单一环境下判断“可用”，至少要显式考虑 Windows、macOS、Linux 的兼容性。

## 能力模块边界规则

1. `.Codex/skills/uxb/` 负责需求分析定案。
2. `.Codex/skills/experience-blueprint/` 负责体验蓝图输出。
3. `shared-workflow/` 存放跨 Skill 协调文件，包括流转关系、context schema 和交接模板。
4. `.Codex/skills/knowledge-wiki/` 是业务知识的正式维护方，内嵌的 `knowledge/` 是唯一消费源。项目根目录的 `knowledge/` 仅作备份保留。
5. `input/` 是用户输入层，`spark-output/` 是输出层。输入层只定义存放位置，不代表 Agent 获得自动发现或选择文件的权限。
6. Skill 之间通过 `shared-workflow/` 定义的协议交接，不直接调用对方。
7. `packages/experience_preview/` 仅作为 HTML 预览参考实现保留，不再承担正式主链路入口职责。

## Git 提交规则

1. 当我说"提交 github"时，表示提交并推送到远程当前分支。
2. 每次提交仅包含当前工作树最新内容，不包含之前会话遗留的暂存。
3. 涉及删除、回退等危险操作时，暂停并询问我。

## 适用范围
- 本文件用于约束 AI 编程助手在本项目中的执行方式与行为边界。
