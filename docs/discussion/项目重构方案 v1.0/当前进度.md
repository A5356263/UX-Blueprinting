这是我最后的指令：愿意，另外tasks/：不需要了。它是旧主线，应该彻底退出。
artifacts/：不需要了。归档已经进 projects/<id>/exports/ 了。
checks/：不该再作为顶层目录存在。它里面这些内容现在更适合并进规则层或执行层。这个任务也处理掉


我这一步会做两件事一起收身：一是把旧主线目录 tasks/、artifacts/、顶层 checks/ 彻底退场，二是把 projects/_template 和项目可读结构收轻，尽量只保留人真正会看的东西。


Ran 5 commands
我会先把“活跃引用”里的 checks/ 替换掉，再把旧目录和过重模板一起收掉。这样删目录后不会留下主线断链。

我先补一轮精确读取，再按实际文本落 patch，避免编码和旧稿差异把这次收身卡住。


Ran 5 commands
我这轮会直接把主线说明文件也收轻，不只删目录名。这样删完 checks/tasks/artifacts 后，仓库读起来也不会再像两套体系拼在一起。


Created 8 files, edited 2 files, deleted 4 files
我已经把主线说明和任务卡引用收过来了。接下来把模板目录和旧根目录物理删掉，再重跑一次试点装配，确保不会再引用被删的老目录。


Ran 3 commands
背景信息已自动压缩
我先把这轮“轻量化收身”做到底：删掉旧主线目录，收紧 projects/_template 和 packages 的默认结构，然后重新跑一遍试点项目，确保现在的人类阅读路径足够简单。


Ran 4 commands
我准备把项目目录再收紧一层：让人主要只看 source/ 和 workspace/，机器文件收进 runtime/，同时把旧的顶层目录彻底移除。这样后面给别的 Agent 用时，阅读路径会直接很多。


Ran 4 commands
我准备再做一个关键收口：把“人看的阶段判断”移到 workspace/，把 runtime/ 只留机器状态，这样每个项目基本就只剩 3 份结果文档 + 3 份阶段判断文档可读。然后顺手把旧目录彻底删掉。


Ran 4 commands
我这一步会直接动结构，不只是文档：把阶段报告改成人可读地落到 workspace/，同时把 _template 和 bootstrap 收成最小骨架，最后再把旧目录物理删除。


Ran 3 commands
已处理 5m 9s
我在收最后这层：把项目模板和命令输出改成“人看结果、机器看状态”的最小结构，同时去掉归档快照这类对你现在阶段没必要的重量。这样后面试需求时，目录会明显顺眼很多。

文件已删除
core.py
+0
-120

from __future__ import annotations

import json
from pathlib import Path

from packages.common import (
    get_project_dir,
    get_project_exports_dir,
    get_project_runtime_dir,
    get_project_source_dir,
    get_project_workspace_dir,
    get_repo_root,
)


TEMPLATE_MAP = {
    "task_card.template.md": "task_card.md",
    "facts.template.md": "facts.md",
    "business_blueprint.template.md": "business_blueprint.md",
    "experience_blueprint.template.md": "experience_blueprint.md",
    "gap_list.template.md": "gap_list.md",
    "check_report.template.md": "check_report.md",
    "check_status.template.json": "check_status.json",
}


def render_template(content: str, task_id: str, task_name: str, domain: str) -> str:
    return (
        content.replace("{{TASK_ID}}", task_id)
        .replace("{{TASK_NAME}}", task_name)
        .replace("{{DOMAIN}}", domain)
    )


def write_text(path: Path, content: str, force: bool) -> None:
    if path.exists() and not force:
        return
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding="utf-8")


def run_task_bootstrap(task_id: str, task_name: str = "Demo Task", domain: str = "permission", force: bool = False) -> int:
    repo_root = get_repo_root()
    templates_dir = repo_root / "templates"
    project_dir = get_project_dir(task_id)
    source_dir = get_project_source_dir(task_id)
    workspace_dir = get_project_workspace_dir(task_id)
    runtime_dir = get_project_runtime_dir(task_id)
    exports_dir = get_project_exports_dir(task_id)

    for path in [
        source_dir / "attachments",
        workspace_dir,
        runtime_dir / "context_bundle",
        runtime_dir / "gates",
        runtime_dir / "run_logs",
        runtime_dir / "snapshots",
        exports_dir / "final",
        exports_dir / "checks",
    ]:
        path.mkdir(parents=True, exist_ok=True)

    for template_name, output_name in TEMPLATE_MAP.items():
        template_path = templates_dir / template_name
        output_path = source_dir / output_name if output_name == "task_card.md" else workspace_dir / output_name
        rendered = render_template(
            template_path.read_text(encoding="utf-8"),
            task_id=task_id,
            task_name=task_name,
            domain=domain,
        )
        write_text(output_path, rendered, force)

    requirement_content = "\n".join(
        [
            "# Requirement",
            "",
            f"- Task ID：{task_id}",
            f"- Task Name：{task_name}",
            f"- Domain：{domain}",
            "- 请在此粘贴原始需求正文",
            "",
        ]
    )
    background_content = "\n".join(
        [
            "# Background",
            "",
            "- 请在此补充背景资料、约束、相关链接或历史上下文",
            "- 若存在必须阅读的原始附件，也在此说明",
            "",
        ]
    )

    write_text(source_dir / "requirement.md", requirement_content, force)
    write_text(source_dir / "background.md", background_content, force)
    write_text(source_dir / "attachments" / ".gitkeep", "", force)
    write_text(runtime_dir / "context_bundle" / ".gitkeep", "", force)
    write_text(runtime_dir / "gates" / ".gitkeep", "", force)
    write_text(runtime_dir / "run_logs" / ".gitkeep", "", force)
    write_text(runtime_dir / "snapshots" / ".gitkeep", "", force)
    write_text(exports_dir / "final" / ".gitkeep", "", force)
    write_text(exports_dir / "checks" / ".gitkeep", "", force)
    write_text(
        project_dir / "meta.json",
        json.dumps(
            {
                "project_id": task_id,
                "task_name": task_name,
                "domain": domain,
                "status": "draft",
            },
            ensure_ascii=False,
            indent=2,
        ),
        force,
    )

    print(f"Project created: {project_dir}")
    return 0
文件已创建
core.py
+110
-0

from __future__ import annotations

import json
from pathlib import Path

from packages.common import (
    get_project_dir,
    get_project_exports_dir,
    get_project_gates_dir,
    get_project_source_dir,
    get_project_workspace_dir,
    get_repo_root,
)


TEMPLATE_MAP = {
    "task_card.template.md": ("source", "task_card.md"),
    "facts.template.md": ("workspace", "facts.md"),
    "business_blueprint.template.md": ("workspace", "business_blueprint.md"),
    "experience_blueprint.template.md": ("workspace", "experience_blueprint.md"),
    "gap_list.template.md": ("workspace", "gap_list.md"),
    "check_report.template.md": ("workspace", "check_report.md"),
    "check_status.template.json": ("workspace", "check_status.json"),
}


def render_template(content: str, project_id: str, task_name: str, domain: str) -> str:
    return (
        content.replace("{{TASK_ID}}", project_id)
        .replace("{{TASK_NAME}}", task_name)
        .replace("{{DOMAIN}}", domain)
    )


def write_text(path: Path, content: str, force: bool) -> None:
    if path.exists() and not force:
        return
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding="utf-8")


def run_task_bootstrap(
    project_id: str,
    task_name: str = "Demo Task",
    domain: str = "permission",
    force: bool = False,
) -> int:
    repo_root = get_repo_root()
    templates_dir = repo_root / "templates"
    project_dir = get_project_dir(project_id)
    source_dir = get_project_source_dir(project_id)
    workspace_dir = get_project_workspace_dir(project_id)
    gates_dir = get_project_gates_dir(project_id)
    exports_dir = get_project_exports_dir(project_id)

    for path in [source_dir, workspace_dir, gates_dir, exports_dir]:
        path.mkdir(parents=True, exist_ok=True)

    for template_name, (target, output_name) in TEMPLATE_MAP.items():
        template_path = templates_dir / template_name
        output_root = source_dir if target == "source" else workspace_dir
        output_path = output_root / output_name
        rendered = render_template(
            template_path.read_text(encoding="utf-8"),
            project_id=project_id,
            task_name=task_name,
            domain=domain,
        )
        write_text(output_path, rendered, force)

    requirement_content = "\n".join(
        [
            "# Requirement",
            "",
            f"- Project ID: {project_id}",
            f"- Task Name: {task_name}",
            f"- Domain: {domain}",
            "- 请在这里粘贴原始需求正文。",
            "",
        ]
    )
    background_content = "\n".join(
        [
            "# Background",
            "",
            "- 请在这里补充背景资料、约束、相关链接或历史上下文。",
            "- 如果目前信息不足，也请显式写出缺失信息。",
            "",
        ]
    )

    write_text(source_dir / "requirement.md", requirement_content, force)
    write_text(source_dir / "background.md", background_content, force)
    write_text(
        project_dir / "meta.json",
        json.dumps(
            {
                "project_id": project_id,
                "task_name": task_name,
                "domain": domain,
                "status": "draft",
            },
            ensure_ascii=False,
            indent=2,
        ),
        force,
    )

    print(f"Project created: {project_dir}")
    return 0
You've hit your usage limit. To get more access now, send a request to your admin or try again at Apr 8th, 2026 2:39 AM.