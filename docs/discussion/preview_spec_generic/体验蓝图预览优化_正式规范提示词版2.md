请继续优化体验蓝图预览生成逻辑。注意：这是对预览生成器的持续优化，不是一次性手改当前 html 页面。目标文件可参考：

- `examples/positive/real-self-apply-v1/runtime/preview/index.html`
- `examples/positive/real-self-apply-v1/workspace/experience_blueprint.md`

本次优化要求如下：

# 总原则

1. 预览 html 中展示的所有信息，必须 100% 来源于 `examples/positive/real-self-apply-v1/workspace/experience_blueprint.md`。
2. 预览层不得新增、改写、删减业务信息，只允许做可读性重组与样式优化。
3. 不要把多个原文信息合成为新的解释性文案。
4. 不要在预览态下对原文做“提炼后改写”，只能原义搬运与结构重排。
5. 本次改的是预览生成逻辑与模板，不是手工修改当前项目下已生成的单个 html。

# 一、流程图区优化

1. 判断说明区不要做成全局统一的单独区域。
2. 每一条流程链路自己的判断逻辑、阻断逻辑、依赖逻辑，必须紧贴该流程图本身展示，放在该流程图卡片的下方，间距16PX。
3. 判断说明区使用纯文案形式展示，不再做单独大模块，不再做远离流程图的集中展示。
4. 每条流程的判断说明只承接该流程自身内容，不混入其他流程。
5. 判断说明区要保持信息层级清楚：
   - 先显示“依赖 / 前提”
   - 再显示“判断 / 阻断”
   - 再显示“失败后去向 / 回退关系”
6. 这些信息仍然必须来源于体验蓝图原文，不得自行解释补写。

# 二、Experience Blueprint 顶部卡片优化

将以下信息放入 `Experience Blueprint` 顶部卡片内，以正文层级清晰、紧凑的方式展示，整体文字尽量小、占高尽量低：
-  目标用户与角色：超管、员工申请人、审批人，以及需要解释规则的实施与运营支持角色。
- 体验目标：让用户清楚理解“能否启用、能申请什么、申请后何时生效、为什么失败、关闭模式为什么被拦截”，避免把治理限制误读为系统异常。
- 任务边界：覆盖模式启停、路径选择、范围配置、审批结果回写、我的权限查询、申请记录、关闭模式拦截这条主链路。
以下信息摘要信息现在就有的，需要做最小化处理
- 该页面仅用于本地阅读正式体验蓝图的派生预览，不回写正式蓝图，也不新增业务语义。
- 项目：real-self-apply-v1
- 来源：E:\AI设计\体验蓝图构建思路\projects\real-self-apply-v1\workspace\experience_blueprint.md

要求：
1. 以上信息统一放在顶部卡片内，不要拆成额外区块。
2. 用最小必要文字展示，不要做过大的上下留白。
3. 顶部卡片整体高度继续压缩，避免占用过多纵向空间。
4. 不要加入额外解释文案。

# 四、信息保真要求

1. 预览中的信息必须全部来自体验蓝图原文。
2. 不允许为了“更好读”而删掉原文项。
3. 不允许为了“更好看”而合并不同原文项后重写为一句新的总结。
4. 不允许把多个字段重新解释成新的说明文字。
5. 只能做：
   - 原文字段归组
   - 原文顺序重排
   - 原文层级展示
   - 原文样式压缩
6. 所有页面卡、流程卡、顶部卡的信息都必须满足这个原则。

# 五、实现边界

1. 本次不处理页面大布局语义扩展，不新增 topbar / sider / 左右栏知识推断规则。
2. 本次不改业务蓝图，不改体验蓝图正文。
3. 本次只改预览生成逻辑、预览模型映射逻辑、html 模板结构、css 样式。
4. 目标是让任何项目重新生成 preview 后，都自动得到新效果，而不是只修当前案例。

# 六、验收标准

请按以下标准自检：

1. 顶部 `Experience Blueprint` 卡片更紧凑，且包含：
   - 只读说明
   - 项目名
   - workspace 来源路径
2. 每条流程链下方都有自己独立的判断说明区，不再有全局统一判断说明区。
4. 页面信息不丢失，原文不改写，原文不删减。
5. 整体纵向控件明显收紧，阅读效率提升。
6. 修改后请重新生成预览，并确保输出新的本地预览地址。