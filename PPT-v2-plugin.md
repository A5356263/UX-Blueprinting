# SparkSkillsHub 团队宣讲 PPT 框架（v2 · Plugin 架构）

## 全局约束
- **受众**：全团队（产品、设计、运营），非纯技术岗
- **调性**：不讲架构、不讲代码、不讲工程细节
- **目标**：认知建立 + 价值说服，讲清楚"为什么这种形式好"
- **预设页数**：5 页（1 封面 + 4 内容页）
- **核心原则**：每页只讲一件事，标题即结论，内容区块不超过 3 个，少即是多

### 视觉风格

完全继承 PPT.MD 的视觉规范：

| 用途 | 色值 | 说明 |
|---|---|---|
| 主色 | `#1E3A5F` | 深蓝灰——标题、重点文字、导航元素 |
| 强调色 | `#F59E0B` | 暖琥珀——关键数字、核心观点、行动号召 |
| 背景 | `#FAFAFA` | 米白——全局底色 |
| 卡片 | `#FFFFFF` | 纯白——加极淡阴影（`0 1px 3px rgba(0,0,0,0.06)`） |
| 正文 | `#2D2D2D` | 炭黑——正文颜色 |
| 辅助文字 | `#888888` | 中灰——说明性小字、脚注 |
| 分割线 | `#E8E8E8` | 浅灰——卡片边框、分割线 |

字体：思源黑体 / 阿里巴巴普惠体（标题 Bold，正文 Regular）。
排版：每页只讲一件事；内容区块不超过 3 个；正文区域不超过页面面积 80%；卡片间距充足；不用图标装饰、不用渐变背景、不用动画。

---

## 第 1 页：SparkSkillsHub 是什么

### 视觉布局
- **上半部分**：大标题 + 一句话定义
- **中间偏下**：7 个 Skill 展示——管线 Skill 一排（5 个），基础 Skill 一排（2 个）
- **底部**：关于"Plugin"概念的补充说明（浅灰横条）

### 标题
> SparkSkillsHub：一套按设计流程分工协作的 AI 技能包

### 标题下方小字
> 基于 Claude Code 运行。7 个 Skill 各司其职，在同一个体验设计大场景下覆盖完整链路。

### 补充说明："Plugin"在各产品中的定义
> "Plugin"目前不是行业标准。同样叫 Plugin，Claude Code 里是 Skill，Cursor 里是 Rules，OpenAI 里是 Custom GPT——每家定义都不一样。SparkSkillsHub 的 Skill 不是一段系统提示词，而是把体验设计的完整工作方法，拆成了结构化的专业剧本。每个剧本管一个环节，剧本之间能自动交接。

### 一句话定义（标题下方，字号次大）
> 把设计需求放进去，设计方案流转出来——每个阶段有专门的 AI 技能负责，上一个做完交给下一个，不需要从零开始。

---

### 7 个 Skill 展示

**主链路 Skill（5 个，按流程从左到右）**

| 需求分析 | 体验策略 | 异常态 | 设计文档 | 设计走查 |
|---|---|---|---|---|
| 需求分析定案 | 交互流程与页面方案 | 穷举每屏异常态 | 情绪 → 色彩方案 | 独立走查审查 |

**基础支撑 Skill（2 个）**

| 业务知识库 | 产品分析 |
|---|---|
| 业务域知识 + 设计准则，供所有 Skill 消费 | 需求方向不清晰时，先做问题重定义 |

### 视觉参考
- 主链路 5 个 Skill 用横向箭头串联（→），每个一个圆角卡片
- 基础支撑 2 个 Skill 放第二行，用虚线框或浅底色区分"按需调用"
- 底部补充说明用浅灰横条，小字
- 整体：专业、干净、信息不堆砌

---

## 第 2 页：为什么拆成 7 个，而不是一个"超级 Prompt"

### 视觉布局
- **左侧**：塞成一个会怎样（问题卡片）
- **右侧**：拆开之后发生了什么（方案卡片）
- **底部**：一句总结

### 标题
> 为什么拆成 7 个 Skill，而不是一个"万能 Prompt"？

---

### 左侧卡片：塞成一个

当一份超级文档把"分析时不要替需求编造"和"做设计时要主动补全方案"塞在一起——这两条指令互相矛盾。AI 困惑：此刻该保守还是发散？

结果：什么都能聊，但每个专业环节都不够深。就像一个设计师同时当用研、交互、视觉和 QA，哪个角色都做不到位。

---

### 右侧卡片：拆成 7 个

每个 Skill 一次只聚焦一种思维模式：

- **需求分析** → 诚实的提炼者：需求没说的不替它编
- **体验策略** → 交互方案构建者：在已定边界内展开
- **异常态** → 状态穷举者：6 类 25 子类型全覆盖
- **设计文档** → 视觉推导者：情绪映射色彩方案
- **设计走查** → 独立审查者：拿外部标准逐条核对

互不干扰，各用各的专业方法。

---

### 底部一句话
> 拆的不是功能，是思维方式。一个超级 Prompt = 一个人同时穿 5 套制服，哪套都穿不好。

### 视觉参考
- 左右两栏，左栏用浅灰/警示色调表示问题，右栏用主色表示解决
- 每栏信息简洁，突出类比
- 底部一句话用深蓝灰收束

---

## 第 3 页：Skill 之间怎么协同——每个只读自己需要的

### 视觉布局
- **上半部分**：5 节点精简流程图 + 核心机制说明
- **下半部分**：两种环境下的协同方式（左右两张卡片）

### 标题
> 每个 Skill 知道自己该看什么——不用翻全部聊天记录

---

### 上半部分：核心机制

```
需求分析 → 体验策略 → 异常态 / 设计文档 → 设计走查
```

每个 Skill 完成后，把关键信息打包成一份"交接单"（结构化 JSON 文件）。下一个 Skill 只看交接单，不用在聊天记录里大海捞针。

---

### 下半部分：两种环境，同一套机制

#### 卡片 A：有文件系统时（当前 Claude Code 环境）

上游 Skill 把完整结果写入项目目录。下游 Skill 扫描目录，精准读取，干净持久，跨会话不丢。

#### 卡片 B：无文件系统时（后续兼容纯对话 Agent）

Chain 协议：上游 Skill 在对话中输出轻量标记指向自己的产出。下游扫描标记定位上游结果，不需要读完整个对话窗口。同一套逻辑，载体从文件变成对话标记。

---

### 底部一句话
> 本质就是：**每个 Skill 给自己打包好"交接单"，下一个只读交接单，不翻历史。**

### 视觉参考
- 流程图简洁：4 组圆角卡片 + 箭头
- 下方两张卡片左右排列，用"有文件 / 无文件"对比
- 克制，不比第 2 页复杂

---

## 第 4 页：灵活性 + 扩展性——不同场景，按需组合

### 视觉布局
- **上半部分**：标题
- **主体**：三个典型设计场景的 Skill 组合（三行或三列）
- **底部**：扩展性一句话

### 标题
> 不同场景，不同组合——像搭积木，不是一键全跑

---

### 三组典型设计场景

#### 场景 1：只做需求评审

**用的 Skill**：需求定案 → 设计走查

把需求文档丢进去，需求定案拆解后，设计走查独立审查"有没有遗漏、有没有矛盾"。不需要跑蓝图和异常态。

**就像**：有经验的评审专门看你的需求文档，指出哪有问题。

---

#### 场景 2：完整设计方案输出

**用的 Skill**：需求定案 → 体验蓝图 → 异常态 → 视觉情绪板 → 设计走查

全链路。从需求分析到交互方案，到异常覆盖，到视觉方向，最后独立走查。每个环节专人负责，自动交接。

**就像**：完整项目组——用研出报告、交互出方案、视觉定方向、QA 做审查。

---

#### 场景 3：只诊断一个现有页面

**用的 Skill**：需求定案（快速）+ 设计走查

描述要诊断的页面，需求定案快速提炼关键信息，设计走查拿 10 类检查清单诊断。不需要先跑完整蓝图。

**就像**：找资深设计师帮你看看这个页面有什么体验问题。

---

### 底部：扩展性

> 想加新能力？写一份"技能剧本"（该读谁、该交给谁），在团队花名册注册一下，新 Skill 就接入链路。不用改已有 Skill，不用推倒重来。**用着用着就长全了。**

### 视觉参考
- 三组场景用三行横向条或三张卡片
- 每组 Skill 组合用 pill 标签展示流程
- 底部扩展性用暖琥珀强调

---

## 第 5 页：现在在哪，往哪走

### 视觉布局
- **上半部分**：标题
- **中间**：两个状态卡片左右排列——"现在"和"未来"
- **下半部分**：行动号召

### 标题
> 核心链路已跑通，现在需要的，是大家一起用起来

---

### 两个状态卡片

#### 卡片 A：现在

核心主链路（需求定案 → 体验蓝图 → 异常态 / 视觉情绪板 → 设计走查）已跑通。在权限管理、审批流程等真实需求上完成端到端测试。

7 个 Skill 全部可用。输出质量核心指标已验证——AI 是否真的在用知识推理（而非假意推理）、是否在复述需求（而非真正分析）——均合格。

---

#### 卡片 B：未来

一个人的使用是验证可行性，一群人的使用才是在测试上限。

更多业务域、更多需求类型涌入后，才能真正暴露哪些环节判断不够准、哪些产出不够好、知识库哪些域该补。Chain 协议后续落地后，这套 Skill 也能在不支持文件系统的纯对话 Agent 上运行，不锁定在单一平台。

---

### 核心表达（居中，暖琥珀大字）

> 不是"做好了再给你们用"，是"你们用了才能做好"。

### 行动号召

**主行动（按钮风格，暖琥珀底）**：
> 打开 Claude Code，说一句"进入需求定案"，然后描述你要做什么。

**小字**：
> 不需要安装、不需要部署、不需要配环境。三步：解压 → 改名 → 打开。

### 视觉参考
- 两个状态卡片左右排列，"现在"标签暖琥珀、"未来"标签深蓝灰
- 核心表达 32-36px，暖琥珀色，居中，视觉锚点
- 底部行动号召简洁干净，收尾有温度
